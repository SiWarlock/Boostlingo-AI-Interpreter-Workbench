# AI Interpreter Workbench `server/` — Build Guide

> **You're in `server/`.** This file plus root `CLAUDE.md` both load. The root file covers global project conventions + shared comm rules (track-prefix, escalation taxonomy, messaging budget); this file owns code-area conventions for the .NET backend.

## Launch protocol

| Working on... | cwd | Loads |
|---|---|---|
| Planning / docs / commits | repo root (`ai-interpreter-workbench/`) | root `CLAUDE.md` only |
| the .NET backend code | `server/` | this `CLAUDE.md` + root |

<!-- For a multi-area project, add a row per additional code area. -->

If you find yourself fighting the wrong conventions, check your cwd.

## Session start/end protocol

**At session start:**
1. Read `MVP_TASKS.md` (repo root) → "Currently in progress" section.
2. Confirm with the user what feature this session is targeting.
3. Read the relevant section of `ARCHITECTURE.md` from the lookup table below.

**At session end** (only when the user explicitly says we're done):

1. **Implementer runs `/session-end`.** Implementer writes ONLY:
   - `server/` code files (the slice's implementation)
   - test files (the slice's tests)
   - dependency manifest / lockfile (deps the slice adds)
   - `docs/sessions/<NNN>-<date>-<topic>.md` (session doc, created at `/session-end` Step 5)

   **Implementer must NOT touch (all orchestrator territory):**
   - `MVP_TASKS.md`
   - `server/LESSONS.md`
   - `server/CLAUDE.md` (entire file — both the Cross-doc invariants table AND the Lessons logged index)
   - `ARCHITECTURE.md`
   - `docs/orchestrator-briefing.md` / `docs/tdd-brief-template.md` / `docs/briefs/` / `docs/runbooks/`
   - other top-level deliverable / design docs
   - `.gitignore` and root-level dotfiles (unless adding a new artifact to ignore, flagged at Step 9)

   At the slice's Step 10 commit, **explicit `git add <path>` for each slice file**; **never `git add -A`** or `git add .`; **never stage an orchestrator-territory file**. If the slice surfaces a change to any orchestrator-territory file (new model needing a cross-doc table row, a lesson candidate, an architecture note), the implementer **flags it at Step 9** per the routing matrix in `docs/orchestrator-briefing.md`. The orchestrator writes the change hot during the same session — working-tree state stays aligned within the round even though commits stagger.

2. **Orchestrator runs `/orchestrate-end`** for round close-out + Carry-forward triage + round terminal commit + push.

## Lookup table — where to find canonical info

Don't paste these sections into the prompt. Grep the file:section, read only what you need. `/check-arch <topic>` dispatches off this table.

| Topic | File (relative to repo root) | Section |
|---|---|---|
| Domain model & lifecycle (records, enums, invariants) | `ARCHITECTURE.md` | §3 / ARCH-005 |
| Repository scaffold (project layout) | `ARCHITECTURE.md` | §5 / ARCH-006 |
| Backend architecture + layering | `ARCHITECTURE.md` | §5 / ARCH-008 |
| API contracts (sessions, turns, config, realtime token, cascade WS, evaluation) | `ARCHITECTURE.md` | §6 / ARCH-009 |
| Realtime broker (client_secrets mint, event ingest) | `ARCHITECTURE.md` | §7 / ARCH-010 |
| Cascade orchestrator + streaming pipeline | `ARCHITECTURE.md` | §8 / ARCH-011 |
| Provider interfaces, ProviderError, fakes, timeout/cancellation | `ARCHITECTURE.md` | §9 / ARCH-012 |
| Metrics / latency model + MUST/nice tiers | `ARCHITECTURE.md` | §10 / ARCH-013 |
| Cost estimation + pricing.json | `ARCHITECTURE.md` | §11 / ARCH-014 |
| WER evaluation | `ARCHITECTURE.md` | §12 / ARCH-015 |
| Persistence (JSON, path-traversal, write tiers) | `ARCHITECTURE.md` | §13 / ARCH-016 |
| Errors / normalized codes / 429; security boundaries | `ARCHITECTURE.md` | §15 / ARCH-018, ARCH-019 |
| Config & secrets; build & run contract | `ARCHITECTURE.md` | §15 / ARCH-028, ARCH-029 |
| Testing strategy + priority tiers | `ARCHITECTURE.md` | §15 / ARCH-020 |
| Model / contract inventory | `ARCHITECTURE.md` | Appendix A |
| Lessons logged (full prose) | `server/LESSONS.md` | by lesson # |

<!-- Seeded from the (complete) architecture doc. Add a row whenever a new topic is looked up twice. -->

## Stack

<!-- ▼ EXAMPLE BLOCK: stack quick-reference for implementer sessions. Canonical stack lives in root CLAUDE.md + ARCHITECTURE.md; this is the cheat sheet. ▼ -->

- **Runtime:** .NET 8 (C# 12)
- **Framework:** ASP.NET Core Web API
- **Validation:** System.Text.Json + record types (manual validation)
- **Lint / types / tests:** dotnet format + Roslyn analyzers / dotnet build (nullable enabled) / xUnit

<!-- ▲ END EXAMPLE BLOCK ▲ -->

## Standard commands

```bash
# Install deps (run once; re-run when the manifest changes)
dotnet restore

# Run the dev server (if applicable)
dotnet run --project server/AiInterpreter.Api

# Tests
dotnet test

# Quality
dotnet format --verify-no-changes
dotnet format whitespace --verify-no-changes
dotnet build

# Preflight (use before saying "done" with a feature)
dotnet format --verify-no-changes && dotnet build && dotnet test
```

## TDD protocol

**Write the failing test first.** Applies to deterministic code — see the TDD posture in root `CLAUDE.md` for what is test-first vs. exempt.

**Commit per slice when practical.** Never bundle a safety-critical slice with anything else.

## Forbidden patterns

Do not:

1. **Write code without a failing test first** (for deterministic code per the root TDD posture). Even one-line functions.
2. **Put provider logic in controllers / endpoints** — controllers delegate to application services; provider calls live behind `ISttProvider` / `ITranslationProvider` / `ITtsProvider` (ARCH-008). Keeps swapping a provider a contained change.
3. **Synthesize or back-date stage latency** — stamp each `LatencyEvent` on the *real* first arrival of its event type from the provider stream; never fabricate `stt.first_partial` / `translation.first_token` / `tts.first_audio` (ARCH-011/013). A buffered single response is not streaming.
4. **Collapse the translation stage to a blocking call** — `OpenAiTranslationProvider` streams tokens via the Responses API (`stream=true`); the orchestrator consumes the `IAsyncEnumerable` (ARCH-011/012).
5. **Let a standard API key, the ephemeral secret, or raw audio reach a response body or session JSON** — sanitize provider errors before returning them (root Key safety rules; ARCH-018/019).
6. **`git add -A` / `git add .`** — stage slice files explicitly (root push posture).

<!-- Accretes as lessons surface; each durable rule earns a LESSONS.md entry. -->

## Cross-doc invariants — schema/docs mirroring

Several typed models in this codebase are **contracts** mirrored in `ARCHITECTURE.md` and indexed in the table below. The architecture doc is the canonical contract; the model is the executable enforcement. Drift produces silent disagreement.

**Authoring discipline (orchestrator owns this table).** When the implementer adds, removes, or renames a field on one of these models, the implementer **flags it at Step 9 categorized as `Cross-doc invariant change`** per the routing matrix in `docs/orchestrator-briefing.md`. The implementer does NOT edit `server/CLAUDE.md` or `ARCHITECTURE.md` directly — the orchestrator writes the table row + the architecture edit hot during the same session. Working-tree state aligns within the round; commits stagger (implementer's slice commit lands code+tests; orchestrator's round commit lands the doc rows).

| Model | `ARCHITECTURE.md` section | Notes |
|---|---|---|
| `DeepgramOptions` | ARCH-012 / ARCH-028 (Appendix A) | Section `"Deepgram"`; inline defaults are source of truth; ApiKey backend-only (ARCH-019) |
| `OpenAiTranslationOptions` | ARCH-012 / ARCH-028 (Appendix A) | Section `"OpenAiTranslation"`; default `Model` + the `ConfigService` translation catalog = **`gpt-5-nano` / `gpt-5-mini`** (051, 2026-05-31 — REAL + on-the-user's-key; the prior `gpt-5.4-*` were real-but-NOT-on-the-key → HTTP 400 on every cascade turn). A guard test couples catalog↔pricing↔non-zero estimate so an off-catalog/unpriced model can't ship silently. |
| `OpenAiTtsOptions` | ARCH-012 / ARCH-028 (Appendix A) | Section `"OpenAiTts"`; optional `VoiceByLanguage` map |
| `RealtimeOptions` | ARCH-012 / ARCH-028 / ARCH-019 (Appendix A) | Section `"Realtime"`; ApiKey backend-only |
| `PricingOptions` | ARCH-014 (Appendix A) | Full ARCH-014 shape (A.4); **file-loaded via `PRICING_CONFIG_PATH`** (not section-bound; no SectionName); `realtime` explicit class (estimatorNote string sibling), `translation`/`tts` model-keyed dicts; **translation keys `gpt-5-nano` 0.05/0.40 + `gpt-5-mini` 0.25/2.00, `version: "2026-05-31-payg-estimates"`** (051 — real per-1M rates, no `0.0` placeholder) |
| ARCH-005 domain model — 6 enums + 15 records | ARCH-005 / Appendix A | `InterpretationSession`/`InterpretationTurn`(+**`IsEvaluation`** bool, F.4 — trailing-defaulted; excluded from per-mode `ModeSummary`, kept in `WerSummary`)/`TranscriptSegment`/`LatencyEvent`(+ARCH-013)/`CostEstimate`(+ARCH-014)/`SessionSummary`·`ModeSummary`·`WerSummary`(+ARCH-009)/`ProviderError`(+ARCH-012)/`EvaluationPhrase`·`WerResult`(+ARCH-015)/enums/etc. Full field inventory in Appendix A; a field change pairs with the ARCH-005 edit. Serialized via `Common/JsonDefaults` (camelCase + enum-as-string + explicit-null). |
| Provider contracts — `ISttProvider`/`ITranslationProvider`/`ITtsProvider` + event hierarchies + request records + `AudioFrame` | ARCH-012 / Appendix A | Verbatim ARCH-012 §9 (B.1); consumed by B.4, implemented by B.2 fakes / C real providers. `ProviderErrorMapper` owns the exception/status→`ProviderError` table (SafeMessage never echoes `ex.Message`); entry points `Map(Exception)` + **`MapStatus(int)`** (C.6 — explicit-status door for SDK-hidden-status recovery, e.g. Deepgram `err_code`→status); vendor-agnostic (no provider-SDK dep in `Abstractions/`). |
| `UiError` (backend record) | ARCH-007 / ARCH-018 / ARCH-019 (Appendix A) | B.8 — mirror of the ARCH-007 TS `UiError` `{code, safeMessage, stage?, retryable, turnId?}`; produced by `ErrorSanitizer` (safe-by-construction; lesson §13). **Server-only `[JsonIgnore] HttpStatusCode` is NOT part of the wire mirror** — a field change to the wire shape pairs with the ARCH-007 TS edit. `ProviderError` is the provider/stage/persisted error; `UiError` is the lean client-facing one. |

> The canonical contract-model inventory is `ARCHITECTURE.md` **Appendix A** (e.g. `InterpretationSession`, `InterpretationTurn`, `TranscriptSegment`, `LatencyEvent`, `CostEstimate`, `SessionSummary`/`ModeSummary`, `ProviderError`, the three provider interfaces + event types, and the API DTOs). When a slice first implements one of those models, the orchestrator adds its row here (model → §) so a field change is paired with the matching `ARCHITECTURE.md` edit in the same round.

## Module organization

```
server/AiInterpreter.Api/
  Controllers/        SessionsController, RealtimeController, CascadeController, EvaluationController, ConfigController
  Realtime/           RealtimeClientSecretService + RealtimeOptions
  Cascade/            CascadeStreamingOrchestrator, CascadeOrchestrator (blob fallback), CascadeWebSocketEndpoint, CascadeModels
  Providers/
    Abstractions/     ISttProvider, ITranslationProvider, ITtsProvider, ProviderEvents, ProviderErrors, AudioFrame
    Deepgram/  OpenAI/  Fakes/
  Sessions/           SessionStore, SessionPersistenceWriter, SessionSummaryService, SessionModels
  Metrics/  Cost/  Evaluation/  Config/  Security/  Common/
server/AiInterpreter.Tests/   xUnit project (see ARCH-020 priority tiers)
```
(Full scaffold: `ARCHITECTURE.md` ARCH-006.)

Layer dependency direction (top depends on bottom, never reverse):

```
Controllers / WebSocket endpoint
  → Application services (orchestrators, SessionStore, SessionSummaryService, EvaluationService)
    → Provider abstractions (ISttProvider / ITranslationProvider / ITtsProvider)
      → Provider implementations (Deepgram / OpenAI / Fakes)
  → Utilities (Metrics, Cost, Evaluation/WER, Persistence, ErrorSanitizer, Common)
```

**Provider logic never lives in controllers** (ARCH-008). Cross-cutting layers (Common, Metrics) may be imported from anywhere. Pin the rule with a structural test where practical — the test *is* the spec for the rule.

## Subagents

See `.claude/agents/README.md` for the canonical inventory + integration points.

<!-- ▼ EXAMPLE BLOCK: area-specific subagent candidates — list candidates that would earn their keep specifically in this area (e.g. an ABI/types syncer for a frontend area, a Pyth/feed verifier for a contracts area). Build only on real friction. ▲ -->

## Lessons logged from prior sessions

The full prose for each lesson lives in `server/LESSONS.md`. This index is the compact orientation surface.

**Lesson numbers are stable IDs** — once assigned, they don't change. New lessons get the next sequential number. `/session-end` proposes additions when it detects them; the user approves before the entry is written and a row is added here.

Lessons start at §1.

| # | Date | Topic | Rule (one-liner) |
|--:|---|---|---|
| 1 | 2026-05-28 | [IOptions config-binding pattern](LESSONS.md#1) | Options are bindable types (class + get/set, or record w/ init + parameterless ctor), NOT ARCH-005 immutable records; inline defaults = single source of truth; expose `const string SectionName`; bind via `Bind(new T())` not `Get<T>()`; non-web test projects need explicit `Microsoft.Extensions.Configuration.*` refs. |
| 2 | 2026-05-28 | [Shared JSON contract + record-`==` gotcha](LESSONS.md#2) | One shared `JsonSerializerOptions` (`Common/JsonDefaults`: camelCase + enum-as-camelCase-string + explicit-null) is the single source for API + persisted JSON; record `==` is reference-based over `List<>`/`Dictionary<>` members, so round-trip tests assert JSON-string equality, not record `==`. |
| 3 | 2026-05-28 | [Degrade-don't-crash on optional config](LESSONS.md#3) | Load optional external config (e.g. `pricing.json`) with a missing-file guard + size guard (pre-read, no OOM) + a *filtered* catch (never bare; never swallow OOM/`SecurityException`) + null-result→`Failure`, returning `Result<T>` so the caller degrades to "unavailable"; deserialize via the shared `JsonDefaults`. |
| 4 | 2026-05-28 | [Host config: env→section bridge + single JSON point](LESSONS.md#4) | Map flat ARCH-028 env vars → Options sections in one `Program.cs` bridge (set only present keys so inline defaults stand; a shared key can fan out); wire HTTP JSON through the single `JsonDefaults.Apply`; host tests touching process env vars set-before-factory + finally-unset + run serialized. |
| 5 | 2026-05-28 | [Error-mapper boundary pattern](LESSONS.md#5) | One static mapper owns the exception→`ProviderError` table; `SafeMessage` is a fixed string per code and **never echoes `ex.Message`/StackTrace** (no secret/stack leaks; B.8 is the full sanitizer); validate the closed `stage` set by caller-contract, not a runtime throw inside the mapper; raise non-exception outcomes (empty/timeout) via explicit factories. |
| 6 | 2026-05-28 | [Streaming-fake pattern](LESSONS.md#6) | Streaming fakes/providers = `async IAsyncEnumerable<TEvent>` + `[EnumeratorCancellation]`; route per-event delay through one `PaceAsync(delay, ct)` (`ThrowIfCancellationRequested` + `await Task.Delay`) for deterministic OCE + no CS1998; error variants `yield` a real-code `*Failed` then `yield break`; test ordering + cancellation, not wall-clock. |
| 7 | 2026-05-28 | [Metrics aggregation: absolute-Timestamp math](LESSONS.md#7) | Aggregate from absolute `Timestamp` (cross-clock safe); `relativeMs` is per-event display (factory clamps ≥0), never a cross-event math input; the aggregator does NOT clamp cross-clock pairs (skew disclosed); missing endpoint → `null`/n·a, never throw. Harness pins: wrong-`RelativeMs` sentinel proves Timestamp-math; literal wire-strings vs `LatencyEventNames` constants catch a typo. |
| 8 | 2026-05-28 | [Cascade orchestrator streaming pattern](LESSONS.md#8) | Nested per-segment loop (each `SttFinal` → its own streamed translation→TTS, sequential; concurrent interleaving deferred ARCH-025); stamp each `first_*` once on real first arrival; manual `MoveNextAsync` enumeration with try/catch only around the move (yield outside); STT timeout = per-event idle timer via arm/disarm `CancelAfter` (a fired CTS can't un-cancel); OCE filter `when (!ct.IsCancellationRequested)` splits caller-cancel from stage-timeout; emit a flat transport-agnostic `IAsyncEnumerable<CascadeOutputEvent>`. |
| 9 | 2026-05-28 | [Cost estimation: branch-on-basis + degrade](LESSONS.md#9) | Branch on `PricingBasis` (not provider); degrade missing config/usage via `Result<CostEstimate>` (lesson §3), but a `0.0` configured rate estimates to `0.0` (distinguish by `decimal?` null, not `==0`); estimate-only factors are named constants surfaced in `Assumptions` + build-confirm-flagged; `decimal`, no rounding; cascade aggregates to one composite estimate keyed by translation model. |
| 10 | 2026-05-28 | [WER: normalize, DP-backtrace S/I/D, unbounded](LESSONS.md#10) | Normalize = invariant-lowercase → strip `\p{P}` → collapse whitespace, accents preserved, punctuation removed (not spaced); DP backtrace attributes S/I/D with documented tie-break (match>sub>del>ins); WER unbounded (never clamp >1.0); empty reference is a precondition (`ArgumentException`, never ÷0); phrase store degrades via lesson §3 behind a DI facade; F.1 boundary must cap hypothesis length (DP-matrix DoS) + never surface `LoadError`. |
| 11 | 2026-05-28 | [Persistence safety: sentinel-scan + two-layer path guard + degrade](LESSONS.md#11) | Active sentinel-scan (inject `sk-`/`ek_` patterns, assert absent from serialized JSON + legit content present, audio asserted key-form `"audio":`) backs the no-secret/no-audio-field structural guarantee; two-layer path guard (ASCII allowlist pre-FS + canonicalized **separator-terminated** `StartsWith` under root); IO degrades via `persistence.failed`/`Result`, never crashes a turn; writer serializes the model verbatim (not a sanitization boundary — that's B.8); one stable file/session via a `StartedAt`-derived filename. |
| 12 | 2026-05-28 | [Summary aggregation: reuse the seam, propagate null-semantics, test purity by source-integrity](LESSONS.md#12) | Aggregation services compose the existing per-turn seam (reuse `MetricsAggregator` / read each turn's `CostEstimate`; never re-implement the math); propagate "average non-null, absent→null, never 0/throw" up the tier (a sub-summary is null iff its group is empty; cascade fields null for realtime); WER stays unbounded in aggregates (pin no-clamp with a clamp-sensitive fixture); a pure-compute test asserts source-integrity (inputs unchanged + the computation ran), not just absence of the field write. |
| 13 | 2026-05-28 | [Error sanitization: safe-by-construction, log single-lined, project-don't-duplicate](LESSONS.md#13) | Sanitizer is safe-by-construction (fixed safe message per code; NEVER interpolate `ex.Message`/`Result.Error`/stack/payload into a client error; prove via inject-secret-assert-absent across all serialized fields); log the original server-side only, single-lined (`ReplaceLineEndings`) to prevent log forgery; project an already-safe `ProviderError`→`UiError` (drop provider identity) + reuse `ProviderErrorMapper`, don't duplicate its table; preserve HTTP status via a server-only `[JsonIgnore]` field so the wire body stays an exact TS mirror. |
| 14 | 2026-05-28 | [Global exception handler: thin `IExceptionHandler`, `AddProblemDetails` gotcha, emit `UiError`](LESSONS.md#14) | Thin `IExceptionHandler` routes unhandled exceptions through the one `ErrorSanitizer`→`UiError` (never `ProblemDetails`/re-sanitize/re-log), placed first in the pipeline; **`AddProblemDetails()` is mandatory for parameterless `UseExceptionHandler()` startup-init** and as a side effect makes framework 404/405 emit ProblemDetails (accept for unrouted paths the SPA never calls); serialize with explicit `JsonDefaults.Options`; unit-test via `DefaultHttpContext`, prove the end-to-end wire via a throwing-service override at a real endpoint (not an artificial route). |
| 15 | 2026-05-28 | [Controller conventions: thin controller + interface test seam, MVC JSON options, capability-from-key-presence](LESSONS.md#15) | Controllers are thin → delegate to a service (ARCH-008); the controller's collaborator gets a thin interface as the test seam (substitutable throwing fake proves `UseExceptionHandler` — cleaner than `virtual`-for-test); **`AddControllers().AddJsonOptions(JsonDefaults.Apply)` carries the shared JSON contract onto the MVC path** (separate from the minimal-API `ConfigureHttpJsonOptions` — load-bearing for enum DTOs); capability/config endpoints report from key presence (`!IsNullOrWhiteSpace`) never the value, pinned by a no-secret-echo sentinel scan; defensive-copy static catalogs per response. |
| 16 | 2026-05-28 | [Controller request/response boundary: `Result`→DTO never-serialize, record-param validation gotcha, no path disclosure](LESSONS.md#16) | Map `Result`/`Result<T>`→DTO at the boundary, **never serialize a `Result`** (success→`Ok(dto)`; failure→sanitized `UiError` via `SanitizeResult`; expected conditions like 404→`ErrorSanitizer.ForCode`, no Error log; internal outcome wrappers stay internal); **DataAnnotations on a record positional param target the PARAMETER not `[property:]`** (MVC throws `InvalidOperationException` otherwise — caught only by a full MVC boot); cap request strings `[MaxLength]`/`[Required]` (ARCH-019 boundary hygiene) + return **filename-only** paths (no absolute-path/data-dir/username disclosure). |
| 17 | 2026-05-29 | [Provider-boundary contract suite: interface-level, error-code preservation is the contract](LESSONS.md#17) | The ARCH-020 boundary suite is written at the **interface level** (provider-agnostic) so the same assertions run against fakes (B) + real providers (C.5 extends the file + helpers); net-new pin = **error-code preservation** (a `*Failed` carries the real `ProviderError` `Code`/`Retryable`/`Stage`/`Provider` unchanged — incl. a non-retryable 403 case), NOT a re-test of the mapper truth table (B.1) / per-fake behavior (B.2) / cancellation (B.2 — real HTTP-stream cancel is C.5's); keep success assertions tolerant of conformant variants (no-chunk TTS `FirstAudio→Complete`) so they survive real-provider reuse. |
| 18 | 2026-05-29 | [Real provider: thin manual-smoke shell + pure unit-TDD'd mapper](LESSONS.md#18) | A real provider = a thin **manual-smoke transport shell** + a pure **unit-TDD'd `internal static` mapper** (`InternalsVisibleTo`) holding all decision logic (SDK-event→domain-event, response parse, exception→`*Failed` via the reused `ProviderErrorMapper`, schema build); bridge a callback SDK to `IAsyncEnumerable` via `Channel<T>` (cancellation completes the writer + closes the socket); route live-vs-recorded through one `ISttProvider` via a request discriminator (`Encoding`); when the SDK exposes no mappable HTTP status (Deepgram v6.6.1 `DeepgramRESTException`), accept the honest `{stage}.unknown` degrade + pin-and-track it, don't re-map speculatively. Sets the C.2/C.3 pattern. |
| 19 | 2026-05-29 | [SDK-hidden HTTP status: recover via err_code → vendor-agnostic MapStatus; verify SDK shape by reflection](LESSONS.md#19) | When an SDK hides the HTTP status from `ProviderErrorMapper`, recover it in the **vendor-specific** mapping by matching the SDK's documented `err_code` string → status int, then route through a **vendor-agnostic** explicit-status entry point `MapStatus(int,…)` (refactor the private HTTP-status core so `Map` + `MapStatus` share one unchanged table); keep `Abstractions/` SDK-free; never echo `err_code`/`err_msg` into `SafeMessage`; document the `MapStatus(int)` out-of-range contract instead of throwing; **verify the SDK's real shape (fields, defaults, JSON keys) by reflecting the restored package, not docs** — `ErrCode` defaults to `"Unknown Error Code"` (not null) + Deepgram 5xx uses key `error_code` ≠ `err_code` (→ accepted `stt.unknown` residual). |
| 20 | 2026-05-29 | [Raw-HTTP/SSE streaming provider: mock-handler E2E TDD; timeout covers the read loop; OCE polarity vs trailing catch](LESSONS.md#20) | A raw-`HttpClient`/SSE provider (no SDK) is TDD'd **end-to-end** via a mock `HttpMessageHandler` + canned SSE stream (the C.1 Q2 fork infeasible for the Deepgram SDK is feasible here) — pin streaming-honesty (≥2 partials before final, first-delta=first-token), usage-null, request-body shape, 1-byte-per-read line reassembly; the per-stage **linked-CTS timeout must cover the whole read loop, not just the send** (a stalled `ReadLineAsync` → `<stage>.timeout`); and the §8 OCE `!`-idiom **flips with a trailing `catch (Exception)`** — use `catch (OCE) when (ct.IsCancellationRequested) { throw; }` + generic-arm-maps-timeout (inverting would swallow caller-cancel). OpenAI Responses API: nested `reasoning.effort`/`text.verbosity`, no `[DONE]`, snake_case `response.usage`. Binary-chunk addendum (C.3): raw byte buffers (no SSE parse), `TtsFirstAudio` on the real first chunk, `stream_format:"audio"` required for gpt-4o-mini-tts, pre-call cap via `MapStatus(400)`. Sets the C.3 pattern. |
| 21 | 2026-05-29 | [Cascade WS endpoint: thin shell over pure mapping/validation; orchestrator unchanged; boundary owns validation; audio streamed-never-accumulated](LESSONS.md#21) | The pipeline-integrating WS endpoint is a thin transport shell over pure `CascadeStartValidation` (start-parse + `encoding` allowlist = its own SECURITY commit, ARCH-019 — the boundary validates, not the orchestrator) + `CascadeWsMapping` (`CascadeOutputEvent`→ARCH-009 message, cost, `AssembleTurn`); the **B.4 orchestrator stays unchanged** (stage-label `Overall` + `translation.final` Metadata tokens only); binary PCM bridges via `Channel<AudioFrame>`; **single socket sender** (receive pump only feeds the channel + stashes state); **streamed audio is NEVER accumulated/persisted** (invariant #3 — `is not Audio` guard, no Audio case in `AssembleTurn`, assert via sentinel); cost from observable usage (per-segment tokens **additive**, TTS char-proxy) degrading wholesale; DI-swap real↔fake by key-presence + `USE_FAKE_PROVIDERS`. |
| 22 | 2026-05-29 | [WS-boundary hardening: Origin-at-endpoint, bounded-audio-channel + writer-complete-on-Done, fail-closed terminal-less streams, idempotent terminal completion](LESSONS.md#22) | A WS upgrade **bypasses CORS** → the endpoint validates `Origin` against `FRONTEND_ORIGIN` (exact ordinal) before accept via a pure `IsAllowedOrigin` (own SECURITY commit; disallowed/missing/`"null"`→403 `cascade.forbidden_origin`); the audio bridge is **bounded + backpressure** AND completes the writer on `Done` (bounding creates a post-terminal pump-park otherwise); **leave** the pump's OCE swallow (rethrow risks an unobserved task exception); the orchestrator **fails closed** on a terminal-less real stream (`<stage>.unknown` via a fixed-message `ProviderErrorMapper.Unknown` factory, invariant #4; STT clean-no-final = valid silence→Completed, partials-then-no-final→`stt.unknown`); terminal completion is **idempotent at the store** (`FinalizeTurn→{Turn,Applied}`) with **every** caller routed through it (WS + HTTP `/complete` — grep the caller set); cap every client WS string (256, §16 at the WS boundary) + split codes honestly (`invalid_audio`/`invalid_request`/`forbidden_origin`); pin clamps/resolution on the live mapping path, not just the helper. |
| 23 | 2026-05-29 | [Blob fallback: thin adapter over the streaming orchestrator; strip MIME params; degrade STT cost honestly; never persist uploaded/TTS audio](LESSONS.md#23) | The non-streaming fallback is NOT a second pipeline — `CascadeOrchestrator` wraps the blob as a 1-frame container-encoding `AudioFrame` stream → the **unchanged** `CascadeStreamingOrchestrator.RunAsync` → collect the same `IAsyncEnumerable<CascadeOutputEvent>` into a JSON turn via the shared `AssembleTurn`/cost-fold/`FinalizeTurn` (pre-recorded STT comes free: `DeepgramSttProvider` routes non-`linear16` → REST); controller thin (ARCH-008). **Strip MIME params before the content-type allowlist** (`audio/webm; codecs=opus` is the MediaRecorder primary format — an exact-match 415s all of Phase D). Upload validation (size + content-type) = its **own SAFETY commit** (invariant #5) + **bound the multipart/Kestrel body** to the cap (else a buffering-DoS window + >128MB→500). The blob **does not price STT** (no audio-duration signal in `SttEvent`) → degrade honestly, **no processing-wall-clock proxy** (§9; ~30× undercount = synthetic metric); composite → unavailable (the WS path prices fully). Uploaded blob + TTS audio **returned but NEVER persisted** (#3, key-form sentinel). Verify the controller's 413/415/400/404 wire with `WebApplicationFactory`. Don't duplicate C.1–C.3/B.10 boundary cases — net-new = blob outcomes + the real-provider SafeMessage sweep. |
| 24 | 2026-05-29 | [Ephemeral-credential broker: §18/§20 for a non-streaming upstream call; verify the GA response shape; key Bearer-only + secret response-only](LESSONS.md#24) | The Realtime client-secret broker = the §18/§20 real-provider pattern for a **non-streaming** call: thin `HttpClient` shell (`RealtimeClientSecretService`) + pure mapper (`RealtimeClientSecretMapping`), TDD'd via mock `HttpMessageHandler` + `WebApplicationFactory` (no live key — that's the E.3+ smoke); **reuse** `ProviderErrorMapper.Map/MapStatus(…,"openai","realtime")` + `ErrorSanitizer.ToUiError` (codes `realtime.auth/.rate_limited/.invalid_request/.upstream_unavailable/.timeout/.unknown`, **no bespoke codes**); controller mirrors `CascadeController` (`Ok(dto)` / `StatusCode(err.HttpStatusCode ?? 502, ToUiError(err))` off `RealtimeMintOutcome`). **Verify the live GA response shape** — GA `client_secrets` is **top-level** `{value,expires_at,session}`; the **nested** `client_secret.{…}` is legacy `/v1/realtime/sessions` (ARCH-010 had it wrong) — parse **both** (top-level primary), never fabricate a secret on missing `value` (verified via Context7). **Invariant #1:** the `sk-` key is upstream-Bearer-only, never in a body/log (no `ILogger` on the path; sentinel-pinned). **Invariant #2:** the `ek_` is response-only — enforced by taking **no `SessionStore`/`PersistenceWriter` dependency** (can't persist what it can't reach). One bounded **429** retry honors `Retry-After` via an **injected `Func<TimeSpan,CT,Task>` delay** (deterministic suite, §6); off-catalog model (area-owned `RealtimeModelCatalog`) + missing key **fail closed before the upstream call** (`CallCount==0`); missing key → `realtime.auth` non-retryable (honest — not transient). |
| 25 | 2026-05-30 | [Realtime per-turn cost: wire at `/complete`; degrade to null never a synthetic $0; disclose absent output at the wiring layer](LESSONS.md#25) | `CostEstimator.EstimateRealtime` was implemented + tested (B.5) but had **zero callers** → realtime turns shipped a null `CostEstimate` until E.2b wired it at `POST …/turns/{turnId}/complete` (the realtime finalize; cascade is WS-priced — never here), **inside the idempotent `FinalizeTurn` transform** (atomic with terminal status; a re-complete can't recompute). Branch `Mode==Realtime`; model from `SessionConfig.ProviderProfile.RealtimeModel`; reuse `EstimateRealtime` (no re-derivation). **Each mode prices at its own terminal, never double.** **Degrade to null, never a synthetic $0.00** — both on `Unavailable` (pricing/rates absent, off-catalog) AND on an unreported/zero input duration (pricing 0 seconds = a real $0.00 reads as "free" — synthetic). **Disclose absent output AT THE WIRING LAYER:** realtime bills input+output audio separately, `EstimateRealtime` silently does `AudioOutputSeconds ?? 0m` (no disclosure), so the caller forward-builds optional `CompleteTurnRequest.OutputAudioDurationMs?` (E.4 reports it) + appends a "output not reported" assumption when absent. `PricingBasis=="tokens"` (the billing unit) ≠ the input unit (audio-seconds, in `Units`). |
| 26 | 2026-05-30 | [Stale-session flush: reuse the `/end` seam on next `Create`; async `Create` forced; owed write uses `CancellationToken.None`](LESSONS.md#26) | Flush abandoned sessions (ARCH-017 Flow H) by **reusing the `EndAsync` finalize+persist seam on the next `POST /api/sessions`** (no duplicated persistence) — flush **all** un-ended priors **FIRST**, then register (the new session can't self-flush), over a snapshot `SessionStore.ActiveSessionIds()`. **`async CreateAsync` is forced** (must `await` the persist + deterministic test) — keep orchestration in the service (ARCH-008, not the controller); ripple is mechanical, HTTP response shape unchanged. **⭐ The owed artifact write uses `CancellationToken.None`, never the request's token** — owed/cleanup work belonging to a *prior* unit must not be abortable by the (possibly-cancelled) triggering request, else a half-state (stale session ended-in-memory, no artifact; `WriteAsync`'s catch-filter excludes OCE so the cancel propagates). Persist-fail degrades + logs (Warning, injected `ILogger`); `EndedAt` still flips; never blocks the new `Create` (§11). Only un-ended flush (idempotent — `ActiveSessionIds()` excludes ended; pin via `EndedAt`-untouched, not file-count). TTL sweep deferred (G). |
| 27 | 2026-05-30 | [Evaluation WER endpoint: cap hypothesis in-service (domain-400, not `[MaxLength]`); reference-from-store + asymmetric arg-order pin; reuse `UpdateTurn` for post-completion attach](LESSONS.md#27) | The WER endpoint is the first request-reachable caller of `WerCalculator.Compute` (an `n×m` DP allocation, `m`=hypothesis words → memory-DoS, ARCH-019). **Cap the hypothesis in the SERVICE as its first action, before the allocation, returning a DOMAIN `400 evaluation.invalid_phrase` — NOT a `[MaxLength]`** (which ProblemDetails-bypasses the chokepoint); the cap is the security chokepoint so it must own the rejection. Make cap-before-compute **unit-pinnable** via an injected `Func<…,WerResult>` compute delegate (internal test ctor, `InternalsVisibleTo`) + a call-count spy. **Source the reference from the store by `phraseId`, never the request** (unknown → `404 evaluation.phrase_not_found`, no `Compute` call); pin arg-order with an **asymmetric** test (symmetric perfect-match misses a swap). **Attach a post-completion `WerResult` via the existing `SessionStore.UpdateTurn`** (unconditional read-modify-write) **NOT `FinalizeTurn`** (idempotent-terminal → refuses an already-`Completed` turn, silent no-op); best-effort persist + degrade to a `persistenceWarning` (null-guard the TOCTOU `Get(...)!`). Hypothesis stays un-annotated `string?` (empty hypothesis = valid WER 1.0). |
| 28 | 2026-05-30 | [STT-only transcribe: wrap blob through the SAME `ISttProvider`, JOIN multiple finals, reuse the upload validator, body-limit = max of all routes](LESSONS.md#28) | The evaluation transcribe runs **STT only** + is **stateless** (panel owns turn creation). Don't reuse cascade `RunBlobTurnAsync` (full pipeline + finalize). **Wrap the blob as a 1-frame `AudioFrame` through the SAME `ISttProvider`** (C.1 REST path) + **JOIN `SttFinal` across multiple finals** (single space) — last-only silently truncates the hypothesis → wrong WER (§16/E.4a class); stamp `stt.first_partial`/`stt.final` on real arrival only. **Reuse `CascadeUploadValidation`** (validate-before-provider, `413`/`415`, strip MIME params; prove non-reach with a throwing `ISttProvider` → 413 not 500). **⭐ The global multipart/Kestrel body-limit backstop = `max(cascade, eval)`** of all routes' caps (`EVAL_MAX_UPLOAD_BYTES`→`CASCADE_MAX_UPLOAD_BYTES`) — a single-route backstop lets a higher other-route cap hit a framework 500 before the clean per-route 413. **Stateless ⇒ invariant #3 structural** (no `SessionStore`/`Writer` dep, no turn) — pin via data-dir-empty + store-untouched. |
| 29 | 2026-05-31 | [Distinguish auxiliary turns with an explicit marker; exclude from comparison aggregation, keep in their metric](LESSONS.md#29) | A standalone WER-eval turn inherits the session mode → would inflate `ModeSummary.TurnCount`. **Mark it with an EXPLICIT `bool IsEvaluation`, NOT a derived heuristic** ("has a `WerResult`" couples carrying-a-score with being-an-eval-turn — breaks if an interp turn is ever WER-scored). Set it at the **defining moment, atomically** — inside the existing `/wer` `UpdateTurn` transform (`turn with { WerResult = wer, IsEvaluation = true }`) → the frontend stays **byte-identical** (the set-at-create alternative needs a caller signal → rejected). `SummarizeMode` excludes via one `&& !t.IsEvaluation` (covers `TurnCount` + averages + `ErrorCount`); `SummarizeWer` UNCHANGED (eval turns are where WER comes from). **Semantic:** top-level `SessionSummary.TurnCount` counts ALL → top-level ≠ sum-of-per-mode (delta = eval turns); pin the split. **Trailing-defaulted bool** = back-compat (old JSON no-key → `false`; pin the round-trip) + **keep the persistence sentinel fixture (`BuildFullSession`) current** with the model's fields (security-review meta-rule — so a future non-bool add gets the inject-and-assert scan). |
| 30 | 2026-05-31 | [WS stop-path fails closed on any unexpected exception + idempotent SDK Stop; an SDK's own caught-and-logged internal exception can masquerade as our crash — verify the log CATEGORY](LESSONS.md#30) | A WS stop/finalize path **fails closed on ANY unexpected exception** (broaden the OCE-only catch → log single-lined + ONE sanitized terminal `cascade.unknown` frame + best-effort close, invariant #4; extends §22 to the endpoint's own exit), with writer-complete + pump-await consolidated into ONE `try/finally` so the bounded-channel (§22) pump can't park/leak on any exit path. A callback SDK whose `Stop()` isn't double-call-safe → route **every** call site through an `Interlocked`-guarded at-most-once `StopOnceAsync`. **⭐ An SDK's own caught-and-logged internal exception can masquerade as "our" crash** — the smoke's `[Error] Stop: NRE` was Deepgram-SDK-internal double-`Stop()` noise (caught + swallowed inside the SDK, never escaped our already-guarded call sites), NOT our bug; verify the log CATEGORY (method-name prefixes) before writing defensive code against a phantom. (`ProcessSendQueue: NRE` = the same benign post-`Stop` teardown noise, no frames dropped.) Manual-smoke (WS shell + transport ARCH-020-exempt). |
| 31 | 2026-05-31 | [Tolerate spurious empty STT finals — skip; fail empty_transcript only if no non-empty final ever (two-flag); inverse of §28](LESSONS.md#31) | A streaming STT consumer must **tolerate spurious empty/whitespace finals** (Deepgram emits them around real speech — leading silence / VAD boundary / trailing-after-content): **SKIP** an empty `SttFinal` (no `stt.final` stamp / source seg / translate / fail; `continue`). Fail `cascade.empty_transcript` **only at stream end** when `sawEmptyFinal && !sawNonEmptyFinal` — **two flags**: a single non-empty flag wrongly regresses **pure silence** (no final at all → must stay Completed, §22). Three outcomes: pure-silence→Completed / all-empty→empty_transcript / has-content→Completed. **Not stamping the empty final's `stt.final` also removes a trailing-empty-driven inflated-latency symptom.** The inverse of §28 (which JOINS multiple non-empty finals); RED-first via a scripted-finals fake. |
| 32 | 2026-05-31 | [Mid-session mode switch must propagate to the backend (`POST …/mode`); atomic re-entrant `SwitchMode` + raw-string request DTO → sanitized 400](LESSONS.md#32) | A turn's `Mode` is stamped from `config.CurrentMode` at create → a frontend-only mode toggle mislabels turns + invalidates the by-mode comparison (Finding 2c). Propagate via `POST /api/sessions/{id}/mode` (`config.CurrentMode` now mutable mid-session). **⭐ Atomic RE-ENTRANT `SwitchMode`:** under one gate hold, call the existing `RecordModeTransition` (which takes the lock → it must be re-entrant) THEN swap `CurrentMode` — single-owner append + no torn window for a concurrent `CreateTurn`. **⭐ Raw-string `SetModeRequest {string? Mode}`** (NOT the enum) validated at the service chokepoint (`Enum.TryParse(ignoreCase) && IsDefined`) → sanitized `400 session.invalid_mode` (§27 reuse); an enum-typed DTO would throw in the deserializer → framework ProblemDetails, bypassing the `UiError` boundary. Wire shape identical; precedence 404-before-400. No persist at `/mode` (rides `/complete`/`/end`). Test at the integration boundary + store primitive; keep the `BuildFullSession` sentinel current (§29). Residuals LOW: fromMode-read TOCTOU + ended-session not guarded (frontend-gated; G-hardening). |

| 33 | 2026-05-31 | [Auto-load repo-root `.env` at startup — before the config bridge, fill-gaps-not-override, degrade-if-absent, GATED out of the test host](LESSONS.md#33) | .NET reads process env, not `.env`; pre-055 a clean clone had to `set -a && source ../../.env` by hand or config-gating killed both modes. `DotEnvLoader.AutoLoad(AppContext.BaseDirectory)` runs **before `CreateBuilder`** (config providers snapshot env there), walks up to the nearest `.env`, loads **fill-gaps only** (explicit `export`/CI/prod WINS — Q1), **degrades if absent** (§3). **⭐ GATED via `ShouldAutoLoad` (entry assembly == `AiInterpreter.Api`)** — the VSTest host / test assembly / null SKIP, so a dev `.env`'s real keys can't load into a `WebApplicationFactory` host and flip provider DI fakes→real (non-deterministic suite, ARCH-020). Keys stay process-side (invariant #1). Test the gate + parse + fill-gaps precedence. |
| 34 | 2026-05-31 | [Auto-VAD turn-end rides a REAL provider endpointing signal through the ONE idempotent `FinalizeTurn`; the WS pump reads on its own linked-CTS so the finally can cancel it (extends §22)](LESSONS.md#34) | Phase-I cascade auto-finalize (062; REVISES ARCH-003). When the per-turn WS-`start` `autoVad` flag is set, the orchestrator treats Deepgram's **utterance-end** as the turn-terminal (auto-finalize on detected silence, not the client `stop`). `UtteranceEnd` was configured (`utterance_end_ms`) but never subscribed → add a typed **`SttUtteranceEnd`** marker (no text, not an overloaded Final; ARCH-012), subscribe + `vad_events=true`. The auto-terminal routes through the **SAME `FinalizeTurn` seam + the SAME fail-closed/§31 two-flag** (extract `TerminalFailure()`); whichever of {utterance-end, stop} fires first wins (idempotent), the other no-ops; manual mode ignores the marker. **Streaming honesty (#3): rides a REAL signal, never a synthesized timer.** **⭐⭐ When a terminal can fire BEFORE the client stop, the WS audio pump must read on its OWN linked-CTS that the `finally` cancels before `await pump`** — else the `Done`-before-stop ordering parks the pump on `ReceiveAsync` → hangs the handler (extends §22's pump-can't-park). Provider subscribes unconditionally (manual ignores; a future `AutoVad`-in-`SttRequest` gate → G-hardening). |
| 35 | 2026-05-31 | [The persisted-session disk READER reuses §3+§11+§16, degrades per-file, is a path/DoS boundary not a sanitization one](LESSONS.md#35) | H.3-backend (065) — the FIRST request-reachable disk-read of persisted sessions (`GET /api/sessions` + `SessionPersistenceReader`, Option B). **Reuse** §3 (single-open size-guard + filtered catch + `Result<T>`), §11 read-side (`TopDirectoryOnly`+`session_*.json` pattern — no recursion, no per-path canonicalization needed since no input-derived path on read), §16 (`Result`→DTO; sanitized `sessions.read_failed`). **Degrade PER-FILE** (skip corrupt/oversize via `SESSION_MAX_READ_BYTES` 8MB; never blank the list). **Misconfig (file-where-dir → Failure) vs empty (missing dir → empty Success).** A **path/DoS boundary, NOT sanitization** (reads already-clean data — keep the sentinel current); return a lightweight `SessionListItem` summary (payload hygiene; detail = `GET /{id}`, whose disk-fallback **LANDED at 068**). Symlink-in-dataDir documented out-of-threat-model (single-user-local; parse/size-skip backstop) — explicit reviewer decision. **068 by-id amendment:** `ReadById` validates the id pre-FS via the §11 allowlist (`IsValidSessionId` bumped `internal`), then enumerate+match-on-deserialized-`SessionId` (filename embeds `StartedAt`); `SessionService.Get` (+`Summary`) disk-fall-back **in-memory-first**; the widened pre-check means evicted-session MUTATION fail-closes at the in-memory store (`turn.not_found`, no resurrection — pinned). |
| 36 | 2026-05-31 | [Trailing empty STT PARTIAL false-fails a completed cascade turn (skip like §31); disclosed char→minutes TTS estimate when the API reports no usage](LESSONS.md#36) | 069 / G.4 cascade-correctness (LIVE-confirmed). **Bug B:** SKIP a trailing empty/whitespace `SttPartial` at the orchestrator (extends §31's empty-FINAL skip ONE level up) — else the §22 stream-end fail-closed reads it as a lost-final → `stt.unknown` → false-`failed` on a turn with a final translation + `tts.complete` (§30 teardown noise; also poisons `errorCount`); §22/§31 genuine-failure paths preserved. **Bug A (cost):** NO cost-status-gate exists; the null was a TTS basis mismatch — `gpt-4o-mini-tts` bills `audio_output_tokens` but `/v1/audio/speech` reports NO usable usage (Context7) + no per-char rate → estimate output-audio MINUTES from target-text length (`TtsApproxCharsPerMinute=900m`, build-confirm) feeding `approxUsdPerAudioMinute`; basis-agnostic `BuildTtsCostUsage` (Characters + AudioMinutes); 0→null never synthetic $0 (§9/§25); DISCLOSE in `Assumptions` (cascade-ESTIMATED vs realtime-EXACT 059). Resolve `ttsVoiceUsed` from config when the frame omits it. |
| 37 | 2026-05-31 | [Stage-START latency markers stamp at stage INITIATION, not the provider's first response event](LESSONS.md#37) | 075 / cascade tts.first_audio ≈0 fix. Stamp `tts.started` at TTS request-INITIATION (a real moment via the unchanged clock/factory chain), NOT the provider's first event — else the start absorbs the request→provider round-trip and `TtsFirstAudioMs` (`Between(tts.started, tts.first_audio)`) collapses to ≈0 (the provider yields TtsStarted+TtsFirstAudio synchronously). Honest instrumentation, not synthesis (security-reviewer-clean); `tts.first_audio` still on the real provider event; `MetricsAggregator` formula unchanged. **Supersedes the provisional 057c "no fix" decision** (smoke confirmed ≈0; the delta is the metric). |
| 38 | 2026-05-31 | [Ride a provider's TYPED multilingual signal for dynamic direction (the CONTRAST to §19's SDK-hidden-field case); resolve dominant→word-mode-null-on-tie→null; the flip is additive + byte-identical-when-off](LESSONS.md#38) | 078 / Phase J cascade bidirectional. Deepgram nova-3 `multi` detection IS exposed strongly-typed on the live WS path — `v2.WebSocket.Alternative.Languages` (`IReadOnlyList<string>`) + `Word.Language` (`string`) — so ride the typed signal, **no raw-JSON fallback** (the CONTRAST to §19's SDK-HIDDEN status; same discipline: verify the SDK shape by REFLECTION, not docs). Resolve dominant = `Languages[0]` → on no recognized EN/ES code (empty OR `"fr"`-unrecognized) fall back to the **mode of per-word `language` (null-on-tie)** → else null (`en*`→En/`es*`→Es); null=ambiguous → orchestrator keeps the start-frame direction. Fields populate ONLY under `language=multi` → null-tolerate. The flip is **additive, gated on a per-turn `bidirectional` start bool** (mirrors `AutoVad`): resolve `detected→other`, drive translation + the target-TTS voice (pass EMPTY voice → provider `ResolveVoice` picks `VoiceByLanguage[target]`), emit `Direction`→`{type:"direction"}` WS msg, `AssembleTurn` folds the FIRST resolved `Direction` into the EXISTING `InterpretationTurn.Direction`. `bidirectional:false` byte-identical (regression-pinned). NO new persisted field → §29 sentinel unchanged. |
| 39 | 2026-06-01 | [Auto-VAD/continuous empty turn = silence→Completed (not failed); manual-silence still fails; exclude 0-transcript COMPLETED turns from the per-mode summary but KEEP failed-empty](LESSONS.md#39) | 083 / Phase J (J.6). The J.5 continuous loop false-failed an empty re-armed turn (`cascade.empty_transcript`→Failed → false `errorCount`+session "failed"). **Mode-split the §52/§69/§31/§22 empty-final lineage:** manual empty = deliberate silence → stays `empty_transcript`-Failed; auto-VAD empty = a VAD-gap → Completed-silence. Fix = `TerminalFailure()` empty arm gains `&& !p.AutoVad`. **Don't over-broaden:** `pendingPartial`→`{stage}.unknown` (lost final) stays BOTH modes (pin auto-VAD-dangling-partial→`stt.unknown`). Belt-and-suspenders: `SessionSummaryService.SummarizeMode` excludes `IsEmptySilence = Status==Completed && Transcripts.Count==0` — **Completed-scoped, NOT blanket 0-transcript** (a Failed-early 0-transcript turn has a real error → KEEP it; §29 pattern refined). Top-level `TurnCount` counts ALL. Give the test `Turn` helper a content transcript (the summary now reads transcripts). Contract flip → rename+re-assert the superseding test in place (not a silent delete). No model field change. |

<!-- Starts empty. Each row links to its `LESSONS.md` anchor. -->

<!-- Slash commands: see root CLAUDE.md "Slash commands available." Implementer pair: /session-start + /session-end. -->
