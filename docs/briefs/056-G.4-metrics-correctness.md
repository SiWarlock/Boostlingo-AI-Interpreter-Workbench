# /tdd brief — metrics_correctness (G.4; user live-smoke Finding — TOP priority)

> **The workbench's deliverable IS measured evidence.** A user live-smoke (2026-05-31, 3 screenshots) found the per-turn metrics + cost visibly wrong on a COMPLETED cascade turn. Frontend-only (+ a cost-gap diagnosis that may flag a backend follow-up). Sequenced AHEAD of H.3. Bundles related MetricsPanel / `deriveTurnMetrics` / ComparisonSummary fixes; **split into 2–3 commits** as the Step-2.5 design dictates (per-turn metrics fix · cost-gap · comparison model-variant).

## The fixture — raw CASCADE timeline from a COMPLETED turn (derive against this; relative ms)
`recording.started 0 · cascade.audio.received 0 · stt.started 1 · stt.first_partial 1101 · stt.final 4010 · translation.started 4460 · translation.first_token 4729 · translation.final 4915 · tts.started 6034 · tts.first_audio 6034 · tts.complete 6074 · recording.stopped 9796 · turn.completed 9897`
Derivable truth: STT 4009 (`stt.final − cascade.audio.received`) · Translation 455 (`translation.final − translation.started`) · TTS 40 (`tts.complete − tts.started`). The user holds the mic ~3.7s after speech (`tts.first_audio 6034` precedes `recording.stopped 9796`).

## Bugs (symptoms solid; root-causes are hypotheses — pin RED-first during /tdd)
1. **Per-stage STT/Trans/TTS = `n/a` on a COMPLETED turn** despite all events present + derivable (above). **The core bug.** `deriveTurnMetrics` (web §13) isn't producing the per-stage durations though the raw `latencyEvents[]` are in the turn. Pin: feed the fixture timeline → assert STT 4009 / Trans 455 / TTS 40 (not n/a).
2. **TTS first-audio = 0 ms** (`tts.started`==`tts.first_audio`=6034) — coincident/mis-derived stamp; confirm whether it's a real coincident-stamp (TTS truly streamed instantly) vs a derivation collapsing two markers.
3. **Negative speech-end→first-audio** (−372/−3321 session-avg, −6269 headline) — the metric anchors on `recording.stopped` (the MANUAL stop, held ~3.7s after speech) → `tts.first_audio − recording.stopped` < 0. Pre-VAD there is no true speech-end. **+ diagnose the −6269** (doesn't reconcile to `recording.stopped` → a possible clock-source bug). See Step-2.5 Q1.
4. **Total-turn headline 9902 ms shows red `< 3s target`** — the `<target` badge is on the wrong metric (total-turn includes the dead air); it belongs on the responsiveness metric. See Step-2.5 Q2.
5. **Cost `n/a` + "No priced turns yet" on a SUCCESSFUL cascade turn** — disproves "cost populates once turns succeed." **Diagnose which half** (Step-2.5 Q3): does the wire turn from `GET /api/sessions/{id}` carry a non-null `costEstimate`? **YES → frontend display bug** (the `costEstimate`≠`cost` §21 gotcha — `deriveTurnMetrics`/CostPanel reading the wrong field). **NO/null → backend isn't producing it** (the long-standing C.2 `usage`-nesting carry-forward surfacing live) → flag a backend follow-up at Step 9 (the lead confirms the live `cost` WS frame; backend adds temp usage logging if needed). The backend log has NO cost signal today (lead-confirmed), so the wire-turn check is the fastest discriminator.
6. **Comparison view doesn't attribute the MODEL per run** (user feature request) — the "Realtime vs Cascade" cards + per-run metrics aren't model-labeled, only the cost-by-variant sub-section groups by model. The write-up must attribute latency/cost/WER to specific models (`gpt-realtime`/`-mini`; `gpt-5-nano`/`-mini`). The model identity is partly in the data already (`costEstimate.model` drives the cost-by-variant grouping). See Step-2.5 Q4.

## Use case + traceability
- **Task ID:** G.4 metrics-correctness (user live-smoke) + a comparison feature request. **Architecture:** `ARCH-013` (metrics/latency model + the `speech_end_to_first_audio_ms` selection chain), `ARCH-014` (cost), `ARCH-007` (clean-separation render-from-store). No safety invariant.

## Acceptance (RED-first; deterministic — lessons §13 metrics + §21 cost projection + §14 errorCopy/display precedent)
- [ ] **Per-stage durations populate on a completed turn** from the raw `latencyEvents[]` (the fixture → STT/Trans/TTS non-n/a). The core fix.
- [ ] **TTS first-audio** renders correctly (a real coincident stamp shows the true value; a derivation bug is fixed).
- [ ] **The `<target` badge** moves to the responsiveness metric; total-turn is shown without the badge (or as secondary). Per the Q2 resolution.
- [ ] **The responsiveness metric handles the manual-hold case** per Q1 (no misleading negative; honest n/a or a robust anchor).
- [ ] **Cost displays on a successful cascade turn** IF the wire turn carries it (Q3 frontend half); else a backend follow-up is flagged with the diagnosis.
- [ ] **Comparison attributes the model per run** (Q4) — at minimum the cost-by-variant model surfaced on the cards / per-run.
- [ ] Existing metrics/comparison tests stay green (adjust queries, not assertions). `npm run format:check && lint && typecheck && test` green.

## Files (frontend)
`web/src/state/` `deriveTurnMetrics` / selectors (web §13), `components/MetricsPanel.tsx`, `components/latencyTarget.ts` (H.1b tier helper), `components/CostPanel.tsx`, `components/ComparisonSummary.tsx`, the `ComparisonTurn` projection (web §21), `types/metrics.ts`, + tests. (053-C realtime metrics is a SEPARATE slice — it needs the live `[realtime oai-events]` DC log; not in this brief.)

## Things to flag at Step 2.5
1. **Speech-end anchor (bugs 1/3).** Pre-VAD, `recording.stopped` is the manual stop → the responsiveness metric goes negative when the user holds the mic. Options: (a) re-anchor responsiveness to a speech-end *proxy* robust to the hold (e.g. `stt.final` → first-audio = pipeline post-transcription latency, independent of user behavior); (b) keep `recording.stopped` but show honest `n/a`/disclosure when negative (don't clamp — §13 cross-clock posture) + document the limitation until Phase-I VAD gives a true endpoint. **My lean: (a) for the headline responsiveness number** (it's the meaningful pipeline-latency signal + matches the demo/write-up intent), with total-turn as context. **Confirm ARCH-013's exact `speech_end_to_first_audio_ms` definition** before changing — if you re-anchor, that's an ARCH-013 realization note (flag at Step 9). Also diagnose the −6269 (clock-source).
2. **`<target` badge placement (bug 2).** My lean: badge on the responsiveness metric (cascade <3s / realtime <1.5s per ARCH-013); total-turn no-badge/secondary. Confirm vs ARCH-013's target definition.
3. **Cost-gap half (bug 5).** Check the wire turn's `costEstimate` first → frontend-display (§21) vs backend-not-producing. Report which at Step 9 (a backend follow-up if the latter).
4. **Model-variant presentation (bug 6).** Does `ComparisonTurn` need the model id added, or is `costEstimate.model` enough? Presentation (per-card label vs per-run rows vs the existing variant sub-section) is your design call unless a real UX fork surfaces — flag me if so. Include the fixed STT/TTS models only if cheap (completeness for the write-up).

## Dependencies + sequencing
Frontend-only; independent of the in-flight G.2b (backend) + 050-backend (landed). Touches metrics/comparison components — no overlap with 054 (which landed `92f533a`). The cost-gap may flag a backend follow-up (routed at Step 9).

## Estimated commit count
**2–3** — (1) cascade per-turn metrics fix (per-stage derivation + TTS + responsiveness anchor + badge); (2) cost-gap (display fix or the backend-flag diagnosis); (3) comparison model-variant attribution. Bundle/split per your Step-2.5 design; no safety invariant. Cross-doc: possibly an ARCH-013 realization note (if the responsiveness anchor changes) — mine at round-seal; flag categorized at Step 9.

## Lessons-logged candidates anticipated
- Likely: the per-stage-derivation root cause (why n/a despite events) + the speech-end-anchor decision (manual-stop is not a speech-end proxy pre-VAD). Orchestrator's call at round-seal.

## How to invoke
1. Read this brief + the fixture timeline.
2. `/tdd metrics_correctness` — RED the per-stage-derivation fix first (the core bug), against the fixture.
3. Step 2.5: answer Q1–Q4 (esp. the speech-end anchor + the cost-gap half). Step 9: categorized summary (flag the cost backend-follow-up if found + any ARCH-013 note) + ship/no-ship + draft commit message(s).
