# /tdd brief — provider_boundary_tests (B.10)

## Feature
`AiInterpreter.Tests/ProviderBoundaryTests.cs` (CRITICAL per ARCH-020) — the **provider-boundary contract suite**: assert the contract that any `ISttProvider`/`ITranslationProvider`/`ITtsProvider` must honor (ordered streaming events; a `*Failed` event carries a **real ARCH-012 `ProviderError` code**; cancellation honored). Driven via the **interface type** against the B.2 fakes now, structured so **C.5 extends the same file** with real-provider/HTTP-mock cases. No real keys.

> **Last Phase-B slice.** This is the named ARCH-020 boundary-contract file — the home C.5's real providers run against. **Scope it tight:** B.1 (`ProviderErrorMappingTests`), B.2 (`FakeProvidersTests`, 13), and B.4 (`CascadeOrchestratorTests`, 9) already cover a lot — B.10 adds the contract-level framing + the genuinely-new assertions, NOT a re-test of B.2.

## Use case + traceability
- **Task ID:** B.10
- **Architecture sections it implements:** `ARCH-012` (the provider interface + event contracts + `ProviderError` mapping), `ARCH-020` (testing strategy — `ProviderBoundaryTests` = CRITICAL tier; the boundary-conformance suite).
- **Related context — what's ALREADY covered (do NOT duplicate):**
  - **B.1 `ProviderErrorMappingTests` (9):** the exception→`ProviderError` mapper truth table in isolation (429→`<stage>.rate_limited` retryable; 401/403→`auth`; 400/422→`invalid_request`; 5xx/network→`upstream_unavailable`; OCE/timeout→`<stage>.timeout`; empty→`cascade.empty_transcript`; unknown→`<stage>.unknown`; stage interpolation; SafeMessage no-leak).
  - **B.2 `FakeProvidersTests` (13):** each fake's per-variant event ordering (Started→Partial*→Final etc.) + mid-stream + pre-cancelled cancellation, **in isolation**.
  - **B.4 `CascadeOrchestratorTests` (9):** the orchestrator's USE of the boundary (success ordering, empty-transcript short-circuit, partial failures, stage-timeout, caller-cancel-≠-timeout).
  - **The GAP B.10 fills:** the **provider-boundary contract as a reusable, interface-level suite** — specifically (a) a `*Failed` event carries the real `ProviderError` (code + retryable + stage) through the boundary, and (b) the contract is asserted against the **interface** (not the concrete fake), so C.5 plugs real providers into the same suite. The OCE/timeout mapping is already exercised by B.1 + B.4 (the task note: "add per-stage timeout cases only if cheap").

## Acceptance criteria (what "done" means)
- [ ] `ProviderBoundaryTests.cs` exists as the named ARCH-020 boundary suite; tests drive the providers **via their interface type** (`ISttProvider`/`ITranslationProvider`/`ITtsProvider`), so C.5 can register real providers against the same assertions.
- [ ] **Net-new core (MUST):** for each interface, a `*Failed` event from an error-variant fake carries the **real ARCH-012 `ProviderError`** it was scripted with — `Code` (e.g. `stt.rate_limited`), `Retryable`, and `Stage` are preserved unchanged across the boundary (ties B.1's mapper output to a streamed `*Failed`).
- [ ] **Contract anchors (include only where they add C.5-reuse value beyond B.2 — → 2.5 Q1):** the success-path ordered-event contract per interface asserted at the interface level; cancellation honored (a representative/consolidated check). Where a candidate is a pure re-test of a B.2 assertion, **reference B.2 and skip it** (note the skip).
- [ ] No real provider keys; all via fakes. `/preflight` clean. Full suite green.

## Wiring / entry point (Step 7.5)
A test-only file (no production wiring). Reachability = it's the ARCH-020 CRITICAL test deliverable; **C.5 extends it** (real-provider/HTTP-mock cases) — name that forward consumer. Not a production entry point.

## Files expected to touch
**New:**
- `AiInterpreter.Tests/ProviderBoundaryTests.cs`.

If a shared test helper (e.g. a `Collect(IAsyncEnumerable)` extension or a provider-agnostic assertion helper) earns its keep + would be reused by C.5, add it — **flag at Step 2.5**.

## RED test outline (Step 2) — `AiInterpreter.Tests/ProviderBoundaryTests.cs`
**Tight set — the net-new core is the error-code-preservation trio.** You hold B.2's exact assertions; prune anything that's a pure duplicate (→ 2.5 Q1).

1. **`stt_failed_event_carries_real_provider_error()`** (net-new) — drive `ISttProvider` (fake scripted with `ProviderErrorMapper.Map(HttpRequestException(429), "deepgram", "stt")`) → the `SttFailed` event's `.Error` has `Code == "stt.rate_limited"`, `Retryable == true`, `Stage == "stt"`; no `SttFinal` after.
2. **`translation_failed_event_carries_real_provider_error()`** (net-new) — same for `ITranslationProvider` (e.g. a `translation.timeout` or `translation.rate_limited`).
3. **`tts_failed_event_carries_real_provider_error()`** (net-new) — same for `ITtsProvider`.
4. **`stt_success_contract_ordered_events()`** *(contract anchor — keep if it adds interface-level/C.5-reuse value beyond B.2's `fake_stt_success_*`, else skip + note)* — via `ISttProvider`: `SttStarted` → `SttPartial*` → `SttFinal`, single terminal.
5. **`translation_success_contract_ordered_events()`** *(contract anchor — same judgment)*.
6. **`tts_success_contract_ordered_events()`** *(contract anchor — same; assert `TtsFirstAudio` precedes chunks + monotonic `seq` if not already pinned in B.2)*.
7. **`boundary_honors_cancellation()`** *(contract anchor — one consolidated/representative interface; skip if it's a verbatim B.2 re-test)* — mid-stream cancellation → `OperationCanceledException`.

> Tests 1–3 are the MUST (genuinely new). Tests 4–7 are contract anchors — **include the ones that earn C.5-reuse value; skip pure B.2 duplicates and say so.** Don't re-implement the B.1 mapper truth table here.

## Cross-doc invariant impact
- **None** — test-only; no model/contract change. (B.1's provider-contract cross-doc row already covers the interfaces; this is its CRITICAL-tier conformance suite.)

> **Implementer never edits `server/CLAUDE.md`, `ARCHITECTURE.md`, `MVP_TASKS.md`, or `server/LESSONS.md`.** Flag at Step 9 if anything surfaces.

## Things to flag at Step 2.5
1. **Net-new vs duplicate split (the key call — you hold B.2's exact assertions).** My default vote: tests 1–3 (error-code preservation) are MUST; for 4–7, keep a candidate only if asserting it **at the interface level** adds value B.2 doesn't (i.e. it becomes the contract C.5 reuses) — otherwise reference the B.2 test + skip. Propose your final list (which you keep, which you skip + why). I'd rather a tight 4–6 than a 12-test re-frame of B.2.
2. **Interface-level vs concrete-fake driving.** My default vote: drive via the interface type (`ISttProvider x = new FakeSttProvider(...)`) so C.5 swaps the construction + reuses the assertions. Minimal abstraction; the value is the C.5-reusable suite. Flag if you'd parameterize differently (e.g. an `[MemberData]` provider factory) — keep it simple for now.
3. **Shared collect/assert helper.** My default vote: a tiny `await provider.Stream(...).ToListAsync()`-style collector if it de-duplicates the arrange; only if it's genuinely shared. Flag if you add one.

## Dependencies + sequencing
- **Depends on:** B.1 (interfaces + `ProviderErrorMapper`), B.2 (fakes). All landed.
- **Blocks:** C.5 (extends this file with real-provider/HTTP-mock cases) — explicitly the forward consumer.

## Estimated commit count
**1 (this slice).** A focused test file — **scoped tight to avoid B.2 duplication**. Not a safety slice. Last Phase-B slice → Phase-B acceptance follows.

## Lessons-logged candidates anticipated
- **Convention candidate (maybe)** — the provider-boundary contract suite is interface-level + provider-agnostic so the SAME assertions run against fakes (B) + real providers (C.5); error-code preservation (a `*Failed` carries the real ARCH-012 code) is the boundary contract, distinct from the mapper truth table (B.1) and per-fake behavior (B.2). Only bank if it reads as durable, not slice-trivia.

## How to invoke
1. **Read this brief end-to-end** — the key task is the net-new-vs-duplicate split (Q1); don't re-test B.1/B.2/B.4.
2. **Run `/tdd provider_boundary_tests`**.
3. **Step 0/1** — confirm Feature + file.
4. **Step 2.5** — propose your final test list (kept vs skipped-as-duplicate, with reasons) via `SendMessage`; wait for the header before GREEN.
5. **Step 9** — categorized summary + ship/no-ship + draft commit message; note this is the last Phase-B slice (Phase-B acceptance follows).
