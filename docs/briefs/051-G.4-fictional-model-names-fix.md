# /tdd brief — model_name_correction (Finding 1 REAL root cause — fictional model names)

> **Real-key-smoke bug-fix — THE HEADLINE BLOCKER.** Cascade translation fails on every turn because the configured OpenAI translation model `gpt-5.4-nano` (+ `gpt-5.4-mini`) is **NOT AVAILABLE on the user's OpenAI key** → HTTP 400 (the lead verified directly: `gpt-5.4-nano`→400; `gpt-4o-mini` same `/v1/responses` shape→works). **Accuracy (lead-corrected):** these are NOT fictional — they're real, *newer* OpenAI models (gpt-5.4-nano ~$0.20/$1.25), just **gated / not provisioned on this key**. Our request shape + streaming are CORRECT — only the configured model is inaccessible (the spec defaulted to models not broadly available; `pricing.json` flagged them "CONFIRM at build"). The resolution is identical: switch to the broadly-available `gpt-5-nano`/`gpt-5-mini` the user chose. This was "I keep getting failures." NOT the NRE (SDK-internal red herring), NOT empty_transcript (a separate backend bug — tracked). Cross-area config/contract fix.
>
> **✅ FINALIZED (2026-05-31, user-confirmed):** translation models → **`gpt-5-nano`** (nano/fast tier) + **`gpt-5-mini`** (mini/better tier) — both real on the user's key. **Scope NARROWED to TRANSLATION ONLY:** the lead verified against the user's `/v1/models` that REALTIME (`gpt-realtime`, `gpt-realtime-mini`), TTS (`gpt-4o-mini-tts`), transcribe (`gpt-4o-mini-transcribe`), and Deepgram `nova-3` STT are **ALL REAL — do NOT touch those.** Only `gpt-5.4-nano`/`gpt-5.4-mini` is fictional. (The realtime/TTS conditionals elsewhere in this brief are therefore MOOT — translation only.) **⭐ Lead OWNS the live server (`:5179`, keys sourced) — when the BACKEND part commits, PING the lead with the hash; it rebuilds/restarts for the user's immediate live re-test. Do NOT start your own backend (port conflict).**

## Feature
Replace the fictional OpenAI model names with REAL ones the user's key serves, across **all** config/code/pricing/types/UI sites, so cascade translation (and, if also fictional, realtime) works from a clean clone — with REAL pricing rates (no `0.0`/placeholder for an active model).

## Names to substitute (BINDING)
- **Translation:** `gpt-5.4-nano` → **`gpt-5-nano`**, `gpt-5.4-mini` → **`gpt-5-mini`**. (Two distinct variants kept → the comparison-summary's by-variant cost split stays meaningful.)
- **Realtime / TTS / transcribe / STT:** **NO CHANGE** — all confirmed real (`gpt-realtime`/`-mini`, `gpt-4o-mini-tts`, `gpt-4o-mini-transcribe`, Deepgram `nova-3`). Leave `RealtimeModelCatalog`, the realtime Options/union/selector, and the TTS/transcribe config untouched.
- **Real pricing rates (backend) — AUTHORITATIVE (lead/user-provided, OpenAI published, per-1M-tokens):**
  - `gpt-5-nano` → input **$0.05** · cached-input **$0.005** · output **$0.40**
  - `gpt-5-mini` → input **$0.25** · cached-input **$0.025** · output **$2.00**
  Match `pricing.json`'s existing shape (`inputUsdPerMillionTokens` / `outputUsdPerMillionTokens`; add a `cachedInput*` field ONLY if the schema already supports it — else omit, don't invent schema). Bump `version`. Replaces the `gpt-5.4-*` keys (incl. the `gpt-5.4-mini` `0.0` placeholder). No `0.0` for an active model — the guard test enforces non-zero.

## Use case + traceability
- **Task ID:** G.4 (smoke bug-fix — THE blocker) / closes the Decisions-tabled "re-verify pricing.json model names + rates at build."
- **Architecture:** `ARCH-014` (pricing config), `ARCH-028` (`.env` contract), `ARCH-012` (provider Options), `ARCH-005` (the model unions), `ARCH-009` (`ConfigResponse` model catalog).

## Files expected to touch (cross-area)
**Backend:**
- `server/AiInterpreter.Api/Providers/OpenAI/OpenAiOptions.cs` — default `Model` (translation; + realtime/tts options if affected).
- `config/pricing.json` — the `gpt-5.4-*` keys → real model keys + **REAL rates**; bump `version`; keep the disclaimer.
- `server/AiInterpreter.Api/Config/ConfigService.cs` — the published translation-model list (+ realtime list if affected).
- `.env.example` — `OPENAI_TRANSLATION_MODEL` (+ `OPENAI_REALTIME_MODEL` if affected) default + comment.
- `server/AiInterpreter.Api/Realtime/RealtimeModelCatalog.cs` — IF realtime names are fictional (the E.1 mint allowlist).
- Backend tests asserting the fictional names (pricing-lookup, ConfigService catalog, realtime allowlist) → update to the real names.

**Frontend:**
- `web/src/components/SessionSetup.tsx` — the translation-model `<select>` options (shows `gpt-5.4-nano`/`-mini`) → real names (+ realtime selector if affected).
- `web/src/types/domain.ts` — the `TranslationModel` (+ `RealtimeModel` if affected) TS union literals.
- Frontend tests asserting the fictional names → update.

## Acceptance criteria
- [ ] No occurrence of `gpt-5.4-nano` / `gpt-5.4-mini` anywhere in `server/`, `web/`, `config/`, `.env.example` (grep clean) — replaced with the chosen real names.
- [ ] `config/pricing.json` has a real pricing entry **with real non-zero rates** for each active model (no `0.0` placeholder for a configured model); `version` bumped.
- [ ] **Guard test (TDD, both areas where applicable):** the **configured/default** translation (+ realtime) model **resolves to a pricing entry** (and the entry's rate is non-zero / not the placeholder) — so a fictional-or-unpriced model can't ship silently again. (Backend: a `CostEstimator`/pricing-config test that the default model has a real rate. Frontend: the `availableModels` selector + the union compile to the real names.)
- [ ] The realtime model names are verified real (or fixed); the `client_secrets` mint allowlist (`RealtimeModelCatalog`) matches.
- [ ] `/preflight` both areas clean; backend `dotnet test` + web `npm run test` green (the model-name assertions updated to real names).
- [ ] (Runtime, demo-checklist) a cascade turn translates successfully against the real key (the lead's `.env` override already proves the chosen model works; this makes it the default).

## Cross-doc invariant impact (orchestrator writes hot at Step 9 / round-seal)
- **Model-union literals change** (`TranslationModel` / maybe `RealtimeModel`) → `ARCH-005` enum/union list + **Appendix A** + the `server/CLAUDE.md` + `web/CLAUDE.md` cross-doc rows. **This is a cross-doc invariant change** — the implementer flags at Step 9; the orchestrator writes the rows; commits stagger.
- **Pricing keys + rates** → `ARCH-014` (the pricing block realization) + the `pricing.json` version note.
- **`.env` default** → `ARCH-028` + `.env.example` (already a realization site).

## Things to flag at Step 2.5
1. **(both) Which two real models for nano/mini?** PENDING the user (lead relaying). The brief is BLOCKED on this — don't start until filled. My non-binding lean: a gpt-5-nano/gpt-5-mini pair OR a gpt-4.1-nano/gpt-4.1-mini pair keeps the "nano vs mini" variant comparison meaningful (the whole point of the comparison summary is two translation variants). Confirm the user's pick.
2. **(backend) Real rates source.** Use OpenAI's published per-token rates for the chosen models (the lead/user supplies, or source). Flag if a rate is unknown → do NOT ship a `0.0` placeholder for an active model (that's the bug we're closing).
3. **(both) Realtime models real?** If the lead's `/v1/models` check says `gpt-realtime`/`-mini` are real → no realtime change. If fictional → same fix on the realtime sites (flag the scope expansion).

## Dependencies + sequencing
- **PRIORITY: this is the headline blocker — it LEADS the 2c slice (050).** Cascade can't run (let alone be compared) until the model is real. Dispatch the instant the names land (after the in-flight 048/050-frontend slices finish — slice atomicity; I'll re-sequence so 051 precedes 050-backend).
- **Cross-area:** backend (config/pricing/ConfigService/Options/+realtime) + frontend (selector/union). Coordinate on the chosen names (binding once filled).
- **Stopgap:** the lead applied a quick `.env` override to unblock live testing — 051 makes it permanent + clean-clone-correct.

## Estimated commit count
**2** (backend config/pricing/ConfigService/+guard-test = commit 1; frontend selector/union/+test = commit 2) — cross-area + a cross-doc invariant change (model unions), so the halves stagger + the doc rows ride the round-seal. Data/config correction + a regression-guard test; no safety invariant (model names are non-secret).

## Lessons-logged candidates anticipated
- **Convention candidate** — "Provider model NAMES are external reality, not spec freebies — a fictional/unverified model name fails only at the live boundary (fake-tested code is blind to it). Pin the configured default to a real, priced model with a guard test (default model ⟹ a non-placeholder pricing entry) so a fictional-or-unpriced model can't ship silently. (The §18/§24 'verify the live surface' discipline, applied to model IDs + pricing.)"
- **Architecture-doc note** — the real model names + rates; closes the ARCH-014 "CONFIRM at build" + the Decisions-tabled pricing re-verify.

## How to invoke (each half — AFTER names filled)
1. Read this brief (the filled real names are binding for both halves).
2. Backend: `/tdd model_name_correction_backend` — the guard test (default model resolves to a real non-zero pricing entry) RED-first, then swap the names/rates + update the name-asserting tests.
   Frontend: `/tdd model_name_correction_frontend` — update the selector + union + the name-asserting tests (the union compiling to real names is the pin).
3. Step 2.5: confirm the names (Q1) + rates (Q2) + realtime scope (Q3). Step 9: categorized (esp. the cross-doc model-union rows) + ship/no-ship + draft commit message.
