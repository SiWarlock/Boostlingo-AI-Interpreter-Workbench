# /tdd brief — config_endpoint (B.9b) — SAFETY

## Feature
`ConfigController` (`GET /api/config`) + `ConfigService` — reports capability flags **from key presence only, never key values** (safety invariant #1: standard keys are backend-only). Returns the ARCH-009 `ConfigResponse` (realtime/cascade configured booleans + selectable model catalogs + languages + pricing version). Adds controller infrastructure (first MVC controller). Also lands the **relocated B.9a end-to-end exception-boundary integration test** (first real controller endpoint = the clean seam to prove the global handler catches on the production path).

> **B.9 split (a/b/c):** B.9a global handler (landed `88a7f3e`), **this = B.9b ConfigController (invariant #1)**, B.9c SessionsController CRUD. SAFETY slice — own commit, never bundled; **mandatory `security-reviewer` pass at Step 8.**

## Use case + traceability
- **Task ID:** B.9 (part b)
- **Architecture sections it implements:**
  - `ARCH-009` — `GET /api/config` exact shape: `{ realtime:{configured, models[]}, cascade:{ stt:{configured, provider, model}, translation:{configured, provider, models[]}, tts:{configured, provider, model} }, languages:[…], pricingConfigVersion }`.
  - `ARCH-019` — security boundary #1: standard keys live only in backend env/config; **capability flags derive from key presence only — never values.**
  - `ARCH-008` — layering: the controller is thin and delegates to `ConfigService` (no logic in controllers).
  - `ARCH-002` — EN↔ES language pair; `ARCH-010`/`ARCH-014` — the switchable model catalogs (realtime: `gpt-realtime`/`-mini`; translation: `gpt-5.4-nano`/`-mini`).
  - `ARCH-020` — `ConfigEndpointTests` (IMPORTANT tier; the no-secret-echo pin is a **safety invariant** assertion).
- **Safety invariant pinned:** #1 — no standard API key value ever appears in the `/api/config` response.
- **Related context:**
  - Reads the existing A.2 `IOptions` (`DeepgramOptions`/`OpenAiTranslationOptions`/`OpenAiTtsOptions`/`RealtimeOptions`) — `configured = !string.IsNullOrWhiteSpace(ApiKey)`. Per `Program.cs` `BuildSectionOverrides`, `OPENAI_API_KEY` fans out to realtime+translation+tts; `DEEPGRAM_API_KEY` → stt.
  - `pricingConfigVersion` from the A.4 pricing singleton (`Result<PricingOptions>` → `Version` when loaded; → 2.5 Q4 for the unavailable case).
  - First MVC controller — adds `AddControllers()` + `MapControllers()` to `Program.cs` (health stays minimal-API; both coexist).
  - **Relocated B.9a proof** (carry-forward, origin B.9a): the global handler's end-to-end wire is proven here via a throwing `ConfigService` fake (`ConfigureTestServices`) → real endpoint → sanitized `UiError`.
  - Consumed by: D.2 (`SessionSetup`/`ModeToggle` call `GET /api/config` to disable unconfigured modes + populate the model selectors — Flow A).

## Acceptance criteria (what "done" means)
- [ ] `GET /api/config` returns 200 + the ARCH-009 `ConfigResponse` shape (camelCase via `JsonDefaults`).
- [ ] `configured` flags derive from `!string.IsNullOrWhiteSpace(ApiKey)`: `realtime`/`translation`/`tts` from the OpenAI key, `stt` from the Deepgram key.
- [ ] **Safety pin (#1):** a sentinel API-key value set in config does **NOT** appear anywhere in the serialized response (scan the full body); only booleans + model **names** + provider strings + languages + version are present.
- [ ] `realtime.models` = `["gpt-realtime","gpt-realtime-mini"]`; `translation.models` = `["gpt-5.4-nano","gpt-5.4-mini"]` (selectable catalogs, → 2.5 Q2); `stt.model`/`tts.model` = the configured single model; provider strings = `deepgram`/`openai`; `languages` = `["en","es"]`.
- [ ] `configured=false` for a stage whose key is absent (the ARCH-020 named case).
- [ ] The controller is thin — all assembly in `ConfigService` (ARCH-008).
- [ ] **Relocated boundary test:** overriding `ConfigService` with a throwing fake (`ConfigureTestServices`) → `GET /api/config` → the global handler returns a sanitized `UiError` (status 500, no stack/secret) — proves `UseExceptionHandler` catches a real endpoint's unhandled exception.
- [ ] All tests in `AiInterpreter.Tests/ConfigEndpointTests.cs` pass. `/preflight` clean. **Mandatory `security-reviewer` pass at Step 8.**

## Wiring / entry point (Step 7.5)
`GET /api/config` is a live HTTP route (first MVC controller — `AddControllers()`/`MapControllers()` wired in `Program.cs`). Reachable now; consumer = D.2 (frontend config-gating). `ConfigService` registered in DI. Named + reachable, not deferred.

## Files expected to touch
**New:**
- `Config/ConfigService.cs` — builds `ConfigResponse` from Options key-presence + catalogs.
- `Config/ConfigModels.cs` — `ConfigResponse` + nested records (→ 2.5 Q7 for placement).
- `Controllers/ConfigController.cs` — thin `GET /api/config` delegating to `ConfigService`.
- `AiInterpreter.Tests/ConfigEndpointTests.cs` — configured flags + **no-secret-echo** + shape + the relocated boundary test.

**Modified:**
- `Program.cs` — `AddControllers()` + `app.MapControllers()` (first controller) + register `ConfigService`.

If implementation needs files beyond this, **flag at Step 2.5**.

## RED test outline (Step 2) — `AiInterpreter.Tests/ConfigEndpointTests.cs`
The no-secret-echo pin (test 2) is the safety centerpiece. Use `WebApplicationFactory` (env-var-touching → coordinate with the existing host-test collection to avoid cross-test env races — see B.9a's `[Collection("HostEnv")]`).

1. **`config_returns_configured_true_when_keys_present`** — keys set → `realtime`/`translation`/`tts`/`stt` `configured=true`; shape matches ARCH-009 (models/provider/languages/version present).
2. **`config_response_excludes_secret_values`** (SAFETY #1) — set sentinel `OPENAI_API_KEY=sk-SENTINEL-CONFIG` + `DEEPGRAM_API_KEY=dg-SENTINEL` → the serialized body contains **neither** sentinel; booleans + model names present.
3. **`config_returns_configured_false_when_key_absent`** — unset OpenAI key → `realtime`/`translation`/`tts` `configured=false`; unset Deepgram → `stt.configured=false` (ARCH-020 named case).
4. **`config_model_catalogs_and_languages`** — `realtime.models`/`translation.models` = the two-model catalogs; `stt.model`/`tts.model` = configured single model; `languages=["en","es"]`.
5. **`config_pricing_version_present`** — `pricingConfigVersion` reflects the loaded pricing version (→ 2.5 Q4 for the unavailable fallback).
6. **`unhandled_endpoint_exception_returns_sanitized_uierror`** (relocated B.9a boundary proof) — override `ConfigService` with a fake that throws (incl. a secret substring) via `ConfigureTestServices` → `GET /api/config` → response is a sanitized `UiError` (500, no stack/secret in the body).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **NEW `ConfigResponse` + nested records** — these are API DTOs. Appendix A already lists `ConfigResponse` under the "API DTOs" umbrella row. Flag at Step 9: I confirm the concrete nested shape matches ARCH-009; **no new Appendix A row expected** (covered by the API-DTOs row) — I'll refine that row only if the nested detail warrants.
- **No model field changes** to existing contracts.

> **Implementer never edits `server/CLAUDE.md`, `ARCHITECTURE.md`, `MVP_TASKS.md`, or `server/LESSONS.md`.** Flag at Step 9; orchestrator writes hot. **Safety findings escalate.**

## Things to flag at Step 2.5
1. **Controller vs minimal-API.** My default vote: an MVC `ConfigController` per ARCH-006 (`Controllers/ConfigController.cs`), adding `AddControllers()`/`MapControllers()` (the first controller; health stays minimal-API). Flag if you'd rather keep `/api/config` minimal-API and defer controller infra to B.9c.
2. **Model catalogs — hardcode vs derive.** My default vote: **hardcode** the selectable catalogs as constants (`realtime: gpt-realtime/-mini`, `translation: gpt-5.4-nano/-mini`) — fixed by ARCH-010/ARCH-014, matches the ARCH-009 example exactly. Alternative: derive from `pricing.json` model keys (config-driven, but couples to pricing + degrades when pricing is unavailable). Flag if you prefer derived.
3. **`configured` detection.** My default vote: `!string.IsNullOrWhiteSpace(ApiKey)` from each Options; OpenAI key → realtime+translation+tts, Deepgram key → stt (mirrors `Program.cs` fan-out).
4. **`pricingConfigVersion` when pricing unavailable.** My default vote: `Version` from the pricing `Result` when loaded; when the pricing file failed to load, emit a safe fallback string (e.g. `"unavailable"`) — never throw. Flag your preference (null vs "unavailable").
5. **No-secret-echo test.** My default vote: set sentinel key values in config + assert the serialized response body contains neither (mirrors the B.7a/B.8 sentinel-scan pattern). The active scan is the invariant-#1 drift defense.
6. **Boundary test here vs B.9c.** My default vote: **here** — B.9b is the first real controller endpoint, so the relocated B.9a end-to-end boundary proof (throwing `ConfigService` fake → sanitized `UiError`) lands cleanest now rather than waiting for B.9c. It's a test (not #4 production code), so it doesn't mix safety-invariant production code in this commit.
7. **`ConfigResponse` records placement.** My default vote: `Config/ConfigModels.cs`. Flag if you prefer `ConfigService.cs` or `Controllers/`.

## Dependencies + sequencing
- **Depends on:** A.2 (Options), A.4 (pricing singleton), B.8 (`UiError`/`ErrorSanitizer`), B.9a (global handler — the boundary test exercises it). All landed.
- **Blocks:** D.2 (frontend config-gating + model selectors). B.9c (SessionsController) is independent of this — either order after B.9a.

## Estimated commit count
**1 (this slice).** SAFETY slice (invariant #1) — **its own commit**, never bundled. Mandatory `security-reviewer` pass gates it. (The boundary test is a test artifact riding along, not separate #4 production code.)

## Lessons-logged candidates anticipated
- **Convention candidate** — capability reporting derives booleans from key **presence** (`!IsNullOrWhiteSpace(ApiKey)`), never the value; the no-secret-echo sentinel-scan test is the invariant-#1 drift defense; the controller is thin (delegates to `ConfigService`, ARCH-008).
- **Architecture-doc note candidate** — confirm the concrete `ConfigResponse` nested shape (the per-stage configured/provider/model[s] structure) against ARCH-009.

## How to invoke
1. **Read this brief end-to-end** — SAFETY slice; the no-secret-echo pin (test 2) comes first.
2. **Run `/tdd config_endpoint`**.
3. **Step 0/1** — confirm Feature + file list (note the first-controller infra + the relocated boundary test).
4. **Step 2.5** — per-test write-up + answers via `SendMessage`; wait for the header before GREEN.
5. **Step 8** — `security-reviewer` is **mandatory** (invariant #1). Surface its result at Step 9.
6. **Step 9** — categorized summary + ship/no-ship + draft commit message; **escalate any safety finding**.
