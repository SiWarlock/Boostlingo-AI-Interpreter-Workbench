# /tdd brief — solution_repo_scaffold

> **Scaffold slice — read the "Scaffold-slice note" first.** This is the project's first slice (bootstrap). It stands up structure, not behavior, so it does **not** run a classic `/tdd` RED→GREEN cycle. The two protocol checkpoints (plan-confirm + Step-9 summary) still apply.

## Feature
Stand up the .NET solution (`AiInterpreter.Api` + `AiInterpreter.Tests`) and the Vite React-TS web app, with the full `ARCH-006` directory skeleton, `.gitignore`, and placeholder config + docs — so every later phase (A.2 → G) has a compiling, correctly-laid-out foundation to build against.

## Use case + traceability
- **Task ID:** A.1
- **Architecture sections it implements:** `ARCH-006` (Repository Scaffold) — the canonical directory tree.
- **Related context:** First slice; no prior slices. Build/run host wiring is **ARCH-029**, but that's A.5 — A.1 only needs the projects to *build*, not the host wired (no DI/CORS/health/port yet). This brief lives at `docs/briefs/001-A.1-solution-repo-scaffold.md`.

## Scaffold-slice note (READ FIRST — execution mode)
A.1 is **scaffolding, not deterministic behavior.** There is no failing test that can pin "the project structure exists" before it exists. Per the project's TDD posture (root `CLAUDE.md`: *"Can I write a failing test that pins this behavior deterministically? If yes, /tdd. If no, ship via the non-deterministic-coverage path"*), A.1 ships via the **structural + build-success path**, not RED→GREEN. Concretely:

- **No `/tdd` RED tests.** The acceptance gate is: `dotnet build` clean, `dotnet test` green, web `npm run build` + `npm run typecheck` + `npm run lint` clean, `npm run test` green, layout matches `ARCH-006` exactly, `.gitignore` correct, **no secrets committed.**
- **Keep both protocol checkpoints:**
  1. **Plan-confirm (Step-2.5-equivalent)** — before creating files, send me your scaffold plan: package/SDK versions, template choices, and your answers to the design questions below. I reply with a parseable header (`APPROVED.` / `TWEAK:` / `ADD:`). Do not start creating files until you have `APPROVED.`.
  2. **Completion summary (Step-9-equivalent)** — after the acceptance gate passes, send the categorized summary + ship/no-ship + a draft commit message. I reply commit-message-first; that reply is your go to commit (Step-10-equivalent).
- **Run `/session-start` first** (this is the first slice of the session). Then execute the scaffold against the plan. Skip the `/tdd` walker — it has no RED target here.

## Acceptance criteria (what "done" means)
- [ ] A .NET solution links two projects: `server/AiInterpreter.Api` (ASP.NET Core Web API, **`net8.0`**, **nullable enabled**, **warnings-as-errors** per root typing posture) and `server/AiInterpreter.Tests` (xUnit, project-references the Api).
- [ ] `web/` is a Vite **React-TS** app: `package.json` with scripts `dev` / `build` / `lint` / `format:check` / `typecheck` / `test` all wired and runnable; `vite.config.ts`; TypeScript **strict** mode; ESLint + Prettier + Vitest installed as dev-deps.
- [ ] Directory layout matches `ARCH-006` **exactly** — every server subdir exists: `Controllers/`, `Realtime/`, `Cascade/`, `Providers/{Abstractions,Deepgram,OpenAI,Fakes}/`, `Sessions/`, `Metrics/`, `Cost/`, `Evaluation/`, `Config/`, `Security/`, `Common/`; and `web/src/{api,audio,realtime,cascade,state,components,types}/`. Use a `.gitkeep` in any dir that would otherwise be empty (git doesn't track empty dirs).
- [ ] `.gitignore` excludes `.env`, `.env.local`, `server/**/bin/`, `server/**/obj/`, `web/node_modules/`, `data/sessions/*.json`; and **keeps** `data/sessions/.gitkeep` (negation pattern or path-specific keep).
- [ ] `config/pricing.json` placeholder created (valid JSON; A.4 populates the real `ARCH-014` block). `docs/COMPARISON_WRITEUP.md` + `docs/DEMO_SCRIPT.md` placeholders created.
- [ ] `data/sessions/.gitkeep` created.
- [ ] **Acceptance gate passes:** `dotnet build` (both projects) + `dotnet test` green; `cd web && npm install && npm run build` succeeds; `npm run typecheck` + `npm run lint` clean; `npm run test` green.
- [ ] **No secrets** in any committed file (no `.env`, no real keys, no `appsettings` with secret values).

## Files expected to touch
**New (server):**
- `server/AiInterpreter.sln` — solution file (default location; see Step-2.5 Q1).
- `server/AiInterpreter.Api/AiInterpreter.Api.csproj` — `net8.0`, `<Nullable>enable</Nullable>`, `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>`.
- `server/AiInterpreter.Api/Program.cs` — **minimal buildable** `WebApplication` (builder → build → run), **no** endpoints/DI/CORS (those are A.5). Strip any template sample (WeatherForecast).
- `server/AiInterpreter.Api/appsettings.json` + `appsettings.Development.json` — template-generated, left minimal (A.2 populates Options/config).
- `server/AiInterpreter.Api/{Controllers,Realtime,Cascade,Providers/Abstractions,Providers/Deepgram,Providers/OpenAI,Providers/Fakes,Sessions,Metrics,Cost,Evaluation,Config,Security,Common}/.gitkeep` — empty ARCH-006 dirs.
- `server/AiInterpreter.Tests/AiInterpreter.Tests.csproj` — xUnit, references Api.
- `server/AiInterpreter.Tests/ScaffoldSmokeTest.cs` — one trivial harness-proof test (see Step-2.5 Q3).

**New (web):**
- `web/package.json`, `web/vite.config.ts`, `web/tsconfig.json` (+ `tsconfig.node.json` if the template emits one), `web/index.html`, `web/src/main.tsx`, `web/src/App.tsx`.
- ESLint + Prettier config (`web/eslint.config.js` / `.prettierrc` or equivalent), Vitest config (in `vite.config.ts` or `vitest.config.ts`).
- `web/src/{api,audio,realtime,cascade,state,components,types}/.gitkeep` — empty ARCH-006 dirs.
- `web/src/App.test.tsx` (or `web/src/scaffold.test.ts`) — one trivial Vitest harness-proof test.

**New (root / shared):**
- `.gitignore`, `config/pricing.json` (placeholder), `docs/COMPARISON_WRITEUP.md` (placeholder), `docs/DEMO_SCRIPT.md` (placeholder), `data/sessions/.gitkeep`.

> **Standard Vite React-TS template files are expected and fine** even though `ARCH-006`'s tree elides them (`tsconfig.json`, `index.html`, eslint/prettier config). That's a benign expansion, pre-authorized here — no need to flag it as scope creep. If you find you need a server file beyond this list (e.g. a `Directory.Build.props`), flag it at plan-confirm.
>
> **Explicitly NOT in A.1** (deferred — do not create): `.env.example` (A.2), populated `appsettings` Options/config (A.2), real `pricing.json` values (A.4), full `Program.cs` host wiring / health endpoint / CORS / port (A.5), domain models + `web/src/types/domain.ts`+`metrics.ts` content (A.3), `README.md` + `AGENTS.md` (Phase G). Create the *empty `types/` dir* now (.gitkeep), not its contents.

## Structural verification (replaces RED test outline)
Instead of RED→GREEN, verify structurally after building the scaffold:

1. **Layout parity** — `find server/AiInterpreter.Api -type d` (and the web equivalent) matches the `ARCH-006` tree exactly (no missing dir, no extra dir).
2. **Build parity** — `dotnet build` clean (nullable + warnings-as-errors on, so the minimal Program.cs must be warning-free); `dotnet test` green.
3. **Web build parity** — `npm install && npm run build` succeeds; `npm run typecheck` + `npm run lint` clean; `npm run test` green.
4. **Ignore parity** — `git status` after a dummy `data/sessions/foo.json` + `server/.../bin/` shows them ignored, while `data/sessions/.gitkeep` is tracked. (Sanity-check the `.gitignore` negation actually keeps `.gitkeep`.)
5. **Secret parity** — `git status` / staged set contains no `.env`, no key material.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none — A.1 introduces no domain models (those are A.3). The cross-doc invariant tables in `server/CLAUDE.md` / `web/CLAUDE.md` stay `(none yet)`.
- **Orchestrator doc rows to write hot (Step 9 routing):** none expected. If `ARCH-006`'s tree turns out to omit a file the scaffold genuinely needs (e.g. `tsconfig.json`), flag it at Step 9 as an **Architecture-doc note** — I'll add a benign annotation to `ARCH-006`.

> **Implementer never edits** `server/CLAUDE.md`, `web/CLAUDE.md`, `ARCHITECTURE.md`, `MVP_TASKS.md`, `*/LESSONS.md`, or `docs/briefs/` — orchestrator territory. Flag at Step 9.

## Things to flag at Step 2.5 (answer these in your plan-confirm send)
1. **Solution file location.** `server/AiInterpreter.sln` vs a repo-root `.sln`. My default vote: **`server/AiInterpreter.sln`** — keeps all .NET under `server/` matching the `ARCH-006` tree; `web/` stays npm-only. (`dotnet test` / `dotnet build` then run from `server/` or target the sln explicitly.)
2. **`Program.cs` scope.** Minimal buildable host vs pre-wiring A.5. My default vote: **minimal `WebApplication` (builder → build → run), no endpoints/DI/CORS** — so `dotnet build`/`dotnet run` work but A.5 owns the real host. Do **not** pre-build A.5's `/api/health`.
3. **Smoke tests — include or skip.** My default vote: **include one trivial xUnit test + one trivial Vitest test** — a harness-proof that `dotnet test` / `npm run test` actually discover + run a green target, de-risking Phase B/D before they rely on the harness. Explicitly a harness-proof, not a behavior pin. (If you'd rather skip the Vitest one until D, say so — I can be talked out of the web smoke.)
4. **Vitest + ESLint + Prettier wired now or deferred to first real frontend slice (D)?** My default vote: **wire now** — `web/CLAUDE.md` standard commands already assume `npm run lint` / `format:check` / `typecheck` / `test` exist; wiring at scaffold avoids a retrofit and lets `/preflight` pass from day one.
5. **`config/pricing.json` placeholder shape.** My default vote: **valid-JSON placeholder** with a `version` marker + a note pointing to A.4 (e.g. `{ "version": "placeholder-A.1", "note": "real pricing block lands in A.4 (ARCH-014)" }`) — so any early loader gets valid JSON, not a parse error. A.4 replaces it wholesale.
6. **.NET template flavor.** `dotnet new webapi` (minimal-API vs controller-based template) + whether to keep template cruft. My default vote: **minimal-API template, then strip the WeatherForecast sample + the `.http` file** — keep `Program.cs` minimal-buildable; real controllers are B.9, A.1 just creates the empty `Controllers/` dir.

## Dependencies + sequencing
- **Depends on:** nothing (first slice; bootstrap — no commits yet).
- **Blocks:** ALL subsequent slices (A.2 → G). Every later slice compiles/builds against this skeleton, so a clean, exact-to-ARCH-006 scaffold is load-bearing.

## Estimated commit count
**1.** Single scaffold commit — one logical unit (the project skeleton), no safety invariant touched, fully bisectable as "the scaffold." Conventional Commits type **`build`** (or `chore`) — I'll finalize the message at your Step-9-equivalent send. Stage files explicitly (`git add <path>`), never `git add -A`/`.`.

## Lessons-logged candidates anticipated
- **Convention candidate** — scaffold conventions worth a one-liner if they recur: `.gitkeep` for empty `ARCH-006` dirs; `.sln` location; stripping `dotnet new` template cruft; the `.gitignore` negation that keeps `data/sessions/.gitkeep` while ignoring `*.json`.
- **Architecture-doc note candidate** — `ARCH-006`'s tree elides standard template files (`tsconfig.json`, `index.html`, eslint/prettier config); if that ambiguity bites, a benign `ARCH-006` annotation noting "standard template files implied."

## How to invoke
1. **Read this brief end-to-end** (esp. the Scaffold-slice note — this slice does **not** run `/tdd`).
2. **Run `/session-start`** (first slice of the session) — orient against `MVP_TASKS.md` + `ARCH-006`.
3. **Send your plan-confirm** (Step-2.5-equivalent): package/SDK versions, template choices, answers to the 6 design questions. Wait for my `APPROVED.`.
4. **Build the scaffold**, then run the structural verification + acceptance gate.
5. **Send the completion summary** (Step-9-equivalent): categorized summary + ship/no-ship + draft commit message. I reply commit-message-first; you commit (Step-10-equivalent) + send the done-with-slice hash.
