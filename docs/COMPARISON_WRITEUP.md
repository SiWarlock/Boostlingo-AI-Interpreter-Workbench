# Comparison Write-up — Realtime vs Cascade

> **Placeholder (A.1 scaffold).** The real 1–2 page comparison write-up lands in **G.5** (ARCH-023):
> latency, quality (WER), cost, and controllability across both modes, plus an evidence-backed
> recommendation for when each mode fits — using real measured values from the persisted session JSON.
