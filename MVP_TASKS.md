# MVP_TASKS.md — AI Interpreter Workbench

> **Phase note.** This is the single source of truth for build state + the phase plan. It is generated from `ARCHITECTURE.md` (the design contract) and must not invent architecture not present there — if a task needs something the architecture lacks, flag it and add it to `ARCHITECTURE.md` first. Locked decisions (ARCH-003): TS SPA + .NET/C#; Realtime via browser WebRTC + backend-minted ephemeral credential; Cascade as a **fully streaming** Deepgram `nova-3`(`multi`) → OpenAI `gpt-5-nano`/`mini` → OpenAI `gpt-4o-mini-tts` pipeline; turn-based click start/stop (delimits a turn; audio streams within it); local JSON persistence (no raw audio, no secrets); config-driven estimated cost/min; scripted WER. **Build order is local-first; backend seams + tests precede UI; the two highest-risk integrations (Realtime WebRTC = Phase E, live-streaming cascade = Phase C/D) are sequenced last, each with a documented fallback.** The `.claude/` agent-team scaffolding is now in place (root `CLAUDE.md`, `server/` + `web/` area guides + `LESSONS.md`, `.claude/commands` + `.claude/agents`, `docs/` briefing + team-protocol + brief template). The Session protocol block below is active.

> **Session protocol:**
> - **Session start** — orchestrator runs `/orchestrate-start`; implementer runs `/session-start`. Confirm the session target.
> - **Session end** (only when the user says we're done): implementer `/session-end` (TDD + cross-doc audit + `/preflight`); orchestrator `/orchestrate-end` (reconcile checkboxes, append Log, round commit + push).

> **Reference deadlines / budget:**
> - Build ceiling: **~5 days (≈ 2026-06-02)**; aim to finish faster and keep scope tight. Favor the architecturally-correct, PRD-faithful build; do not gold-plate — use the Trims catalog and documented fallbacks as the pressure valve.

> **Spec-anchor convention (architecture-as-contract).** Each phase header carries a `**Spec anchors:**` block listing the `ARCH-###` sections it implements; each task also carries an authoritative inline `Anchors:` line (the header block is a redundant summary). Re-read the listed anchors at session start. If a slice surfaces behavior the anchors don't cover, that's a cross-doc invariant flag — either the anchor is missing or the implementation has drifted. **Cite `ARCH-###` (stable IDs), not `§` numbers.** **Path convention:** backend file paths are relative to `server/AiInterpreter.Api/`, test paths to `server/AiInterpreter.Tests/`, and `web/...` paths are repo-root-relative (per ARCH-006). **Intentionally uncited meta anchors:** ARCH-001 (exec summary), ARCH-024 (alternatives), ARCH-026 (sequencing — realized by the phase plan).

---

## Currently in progress

**▶ ACTIVE — real-key-smoke bug-fix + realtime-completion arc (2026-05-31).** Resumed from handoff-003; **H.1 UI baseline SHIPPED** (H.1a `1d99eaf` + H.1b `c753539` — the delivered Boostlingo design system applied: 3-col shell, active-mode highlight, all panels styled; 209 web green). The **real-key smoke** then surfaced live integration bugs (invisible to fake tests). **Live-CONFIRMED working:** cascade model selection (gpt-5-nano, no 400) + realtime AUDIO playback (WebRTC handshake + interpretation). **⭐ Lead owns the live server `:5179`** (keys sourced); restarts on each backend fix for the user's re-test. **The H.1 + bug-fix slices are now ROUND-SEALED** (crash-recovery finish by the fresh orchestrator — see the Log entry below); all the orchestrator doc-routing is committed. The queue continues from **053 gaps A/C** (await a live `oai-events` DC stream).

**Bug-fix arc status:**
- ✅ **048** cascade-stop ARCH-018 hardening + idempotent Deepgram Stop (`ccd9cb6`) — the `[Error] Stop: NRE` was Deepgram-SDK-INTERNAL caught-noise (red herring), not our crash.
- ✅ **049** frontend smoke bugs (`27cab1f`+`ccfcc17`) — End-button overflow + realtime negative-playback-latency (per-turn sink stamp, refines §16/§17) + MetricsPanel cascade-stages-gating.
- ✅ **051** model-name fix `gpt-5.4-*`→`gpt-5-nano`/`gpt-5-mini` (`1b352e0` frontend + `8c46d13` backend) — the REAL Finding-1 root cause (models real but NOT on the user's key → 400); **LIVE-CONFIRMED**. Guard test (default model ⟹ real non-zero pricing) prevents recurrence.
- ✅ **052** cascade empty_transcript tolerance (`79ef9c7`) — skip empty STT finals; fail only if NO non-empty final ever (two-flag). The active cascade blocker: a trailing empty final failed a *fully-correct* turn; also removed the inflated-latency symptom. (Lead restarted `:5179`; user re-testing a clean `completed` cascade turn — final live confirm pending.)
- ⏳ **050** mode-switch endpoint + `RecordModeTransition` (Finding 2c) — frontend ✅ `7dc398e`; **050-backend QUEUED + pre-approved** (atomic re-entrant `SwitchMode`; after 052). Completes 2c → turns after a mode toggle get the right mode → valid by-mode comparison.
- 🔄 **053** realtime transcript+metrics gap — **053-B LANDED (`e777a31`)**: re-assert input transcription in `session.update` (the partial `audio.input` was clobbering the mint's config) + a DEV `oai-events` DC-frame logger. **Gaps A (transcript GA `type` strings) + C (metrics reporting) await a LIVE DC stream** — audio plays via the WebRTC media track, which does NOT prove the `oai-events` DC delivers events. Fresh-team: capture the DEV diagnostic on the next realtime smoke (re-pin any divergent GA `type` → ARCH-010 §7).

**Smoke matrix — confirmed vs pending:** cascade model ✅ · cascade turn 🔄 pending-052 · realtime audio ✅ · realtime transcripts+metrics 🔄 pending-053 · mode-toggle ⏳ pending-050-backend · WER eval ⏳ pending-live · 4 model-variant comparison ⏳ pending-live · 5-min stability ⏳ pending-G.4. **Cost-in-summary** 🔄 = downstream of turns succeeding (verify post-052 cascade / post-053 realtime — failed turns get no `costEstimate`; NOT a separate fix). **`ProcessSendQueue`/`Stop`/`Connect` NREs** = benign Deepgram-SDK-internal caught-noise (052 confirms not-dropping-frames at Step-9).

**Queue (priority order) after the active fixes:** **H.3** session-history view (⬆ PULLED UP — user asked twice; cross-area: a NEW `GET /api/sessions` list endpoint [none exists today] + a frontend history view reusing the panels; pairs with the 2c timeline) → **metrics-polish** (latency-headline target on speech-end→first-audio not total-turn + per-stage-n/a-on-failed-turn — see Carry-forward) → **G.2b** backend `.env` auto-loader → **G.3 demo / G.5 real numbers / G.4 5-min stability** (smoke-gated) → **Phase I** auto-VAD (both modes + manual toggle; REVISES ARCH-003). Briefs `047`–`053` authored. **✅ ROUND-SEAL CHECKLIST — EXECUTED this round** by the fresh orchestrator (crash-recovery finish; the predecessor orch hit its 75% ACTION mid-arc). All slice commits were landed + HEAD-verified; this round commit lands all the orchestrator doc-routing below (record retained for the audit trail):
- **DONE (banked, uncommitted in the working tree — verify + include in the seal commit):** this MVP_TASKS reconciliation; `web/LESSONS.md` §22 (styling) + §23 (realtime per-turn stamp) + their `web/CLAUDE.md` index rows; `ARCHITECTURE.md` ARCH-007 "Styling (H.1)" note; Phase I + H.3-pulled-up + the metrics-polish Carry-forward entry; the `.gitignore` `.gstack` line (tooling auto-added).
- **DONE at the seal (all 8 items executed this round; broadened scope flagged to the lead):**
  1. **051 model-name cross-doc (⭐ CROSS-DOC INVARIANT — model-union literals):** `gpt-5.4-nano`/`gpt-5.4-mini` → `gpt-5-nano`/`gpt-5-mini` in `ARCHITECTURE.md` (≈ lines 65, 129, 173, 239, 407, 634, 643, 673, 691, 812, 854, 1189, 1290; the ARCH-005 union; Appendix A) + the **ARCH-014 pricing block** (≈ 1094-1095 → real rates **nano 0.05/0.40, mini 0.25/2.00**, version `2026-05-31-payg-estimates`, drop the `0.0`/CONFIRM note + the §16 build-confirm row ≈ 1357 + the ≈1460 audit note) + `README.md` (≈ 18, 65) + a `server/CLAUDE.md` cross-doc note.
  2. **README clean-clone gotcha:** "the configured models must be available on YOUR OpenAI key; defaults `gpt-5-nano`/`gpt-5-mini` (broadly available)" (lead's note — the models were real-but-not-on-the-user's-key, not fictional).
  3. **`server/LESSONS.md`:** the **048** lesson (WS stop-path catch-broaden + idempotent SDK Stop + ⭐ the *an SDK's own caught-and-logged internal exception can masquerade as our crash — verify the log category* meta-insight) + the **052** lesson (tolerate spurious empty STT finals: skip + fail `empty_transcript` only if NO non-empty final ever; two-flag; the inverse of §28 join-finals) + their `server/CLAUDE.md` index rows.
  4. **`ARCHITECTURE.md` realization notes:** ARCH-011 (cascade stop-path fail-closed-on-unexpected-exception + empty-final skipping) · ARCH-013 (frontend `deriveTurnMetrics` now implements the documented `tts.first_audio ?? realtime.first_audio_delta ?? playback.started` chain — from 049/Fix A).
  5. **050-frontend cross-doc:** `web/CLAUDE.md` mirror rows — `SetModeRequest` + `ModeTransitionEvent` graduation (frontend types; `modeTransitions` stays opaque). *(The 050-BACKEND endpoint → ARCH-009/Appendix A `POST /sessions/{id}/mode` row + ARCH-010/017 Flow-G note wait for the fresh team's 050-backend slice.)*
  6. **053 cross-doc:** none expected (B re-asserts existing config); a re-pinned GA `type` string would be an ARCH-010 §7 note (pending the live DC log — fresh team).
  7. **Tick** 048/049/051/052 DONE + a **Log entry** for the H.1 + bug-fix arc round.
  8. **Round commit** (`command git commit -F` + HEAD-verify; brief files `047`–`053` ride it) + push (none configured).
- **Slice commits (all landed):** H.1 `1d99eaf`+`c753539` · 048 `ccd9cb6` · 049 `27cab1f`+`ccfcc17` · 051 `1b352e0`+`8c46d13` · 050-frontend `7dc398e` · 052 `79ef9c7` · 053-B `e777a31`. Briefs `047`–`053`. Live-server: **lead owns `:5179`** (keys sourced).

- **Round sealed 2026-05-31 (H.1 UI baseline + real-key-smoke bug-fix arc — 048/049/050-frontend/051/052/053-B)** — round-seal: this round commit at HEAD (crash-recovery finish by the fresh orchestrator; predecessor hit 75% mid-arc; prior round-seal `d0a457b` PRE-H.1) · slice commits: H.1 `1d99eaf`+`c753539`, 048 `ccd9cb6`, 049 `27cab1f`+`ccfcc17`, 051 `1b352e0`+`8c46d13`, 050-frontend `7dc398e`, 052 `79ef9c7`, 053-B `e777a31` · session docs `013` (backend) + `014` (frontend; web 209 green) · briefs `047`–`053` · **doc-routing landed this round:** the 051 model-name cross-doc invariant (`gpt-5.4-*`→`gpt-5-nano`/`gpt-5-mini` + real rates nano 0.05/0.40 · mini 0.25/2.00 + pricing version `2026-05-31-payg-estimates`) across `ARCHITECTURE.md` (ARCH-005 union / Appendix A / ARCH-014 block) + `README.md` (+ clean-clone gotcha) + `COMPARISON_WRITEUP.md` + the smoke runbook + design-specs + `orchestrator-briefing.md` + `server/CLAUDE.md` rows (broader than the original 3-file checklist — full living-doc sweep; vendored `BoostlingoDesignSystem/` kit + historical briefs/sessions intentionally left); server LESSONS §30 (048 stop-path fail-closed + the SDK-internal-noise-masquerade meta-insight) + §31 (052 empty-final two-flag tolerance) + index rows; `ARCHITECTURE.md` ARCH-011 (048/052) + ARCH-013 (049-A first-audio chain) realization notes; `web/CLAUDE.md` `SetModeRequest`/`ModeTransitionEvent` row · ⭐ live server: **lead owns `:5179`** (keys sourced; restart per backend commit). G.2b (`.env` auto-loader) still pending — a clean clone needs `.env` SOURCED (`set -a && source ../../.env && set +a && dotnet run`). **Next target:** 053 gaps A/C (await a live `oai-events` DC stream) → 050-backend (mode endpoint, pre-approved) → H.3 (session-history; NEW `GET /api/sessions`) → metrics-polish → G.3/G.5/G.4 → Phase I auto-VAD.

**PHASE F — ✅ FULLY COMPLETE (F.1 backend + F.2/F.3 frontend + F.4 backend eval-turn-exclusion). ⏭ Phase G (Docs, Demo, Stability & Write-up) — partly DRAFTED; real-data items SMOKE-GATED (user). Phase H (UI/UX) — ADDED, H.1 pending the user's design.** ✅ Phases A–F done — both interpretation modes ship + the full evaluation/comparison layer, comparison counts now EXACT: **F.1** evaluation endpoints (`7e29f35`/`f9cf6a9`, s010), **F.2** `EvaluationPanel` (`f1697bc`), **F.3** `ComparisonSummary` (`f7daae1`) — s011, **F.4** eval-turn exclusion (`IsEvaluation` marker at `/wer`; `ModeSummary` excludes; `1e8a655`) — s012. **Backend suite 345 green · web suite 193 green; every slice security/code-quality-reviewed.** ⏭ **NEXT — Phase G:** ✅ **G.1 README** + **G.2 `docs/AI_COLLABORATION.md`** authored (`576078d`, user-confirmed form); 🟡 **G.5 `docs/COMPARISON_WRITEUP.md` SCAFFOLDED** (structure/methodology/limitations done; numbers are `[SMOKE: …]` placeholders). 🔒 **HELD for the real-key smoke (user gate):** G.3 demo script, G.5's real measured numbers, G.4 5-min stability run — these need provider keys + a live run. G.4 also carries the accreted backend code-hygiene cleanups. G.6 optional deploy. **Standing non-blocking:** the real-key smokes (cascade `usage`-nesting + realtime GA wire-shape, ARCH-020-exempt) + the backend `ModeTransitionEvent` persistence slice (Phase-E carry — needs a backend+frontend cycle if the Flow-G timeline is wanted; NOT deliverable-blocking).

- **Round sealed 2026-05-31 (F.4 — eval-turn exclusion; Phase F FULLY complete) + Phase-G no-live-data docs** — round-seal: this `docs(tasks)` commit at HEAD · slice commit: F.4 `1e8a655` (`IsEvaluation` marker at `/wer`, `SummarizeMode` excludes; backend 339→345 green; security PASS; zero frontend change — F.3 counts auto-exact) · session doc `012` (impl `/session-end` `1bb8ed2`) · brief 046 · server lesson §29 + index row + ARCH-005 `InterpretationTurn`+`IsEvaluation` + Appendix A + the ARCH-013 top-level-vs-per-mode semantic note · **also folds the Phase-G no-live-data docs** (G.1 README `[x]`, G.2 narrative `[x]` user-confirmed, G.5 scaffold `[~]`; the README front-door links them; committed earlier at `576078d`) · next target: **Phase G real-data items (smoke-gated)**.

- **Round sealed 2026-05-31 (Phase-F COMPLETE — F.2 `EvaluationPanel` + F.3 `ComparisonSummary`; closes Phase F core)** — round-seal: `407be44` · slice commits this round: F.2 `f1697bc` (Flow-D WER panel; dedicated eval-turn persist), F.3 `f7daae1` (Flow-E comparison; per-variant cost from wire `costEstimate`) · session doc `011` (impl `/session-end` `41eaf86`) · briefs 044–045 · web lessons §19–§21 + 3 `web/CLAUDE.md` index rows + 2 cross-doc rows (5 eval mirrors + `TranscribeParams`; `ComparisonTurn` projection) + 2 G.5 limitation notes · web 157→193 green · no safety pin (read-only/eval views) · next target: **Phase G**.

- **Round sealed 2026-05-31 (Phase-F COMPLETE — F.2 `EvaluationPanel` + F.3 `ComparisonSummary`; closes Phase F)** — round-seal: this `docs(tasks)` commit at HEAD · slice commits this round: F.2 `f1697bc` (Flow-D WER panel; dedicated eval-turn persist), F.3 `f7daae1` (Flow-E comparison; per-variant cost from wire `costEstimate`) · session doc `011` (impl `/session-end` `41eaf86`) · briefs 044–045 · web lessons §19–§21 + 3 `web/CLAUDE.md` index rows + 2 cross-doc rows (5 eval mirrors + `TranscribeParams`; `ComparisonTurn` projection) + 2 G.5 limitation notes · web 157→193 green · no safety pin (read-only/eval views) · next target: **Phase G**.

- **Round sealed 2026-05-30 (Phase-F F.1 — evaluation endpoints; backend→frontend area transition)** — round-seal: `4325c16` · slice commits: F.1a `7e29f35` (phrases + wer; ARCH-019 hypothesis-cap pin), F.1b `f9cf6a9` (transcribe STT-only; invariant-#5 upload-validation pin) · session doc `010` (impl `/session-end` `52456bb`) · brief 043 · server lessons §27–§28 + 2 `server/CLAUDE.md` rows + ARCH-009/016/018/019/028 + `.env.example` realization edits · backend 316→339 green · security-reviewer PASS both slices · next target: **F.2 (frontend `EvaluationPanel`)**.

- **Round sealed 2026-05-30 (Phase-E FRONTEND — E.3–E.5b; night wind-down at Phase-E-complete)** — round-seal: this `docs(tasks)` commit at HEAD · slice commits this round: E.3 `87d77d0`, E.4a `7b63fc4`, E.4b `f2b5647`, E.5a `a09c15e`, E.5b `f95bea6` (+ `style(web)` `5c14acb` prettier-drift fix) · session doc `009` (impl `/session-end` `66794d2`) · briefs 038–042 · web lessons §15–§18 + the `RealtimeToken` cross-doc row + 4 ARCH-010 §7 realization notes · E.6 verify-and-tick · web 97→157 green · next target: **Phase F (needs a backend impl)**.

- **Round sealed 2026-05-30 (Phase-E backend — wholesale backend→frontend cycle)** — round-seal: this `docs(tasks)` commit at HEAD · slice commits this round: E.1 `348ea76`, E.2b `a6bc540`, E.5-backend `f007a72` · session doc `008` (impl `/session-end` `233bfd2`) · briefs 035–037 · server lessons §24–§26 · next target: **E.3 (frontend WebRTC client)**.

**Backend-round detail (for the fresh team's context):**
- ✅ **E.1 — ephemeral client-secret broker SHIPPED `348ea76`** (SAFETY slice; invariants #1 standard-key-server-side-only + #2 ek_-never-persisted now LIVE + sentinel-pinned; 284→304 green; security-reviewer PASS; brief 035). Mints via the GA `client_secrets` endpoint; model resolve+allowlist (`RealtimeModelCatalog`) + missing-key short-circuit + one bounded 429 retry.
- ✅ **E.2 — turn-events ingest DONE (delivered by B.9c-ii, no new code).** `POST /api/sessions/{id}/turns/{turnId}/events` → `SessionsController.AppendEvents` already persists normalized `LatencyEvent`s **with `ClockSource`** (count-capped ≤500, backend-owned turnId via `POST …/turns` first; 404 on unknown). B.9c-ii's `AppendEventsRequest` docstring forward-named E.2/E.4 as its consumer. Normalization is the frontend's E.4 job; the backend persists generically. **No realtime-specific extension required.**

✅ **BACKEND PHASE-E COMPLETE (2026-05-30) — all 3 backend slices shipped; next = the frontend transition (E.3).**
1. ✅ **E.1 — ephemeral client-secret broker** `348ea76` (SAFETY; brief 035).
2. ✅ **E.2b — realtime turn cost** `a6bc540` (brief 036) — wired `CostEstimator.EstimateRealtime` into `CompleteTurnAsync`; degrades to null never a synthetic $0.00; fork (a) → optional `CompleteTurnRequest.OutputAudioDurationMs?` forward-built for E.4. Cross-doc written this round (ARCH-009 §6 + Appendix A + ARCH-014 note).
3. ✅ **E.5-backend — stale-session flush (Flow H)** (brief 037) — on-`Create` auto-end+persist of all un-ended priors via `CreateAsync` (reuses `EndAsync`; owed write uses `CancellationToken.None`; degrade+log on persist-fail). ARCH-017 realization note written this round. _(slice hash pending the impl's done-with-slice report; rides this round.)_

**TRANSITION (lead-directed):** backend impl `/session-end` → orchestrator `/orchestrate-end` (round-seal — commits the accumulated E.1/E.2b/E.5-backend doc routing + this whole Phase-E backend round) → lead cycles the backend impl + **spawns a `web/` frontend impl for E.3** (browser WebRTC client). **E.2 ✅ done by B.9c-ii (tick only).** Realtime cost output-seconds: E.4 must report `outputAudioDurationMs` (carry-forward). E.5 frontend WebRTC lifecycle remains in E.5.

**HANDOFF POINT:** after E.2b + E.5-backend ship → **area transition**: lead cycles the backend impl + spawns a **frontend impl for E.3** (browser WebRTC client). E.3 doesn't depend on the cost wiring, so the frontend impl *can* be spawned in parallel.
**Already-clear (no backend work):** E.5 Flow-G (mode-transition) = `SessionStore.RecordModeTransition` (built); E.6 backend half (realtime model resolve+allowlist) = done by E.1.

- **Round sealed 2026-05-30 (D.6 + D.7 — closes Phase D; wholesale cycle)** — round-seal: this `docs(tasks)` commit at HEAD · slice commits this round: D.6 `ce451aa`+`2d62cca`, D.7 `01727f3` · session doc: `007` (D.6+D.7, impl `/session-end`) · briefs 033–034 · next-phase target: **E.1**.

**Last landed (Phase D, across two rounds):** the entire frontend — D.1 foundation (types/store/clients/shell + Vite proxy) · D.2 setup + config-gating selectors + DI'd session actions · D.3 capture (linear16 streaming + blob fallback) · D.4a cascade WS client + streaming store actions · D.4b recording controls + orchestration · D.5 playback (MSE + blob) · **D.6 live transcript/metrics/cost panels + the three-source metrics model** (per-stage from store; top-level client-timing deltas frontend-computed; session-avgs from `GET /summary`) · **D.7 ErrorBanner + actionable `errorCopy` + the jsdom/Testing-Library env + the 2 ARCH-020 component tests**. **The cascade runs end-to-end in the browser** (setup → record → live transcripts/per-stage-latency/estimated-cost → audible TTS playback → sanitized error surface). **Web suite 0→97 green**; security invariants #1–#5 PASS every slice; every code-quality HIGH/MEDIUM fixed in-slice. **Web lessons §1–§14 banked.** Backend suite unchanged at 284. Push deferred (no remote).

> **⚠️ OPERATIONAL (next orchestrator, READ THIS):** the **RTK git hook silently swallows `git commit`** (returns "[RTK:PASSTHROUGH] … ok" but HEAD does NOT move; phantom hashes resulted). **Always `command git commit -F <file>` + VERIFY `command git rev-parse --short HEAD` changed; ONE Bash call per message (bundling cascade-cancels under the `claude-opus-4-8`-classifier hiccup); avoid `git … | cat` (use `git --no-pager`).** Bash auto-mode is OFF (user approves manually — what lets commits land). Memory `rtk-git-commit-swallow`. Also: never fire a HARD-STOP/cycle on a **stale** `/context-check` heartbeat — re-read fresh first (memory `stale-heartbeat-cycle-guard`).
>
> **PHASE-E ENTRY (for the fresh team):** E is Realtime (ARCH-010 — browser WebRTC + backend-minted ephemeral `ek_…` credential; **safety invariants #1/#2 live**). Frontend carry-forwards that activate at E: realtime turn cost+errors (E.4/E.5); the `'ending'`-gate (E.5); capture-reuse — the controller's `onFrame` is cascade-shaped, realtime reuses raw `getUserMedia`/`pc.addTrack` (E.1/E.2). The first real-key cascade smoke (the C/D demo-checklist) is also outstanding (confirm cascade-live-metrics + the OpenAI Responses `usage` nesting).

**Phase D entry — D.1 (app shell, API clients, state store) prereqs (lead-noted):**
- **Re-sequenced into D.1:** the **TS mirror types** (deferred from A.3) + the **Vite dev-config** (dev proxy / `VITE_API_BASE_URL` reaching the backend API + the cascade WS) — D.1 stands these up.
- **The frontend consumes the documented backend API surface** — `ARCH-009` (session/turn/config/cascade-WS/blob/realtime-token/evaluation contracts) + `ARCHITECTURE.md` Appendix A (the DTO inventory, incl. the C.4a WS DTOs + the C.5 `CascadeTurnResponse`). The `web/src/types/` mirror types are the executable enforcement of those contracts (ARCH-005 / ARCH-007).
- **Clean-separation invariant (ARCH-007):** UI components render only from `sessionStore`/`UiSessionState`; the transports (cascade WS client, realtime client) own all wire detail.

**Open backend items carried into later phases** (full detail in session doc `005` + Carry-forward + lessons §22–§23):
- **F.1 (security MEDIUM, Phase F):** cap `POST /api/evaluation/wer` hypothesis length (~2000 chars → `400`) **before** `WerCalculator.Compute` (DP-matrix DoS); never surface `LoadError`.
- **C.6/C.4b LOW residuals (remain open at Phase-C exit, documented):** C.6 live-WS unbounded `Channel<SttEvent>` + Connect/Stop fast-cancel race; C.4b `StartFrameDto.Type=="start"` validation + the TOCTOU not-found live-view read (single-node MVP). All bounded; G.4-adjacent.
- **Tracked enhancement:** FORK-1a `SttFinal.Duration` → price the blob STT stage (cross-doc `SttFinal` change; deferred as fallback-cost gold-plating — see ARCH-014 + C.5 residuals).
- **Real-key smoke** (both cascade paths + the OpenAI Responses `usage`-nesting confirm) = the **C/D demo-checklist** (non-blocking, ARCH-020 exempt).
- See **Carry-forward** for the next-brief working set.

---

## Carry-forward to upcoming briefs

_Entries carry an origin marker `(origin: YYYY-MM-DD <slice-id>)`. Items that belong to a specific phase are inlined as task checkboxes there (not held here); only next-1–2-slice items live here._

_(Triaged at the 2026-05-31 **real-key-smoke bug-fix arc close** (crash-recovery round-seal by the fresh orchestrator): **0 deleted, 0 deferred, 0 inlined, 1 updated, 0 added, rest KEEP.** The arc's Step-9 items routed to lessons (server §30/§31, web §22/§23) + cross-doc (the 051 model-name invariant; the 050-frontend `SetModeRequest`/`ModeTransitionEvent` row) + ARCH-011/013 realization notes — **none to Carry-forward**. **UPDATED (1):** the `ModeTransitionEvent` emit+persistence item — 050-frontend (`7dc398e`) shipped the frontend POST + the `SetModeRequest`/`ModeTransitionEvent` TS types (registered; `modeTransitions` still opaque), so its remaining consumer is **050-backend** (the endpoint + `RecordModeTransition` wiring) then **H.3** (graduate `modeTransitions`). **KEEP:** the MetricsPanel-framing-polish item (still pending its post-052/050/053 metrics-polish slice — verify per-stage-`n/a`-on-success post-052). Rest SPREAD/KEEP (G / G.4 / demo-checklist markers). Next: 053 gaps A/C → 050-backend → H.3.)_

_(Triaged at the 2026-05-31 **F.4-close** `/orchestrate-end`: **0 deleted, 0 deferred, 0 inlined, 1 updated, 0 added, rest KEEP.** F.4 (`1e8a655`) was a focused backend slice — it CLOSED the eval-turn-`turnCount` skew (the success path); **UPDATED (1):** the orphaned-eval-turn item now reads as the confirmed failure-path residual (F.4 landed). No new carry-forward. Phase F is now FULLY complete → next: Phase G. Prior F.2+F.3 triage below stands.)_

_(Triaged at the 2026-05-31 **Phase-F-COMPLETE (F.2+F.3) close** `/orchestrate-end`: **4 deleted, 0 deferred; 0 inlined, 2 marker-updated, 1 added, rest SPREAD/KEEP (~17).** **DELETED (4 — discharged this round):** the 3 F.2/F.3 working-set items — F.1-endpoints-consumed-by-F.2 + TS mirrors (shipped F.2 `f1697bc`), the WER-persist-`turnId` round-trip (shipped F.2), the "WER is STT-only" panel copy (shipped F.2); AND the cascade `playback.started`-not-persisted item — **RESOLVED** by F.3's Q5 decision (accept `n/a` + document; now a G.5 limitations line, not an open question). **MARKER UPDATED (2):** `outputAudioDurationMs` (F.3→**G**) + `cached-audio-input seconds` (Phase-F→**G**) — F.3 is a read-only comparison view; it surfaces persisted cost but did NOT add the realtime-output-duration measurement / cached-input sourcing (both remain a realtime-cost-accuracy gap → a G pass / demo-checklist). **ADDED (1):** the orphaned-eval-turn cleanup (F.2 — a `computeWer` failure after `createTurn` leaves a turn with no `WerResult`; → G). **SPREAD/KEEP:** the rest carry valid markers (G / G.4 / demo-checklist / a backend `ModeTransitionEvent` slice) — Phase-G-distributed hardening + the standing real-key smokes; the next team triages them when authoring G. **Phase F COMPLETE → next: Phase G** (docs/demo/stability/write-up). Prior Phase-F-F.1 triage superseded.)_

- **MetricsPanel framing polish (origin: 2026-05-31 real-key smoke → a post-active-fix frontend metrics-polish slice — LOW/demo-quality) — TWO findings, bundle as one MetricsPanel slice:** (1) **the latency headline puts the `< target` badge on `totalTurnMs`**, but total-turn INCLUDES the user's talk time → a long utterance is always "over"; the target (cascade <3s / realtime <1.5s) belongs on the **responsiveness** metric (speech-end→first-audio / speech-end→playback), with total-turn shown as secondary context (no badge). **Confirm ARCH-013's exact "end-to-end <3s" definition** (speech-end→playback vs recording-start→playback) when authoring. (2) **per-stage metrics show `n/a` on a FAILED turn** even though stage events fired — likely just failed-status suppressing the display; **verify it populates on a SUCCEEDED turn post-052**; if it still shows n/a on success, a small MetricsPanel guard. _(last-consumer-slice: a post-052/050/053 frontend metrics-polish slice.)_
- **Stronger model validation — allowlist (origin: 2026-05-28 B.9c-i, security follow-up) — partially CLIENT-MITIGATED at D.2** — `POST /api/sessions` length-caps `RealtimeModel`/`TranslationModel` but does not **allowlist** them against the published catalog (`ConfigService`). **D.2 client-mitigation:** the model `<select>`s offer ONLY catalog models (`availableModels` reads the `ConfigResponse` catalog), so the frontend never *presents* an off-catalog choice. **Realtime-model half DISCHARGED at E.1 (2026-05-29):** the `POST /api/realtime/client-secret` mint boundary now allow-lists the resolved realtime model against `RealtimeModelCatalog` (`{gpt-realtime, gpt-realtime-mini}`), failing closed (→ 400 `realtime.invalid_request`) **before** the upstream OpenAI call. **Still open:** (a) a runtime membership guard on the D.2 `<select>` onChange `as RealtimeModel/TranslationModel` cast (defense-in-depth — a crafted DOM/event could still set an off-catalog value); (b) the **server-side allowlist of the TRANSLATION model at the `POST /api/sessions` create boundary** (reuse the ConfigService catalogs) — the authoritative guard, still unmitigated server-side. _(last-consumer-slice: a G hardening pass; bounded — backend length-caps + client selectors + the E.1 realtime mint-boundary allowlist constrain today.)_
- **`RealtimeModelCatalog` ↔ `ConfigService` realtime-model unify (origin: 2026-05-29 E.1, → G / opportunistic — LOW)** — E.1 added an area-owned `RealtimeModelCatalog` (`{gpt-realtime, gpt-realtime-mini}`) for the mint-boundary allowlist; `ConfigService` keeps its own private realtime-model list. Two sources of one truth. A future pass should unify to a single source — deliberately NOT refactored inside the E.1 SAFETY slice (keep its blast radius minimal). _(last-consumer-slice: a G cleanup / opportunistic — fold into any slice already touching `ConfigService`.)_
- **No auth/authz gate on `POST /api/realtime/client-secret` (origin: 2026-05-29 E.1, sec LOW → G hardening)** — any reachable client can mint an `ek_` (consuming standard-key quota + getting a short-lived credential). This **reaffirms the documented single-trusted-user Non-Goal** (ARCH-002 — no auth/accounts; ARCH-019 trust boundary), NOT a slice regression — the whole app has no auth middleware anywhere. Tracked here so it's not silently dropped; if the deployment posture ever changes (ARCH-022 optional AWS), this mint endpoint is the first that needs a gate. _(last-consumer-slice: G hardening / a deployment-hardening pass.)_
- **Frontend reports `outputAudioDurationMs` at `/complete` (origin: 2026-05-30 E.2b; marker re-pointed E.4→F.3→**G** across closes — LOAD-BEARING for realtime cost accuracy)** — E.2b forward-built the optional `CompleteTurnRequest.OutputAudioDurationMs?` (backend ready; the TS mirror gains the field). **E.4 SHIPPED priced input-only; F.3 (the read-only comparison view) did NOT add the measurement** → the frontend must still measure the played realtime output-audio duration and report it; until then realtime turns price **input-only** with an honest "output not reported" disclosure in the cost Assumptions (never silently 0). Without it, the realtime-vs-cascade cost comparison undercounts realtime output (a **deliverable-skewing gap** — visible in F.3's comparison). _(last-consumer-slice: a G realtime-cost-accuracy pass / the demo-checklist — measure played `<audio>`/track output duration in the realtime turn controller, report at `/complete`.)_
- **Cached-audio-input seconds not sourced at `/complete` (origin: 2026-05-30 E.2b; Phase-F→**G** — LOW)** — `EstimateRealtime` already prices `CachedAudioInputSeconds` if supplied, but `/complete` doesn't source realtime prompt-caching seconds, so cached input is billed at the full input rate (disclosed). A later accuracy refinement if the realtime cache hit-rate matters to the comparison. _(last-consumer-slice: a G cost-accuracy pass / opportunistic — pairs with the `outputAudioDurationMs` item above.)_
- **TTL/timer stale-session sweep (origin: 2026-05-30 E.5-backend, → G hardening — LOW)** — E.5-backend flushes abandoned sessions **on the next `Create`** (single-user demo path). A TTL/timer-based sweep would cover true multi-session / long-idle abandonment (a session left open with no subsequent `Create` never flushes until process exit). Deferred — on-create flush is sufficient for the single-trusted-user MVP. _(last-consumer-slice: a G hardening / multi-user pass.)_
- **SessionSetup `active`-gate omits `'ending'` (origin: 2026-05-29 D.2, forward-compat — LOW)** — D.2's `SessionSetup` treats only `'active'` (not `'ending'`) as the in-session render state; harmless today (D.2's `endSession` goes `active → ended` directly; nothing sets `'ending'`). Becomes relevant when a slice introduces an in-flight `'ending'` status (graceful end with a server round-trip). _(last-consumer-slice: E.5 / a polish pass — whoever first sets `sessionStatus:'ending'` extends the gate.)_
- **Blob-playback-fallback completeness (origin: 2026-05-29 D.5, → G — LOW)** — D.5's non-MSE blob fallback is best-effort (a static `HTMLAudioElement` `src` can't progressively play a live stream; a `done`-driven `flush()` would complete it). MSE/Chrome is the demo path. _(last-consumer-slice: G-hardening, only if non-MSE browsers matter.)_
- **`completeTurn` failed→completed overwrite on a contradictory frame sequence (origin: 2026-05-29 D.6, → G — LOW, backend-contract-protected)** — `completeTurn` would overwrite a prior `failed` status if a `done{completed}` arrived AFTER an `error` frame. Pre-existing D.4a behavior; **protected by the C.4b backend contract** (on failure the WS sends `done{failed}`, and a server `error` frame is terminal — so the contradictory sequence shouldn't occur), and an existing test pins the current behavior. Defensive hardening only. _(last-consumer-slice: G hardening.)_
- **Frontend store unbounded growth — `latencyEvents[]` per-turn + `turns[]` per-session + realtime sink accumulators (origin: 2026-05-29 D.6, +2026-05-30 E.4a, → G.4 — LOW)** — D.6 retains the raw `latencyEvents[]` per turn + `turns[]` accumulates per session; both grow unbounded over a long session. **E.4a adds** the realtime sink's per-turn `sourceText`/`targetText` accumulators (unbounded within a turn; security-reviewer deferred-MEDIUM). Single-trusted-user (ARCH-019); realistic bound is the turn/session lifecycle; mirrors the backend's own bounded-channel deferral. A per-turn `MAX_TRANSCRIPT_CHARS` guard folds in at hardening. _(last-consumer-slice: G.4 — the 5-min no-leak preflight.)_
- **Per-item request-string caps (origin: 2026-05-29 B.9c-ii, security MEDIUM — bounded; partially addressed C.4b/C.5)** — the **cascade WS + blob boundaries now cap every client-supplied string at 256** (C.4b `CascadeStartValidation`, C.5 `CascadeController` — §16 at those boundaries). **Still open:** the B.9 HTTP-DTO per-*item* string lengths (`LatencyEvent.Name`/`Metadata`, `TranscriptSegment.Text` on append-events/complete) — `[MaxLength]` caps collection *counts* (≤500) but not item lengths; bounded today by Kestrel's 30MB + the count caps; full caps need a design call (request DTOs vs domain-record attrs — prefer request DTOs). Low impact (single-trusted-user, ARCH-019). _(last-consumer-slice: a hardening pass / G.)_
- **Confirm OpenAI Responses `usage` nesting on first real-key smoke (origin: 2026-05-29 C.2)** — C.2 reads usage at `response.usage.{input_tokens,output_tokens}` (snake_case), research-verified + fixture/impl agree, but NOT yet exercised against a live key. On the first real-key smoke (C.4/D), confirm; if OpenAI nests differently, `OpenAiTranslationMapping.ReadUsage` + the fixture adjust together (cost depends on it, ARCH-014). _(last-consumer-slice: D — the frontend brings the first real-key cascade smoke.)_
- **Dead `Stream` field on both OpenAi options (origin: 2026-05-29 C.2/C.3, cleanup)** — `OpenAiTranslationOptions.Stream` + `OpenAiTtsOptions.Stream` are never consulted (both providers ALWAYS stream). Harmless dead config. Cleanup: remove from both Options + the Appendix A/ARCH-012/cross-doc rows (a small **cross-doc** change, own atomic slice) OR comment them "ignored (provider always streams)". Not worth a dedicated slice; do at a G cleanup pass or fold the comment-only version into any slice already touching the Options. _(last-consumer-slice: G cleanup / opportunistic.)_
- **`CascadeTurnForm` `[Required]` on Source/Target (origin: 2026-05-29 C.5, D-adjacent) — CLIENT-SIDE MITIGATED at D.1; server guard still open** — the blob endpoint's multipart `CascadeTurnForm` lacks `[Required]` on the `source`/`target` direction fields, so a missing direction silently binds `En→En`. **D.1 mitigated the client side:** `cascadeApi.postCascadeTurn` ALWAYS sends `source`+`target` (pinned by a test). **Still open server-side:** the endpoint should `[Required]`-guard `source`/`target` for defense-in-depth (a non-frontend / malformed POST can still omit them). _(last-consumer-slice: a D form-validation slice or a G hardening pass — server-side guard only; the frontend obligation is discharged.)_
- **Realtime GA wire-shape smoke-confirm (origin: 2026-05-30 E.3/E.4a/E.4b/E.5a, → first real-key realtime smoke / ARCH-020 demo-checklist)** — the realtime path reads specific GA shapes, pinned by tests but NOT yet exercised against a live key. Confirm at first smoke + re-pin **ARCH-010 §7** (the §18/§20 verify-the-real-shape discipline): **(A)** an `error`/`response.error` event's nested `error.code` envelope (+ whether `response.error` is a real GA type); **(B)** `…input_audio_transcription.completed` reads `transcript` while `.delta` reads `delta`, + the exact `type` strings (`response.output_audio.delta` vs legacy `response.audio.delta`; `response.output_audio_transcript.delta`); **(C)** the manual-turn-control frame envelopes (`{type:'session.update', session:{audio:{input:{turn_detection:null}}}}`, `input_audio_buffer.clear`/`commit`, `response.create`); **(D)** whether WebRTC emits `output_audio.delta` on the DC at all (else `playback.started` is the only first-audio timing) + the pc `connectionState` event names (`connected`/`failed`/`disconnected`). Classification/behavior is stable regardless. _(last-consumer-slice: E.5/E.6 live smoke — the realtime demo-checklist.)_
- **Realtime `responseDone`+`error` event-ordering race (origin: 2026-05-30 E.4a, → E.5 — LOW, latent/no caller)** — if `responseDone` (→`completeTurn`, `currentTurn` cleared → `turns[]`) precedes a stray `error` (→`failTurn` on no `currentTurn`), the turn lands `completed` in `turns[]` while the session also errors + `turnStatus:'failed'` — an inconsistent settled state. Needs the GA data-channel **event-ordering model** (which terminal wins, terminal-after-terminal guard) → E.5 (connection lifecycle / event ordering, alongside the §11 idempotent-terminal pattern). No production caller today (the sink isn't wired until E.4b), so latent. _(last-consumer-slice: E.5 — connection lifecycle + event ordering.)_
- **Frontend received-id format pre-validation (origin: 2026-05-30 E.4a, sec LOW → G hardening)** — `appendTurnEvents` (and other clients) interpolate backend-supplied session/turn ids into the URL via `encodeURIComponent`; a `^[A-Za-z0-9_-]+$` pre-validation before interpolation would be defense-in-depth (the backend id contract + `encodeURIComponent` cover it today — the ids are server-generated, not user-supplied). A convention candidate for a frontend client-hardening pass. _(last-consumer-slice: G hardening / opportunistic.)_
- **Realtime `onServerEvent` write-only immutability (origin: 2026-05-30 E.4b, sec LOW → E.5/G)** — the E.3 client's settable raw `onServerEvent` delegate is written by the controller per turn; a hardening pass should ensure it's write-only-by-the-controller (no external reassignment mid-turn) + cleared on teardown. Bounded today (single controller owns it). _(last-consumer-slice: G hardening.)_
- **⚠️ BACKEND-DEPENDENT — `ModeTransitionEvent` emit + persistence (Flow-G timeline) (origin: 2026-05-30 E.5b; frontend half DONE at 050-frontend `7dc398e` → remaining: 050-backend, then H.3)** — **050-frontend (`7dc398e`) shipped** the frontend `POST /api/sessions/{id}/mode` (`sessionsApi.setMode` on mode-switch) + the **TS `SetModeRequest` + `ModeTransitionEvent` mirrors** (`{transitionId, fromMode, toMode, directionAtTransition, occurredAt, clockSource, triggeredByTurnId?}` — mirror of `SessionModels.cs`). **Still UNWIRED server-side:** the backend `SessionStore.RecordModeTransition(sessionId, ModeTransitionEvent)` is built but no endpoint calls it → the frontend POST **404s live**. **050-backend (pre-approved, brief 050, Finding-2c Option B)** lands the `POST /sessions/{id}/mode` endpoint → atomic re-entrant `SwitchMode` → `RecordModeTransition` + `CurrentMode` swap (+ `ErrorSanitizer.SafeMessageForCode` gains `session.invalid_mode`); then **H.3** graduates the store `modeTransitions` (opaque→typed) to consume the timeline. **NOT deliverable-blocking** — the comparison write-up derives each turn's mode from the per-turn `mode` field; the transition *timeline* is a session-level nice-to-have. A backend implementer is in the team this cycle (the arc is cross-area). _(last-consumer-slice: 050-backend + H.3.)_
- **Orphaned-eval-turn cleanup (origin: 2026-05-30 F.2; F.4 `1e8a655` closed the success path — this is the failure-path residual → G hardening, LOW/bounded)** — F.2 persists WER by creating a dedicated eval turn then `POST /wer`; a `computeWer` failure AFTER `createTurn` succeeds leaves a **valid turn with no `WerResult` attached** and (since **F.4** sets `IsEvaluation` at `/wer`) **no `IsEvaluation` marker either**, so it would still be counted in a mode's `turnCount`. Bounded for the single-trusted-user demo; documented in-code. **F.4 excludes the SUCCESS-path eval turns (the common case); this rare failure-path orphan is the documented residual.** A backend turn cancel/cleanup endpoint (or a frontend retry of `computeWer` against the same turn) would close it. _(last-consumer-slice: a G hardening / a backend eval-turn pass.)_

_(Phase-A close — earlier items closed or inlined to their phase. **Closed:** WAE-scope (decided A.5 — Api-only WAE, no `Directory.Build.props`); A.1 template reconciliation (done A.5 — port 5179, template profiles dropped, Swagger Dev-only). **Inlined to B.9:** collection-size bounding + `Result`→DTO mapping (origin A.3) + global sanitizing exception handler (origin A.5, safety). **Inlined to C.4:** cascade WS `Origin` validation (origin A.5).)_

---

## Deliverable map

| Deliverable (PRD / ARCH) | Status | Delivered by |
|---|---|---|
| Browser SPA + mic capture + playback (PRD 1; ARCH-007, ARCH-030) | 🟡 | Phase D (code done; mic/playback runtime = demo-checklist) |
| Realtime mode — voice in/out via `gpt-realtime` (PRD 2; ARCH-010) | 🟡 | Phase E (code end-to-end: reachable/audible/disconnect-resilient/mode-switch-clean/model-selectable; live real-key smoke = demo-checklist) |
| Cascade mode — streaming STT→Translation→TTS (PRD 3; ARCH-011) | 🟡 | Phases B, C, D (code end-to-end; live real-key smoke = demo-checklist) |
| Mode toggle, mid-session or pre-session (PRD 4; ARCH-007, ARCH-017) | 🟡 | Phases D, E (D: ModeToggle + config-gating; E: realtime side) |
| Language pair EN↔ES (PRD 5; ARCH-002) | 🟡 | Phases A, D (wired both sides; runtime smoke pending) |
| Live source + target transcripts as produced (PRD 6; ARCH-007, ARCH-011) | 🟡 | Phases D, E (D: cascade live; E: realtime) |
| Per-stage latency visible (PRD 7; ARCH-013) | 🟡 | Phases B, D (B backend + D frontend; E adds realtime) |
| Provider abstractions + fakes (PRD; ARCH-012) | ✅ | Phase B |
| Streaming throughout, no full-utterance blocking (PRD; ARCH-011) | 🟡 | Phases C, D (code done; live smoke pending) |
| Error handling: rate limits/timeouts/empty/mic-denied (PRD; ARCH-018) | ❌ | Phases B, C, D |
| Targeted tests on cascade + provider boundaries (PRD; ARCH-020) | ❌ | Phases B, C |
| Local JSON persistence, no raw audio/secrets (ARCH-016, ARCH-019) | ❌ | Phase B |
| Cost estimate/min (PRD impact metric; ARCH-014) | ❌ | Phases B, D, F |
| WER Evaluation panel (committed MUST; ARCH-015) | ❌ | Phase F |
| Comparison summary view (ARCH-009 summary; ARCH-007) | ❌ | Phase F |
| README + CLAUDE.md/AGENTS.md (PRD; ARCH-023) | ❌ | Phase G |
| Comparison write-up 1–2 pp (PRD 8; ARCH-023) | ❌ | Phase G |
| 5-min stability: no disconnect/drift/leak (PRD; ARCH-020) | ❌ | Phase G |
| Meaningful scoped git history (PRD; ARCH-023) | 🟡 | All phases |

---

## Phase exit checklist (applies to every phase)

- [ ] All phase task checkboxes ticked (partial work stays unchecked with a Log note).
- [ ] Acceptance criterion met (`/preflight` clean + manual smoke where there's runtime behavior).
- [ ] All phase tests green; no model field changed without the matching `ARCHITECTURE.md` edit in the same commit round (Appendix A invariants).
- [ ] Session doc(s) list every file created/modified.
- [ ] Commits scoped + pushed.

---

## Final-submission acceptance criteria (project-level)

The project is "done" (ARCH-025 + PRD success criteria) when:

- [ ] Local app runs from a clean clone via README steps (ARCH-021, ARCH-029).
- [ ] **Realtime** completes EN→ES and ES→EN turns: voice in → translated voice out; source + target transcripts shown; **speech-end → first audio < 1.5s** observed in the demo (ARCH-010, ARCH-013).
- [ ] **Cascade** completes turns with **live streaming** — source partials render as produced, target tokens + TTS audio stream in before `tts.complete`; **end-to-end < 3s (target < 2s)** observed (ARCH-011, ARCH-013).
- [ ] Mode toggle switches modes between turns within one session (ARCH-007, ARCH-017 Flow G).
- [ ] Per-stage cascade latency + top-level latency display for both modes (ARCH-013).
- [ ] Cost/min estimate displays (labeled estimate) and the summary compares modes **and** model variants (both Realtime models; both translation models) (ARCH-014, ARCH-009 summary).
- [ ] WER Evaluation panel returns a score for a scripted phrase (ARCH-015).
- [ ] Session JSON persists with **no raw audio and no secrets** (sentinel test passes) (ARCH-016, ARCH-019).
- [ ] Backend tests green for cascade orchestration, provider boundaries, WER, cost, persistence, metrics, error sanitization, config (ARCH-020).
- [ ] 5-minute back-and-forth run: no disconnect, no audio drift/overlap, no memory leak (ARCH-020 preflight).
- [ ] Clean separation verified: mode-agnostic UI renders only from `sessionStore`/`UiSessionState`; mode-specific transports own all wire detail (PRD code-quality; ARCH-007).
- [ ] README + CLAUDE.md/AGENTS.md complete; **1–2 pp comparison write-up covers latency, quality (WER), cost, controllability, AND an evidence-backed recommendation for when each mode fits** (PRD must-have 8; ARCH-023).
- [ ] Git history is scoped per logical unit (no single "initial commit" dump) (ARCH-023).

---

## Phase A — Repo, Config & Domain Models

**Goal:** Stand up the .NET solution + Vite app skeleton, configuration/secrets, the pricing config, and the complete domain model so every later phase has a typed contract to build against.

**Spec anchors:** `ARCH-006`, `ARCH-028`, `ARCH-005`, `ARCH-007`, `ARCH-012`, `ARCH-014`, `ARCH-018`, `ARCH-029`, `ARCH-019`.

### A.1 — Solution + repo scaffold
- [x] `dotnet` solution with `AiInterpreter.Api` (ASP.NET Core Web API, net8.0) + `AiInterpreter.Tests` (xUnit) projects; Vite React-TS app under `web/`.
- [x] Directory layout matches `ARCH-006` exactly (Controllers/, Realtime/, Cascade/, Providers/{Abstractions,Deepgram,OpenAI,Fakes}/, Sessions/, Metrics/, Cost/, Evaluation/, Config/, Security/, Common/; web/src/{api,audio,realtime,cascade,state,components,types}/).
- [x] `.gitignore` excludes `.env`, `.env.local`, `server/**/bin/`, `server/**/obj/`, `web/node_modules/`, `data/sessions/*.json`; keep `data/sessions/.gitkeep`.
- [x] `config/pricing.json` and `docs/` (COMPARISON_WRITEUP.md, DEMO_SCRIPT.md placeholders) created.
- [x] Files: NEW — solution files, both csproj, `web/package.json`, `web/vite.config.ts`, `.gitignore`, `data/sessions/.gitkeep`, `config/pricing.json`.
- [x] Anchors: `ARCH-006`. Cross-doc invariant: none.

### A.2 — Configuration, secrets & Options classes
- [x] `.env.example` lists every variable from `ARCH-028` with comments, no real keys.
- [x] `Options` classes bound via `IOptions`: `RealtimeOptions`, `DeepgramOptions`, `OpenAiTranslationOptions`, `OpenAiTtsOptions`, `PricingOptions` — fields per `ARCH-012` "Provider Options (enumerated)".
- [x] Standard API keys load **server-side only**; nothing in this layer is exposed to the SPA.
- [x] Files: NEW — `Realtime/RealtimeOptions.cs`, `Providers/Deepgram/DeepgramOptions.cs`, `Providers/OpenAI/OpenAiOptions.cs` (translation + tts), `Cost/PricingOptions.cs`, `.env.example`. (`appsettings*.json` deliberately **unchanged** — inline defaults are the single source of truth; binding wired in A.5.)
- [x] Anchors: `ARCH-028`, `ARCH-012`, `ARCH-019`. Cross-doc invariant: extended (Options mirror env contract) — rows in Appendix A + `server/CLAUDE.md`.

### A.3 — Domain models (enums + records)
- [x] All enums + records from `ARCH-005` implemented exactly (`InterpretationMode`, `LanguageCode`, `TurnStatus`, `SessionStatus`, `LatencyStage`, `ClockSource`; `LanguageDirection`, `ProviderProfile`, `SessionConfig`, `InterpretationSession`, `ModeTransitionEvent`, `InterpretationTurn`, `TranscriptSegment`, `LatencyEvent`, `CostEstimate`, `SessionSummary`, `ModeSummary`, `WerSummary`, `EvaluationPhrase`, `WerResult`, `ProviderError`). _(verified zero drift vs ARCH-005/Appendix A; impl + code-quality reviewer diffed.)_
- [x] JSON serialization is **camelCase** (System.Text.Json options) so API/persisted JSON matches `ARCH-009`/`ARCH-016` examples. _(shared `Common/JsonDefaults`: camelCase + enum-as-camelCase-string + explicit-null; ISO-8601 `+00:00` ≡ `Z` — see ARCH-009 timestamp annotation.)_
- [x] `Common/Clock.cs` (`IClock` abstraction returning `DateTimeOffset` — injectable for deterministic tests) + `Common/Result.cs`.
- [ ] ~~Mirror the TS domain types in `web/src/types/domain.ts`~~ — **RE-SEQUENCED to Phase D** (D.1 typed-contract prereq; `web/` area + frontend build phase). See brief 003.
- [x] Files: NEW (backend) — `Sessions/SessionModels.cs`, `Providers/Abstractions/ProviderErrors.cs`, `Common/Clock.cs`, `Common/Result.cs`, `Common/JsonDefaults.cs`, + tests. _(`web/src/types/{domain,metrics}.ts` re-sequenced to Phase D.)_
- [x] Anchors: `ARCH-005`, `ARCH-007`. Cross-doc invariant: **NEW** — registered (domain-model row in `server/CLAUDE.md` cross-doc table; full inventory in Appendix A).

### A.4 — Pricing config + binding
- [x] `config/pricing.json` populated with the starting block from `ARCH-014` (Deepgram streaming `$0.0058`/min; both Realtime models; both translation models incl. `gpt-5.4-mini` marked CONFIRM-at-build; TTS bases); `version: "2026-05-28-payg-estimates"`; disclaimer present.
- [x] `PricingOptions` binds it via `PRICING_CONFIG_PATH`; missing config degrades to "estimate unavailable" (ARCH-018), never a crash. _(`Cost/PricingLoader` → `Result<PricingOptions>`; `File.Exists` + 1MB size guard + filtered catch; DI wiring of the path is A.5.)_
- [x] Files: extended — `config/pricing.json`, `Cost/PricingOptions.cs` (full shape, `SectionName` removed); NEW — `Cost/PricingLoader.cs`, `AiInterpreter.Tests/PricingConfigTests.cs`.
- [x] Anchors: `ARCH-014`, `ARCH-018`. Cross-doc invariant: extended (`PricingOptions` minimal→full; file-loaded).

### A.5 — Backend host, CORS, health + build/run wiring
- [x] `Program.cs` wires DI (Options + flat-env→section bridge + pricing-loader singleton + `JsonDefaults.Apply`), camelCase JSON, **CORS restricted to the local frontend origin** (`localhost:5173`, `WithOrigins` — never `AllowAnyOrigin`), WebSocket support enabled, listen port `5179`. Swagger Development-only.
- [x] `GET /api/health` → `{ "status": "ok" }` (minimal-API).
- [ ] ~~`web` dev server config (`VITE_API_BASE_URL` + Vite proxy)~~ — **RE-SEQUENCED to D.1** (frontend area/phase; D.1 builds the API clients + needs the proxy then). Secure-context note → README backlog (G.1). A.5's CORS already allows `localhost:5173`.
- [x] Files: extended — `Program.cs`, `Properties/launchSettings.json` (port 5179; template profiles dropped), `AiInterpreter.Tests.csproj`; NEW — `AiInterpreter.Tests/HostIntegrationTests.cs` (6, WebApplicationFactory). _(`/api/health` is minimal-API; `ConfigController` `/api/config` is B.9; `web/vite.config.ts` + `web/.env.example` → D.1.)_
- [x] Anchors: `ARCH-029`, `ARCH-019`. Cross-doc invariant: none.

### Acceptance criteria (A)
- [x] All A.X ticked (A.5's web Vite-config sub-item re-sequenced to D.1). `dotnet build` 0W/0E ✓; `npm run build` green (A.1; web unchanged since). Backend C# domain types compile ✓ (TS mirror re-sequenced to D, per A.3). `GET /api/health` returns ok ✓ (real host on 5179). No secrets in any committed file ✓ (security-confirmed). **Phase A COMPLETE.**

---

## Phase B — Backend Core Seams + Tests (no real providers)

**Goal:** Build and TDD every deterministic backend seam against **fake providers** — provider abstractions, the streaming cascade orchestrator, metrics, cost, WER, persistence, error sanitization, and session/config HTTP endpoints — so correctness is pinned before any paid API exists.

**Spec anchors:** `ARCH-012`, `ARCH-011`, `ARCH-013`, `ARCH-014`, `ARCH-015`, `ARCH-016`, `ARCH-008`, `ARCH-009`, `ARCH-018`, `ARCH-019`, `ARCH-020`.

### B.1 — Provider interfaces + event types
- [x] `ISttProvider`, `ITranslationProvider`, `ITtsProvider` and their event hierarchies + request records + `AudioFrame` exactly per `ARCH-012` (IAsyncEnumerable streaming contracts; `CancellationToken` on each). _(verified exact-match vs ARCH-012 §9; impl + code-quality reviewer diffed.)_
- [x] `ProviderError` mapping helpers (exception/HTTP-status → code/retryable) per the `ARCH-012` mapping table (logic only; real providers wire it in C). _(`ProviderErrorMapper` — `Map`/`EmptyTranscript`/`Timeout`; SafeMessage fixed-per-code, never echoes `ex.Message`.)_
- [x] Files: NEW — `Providers/Abstractions/ISttProvider.cs`, `ITranslationProvider.cs`, `ITtsProvider.cs`, `ProviderEvents.cs`, `AudioFrame.cs`, `ProviderErrorMapper.cs`, `AiInterpreter.Tests/ProviderErrorMappingTests.cs`. (`ProviderErrors.cs` reused from A.3.)
- [x] Anchors: `ARCH-012`. Cross-doc invariant: **NEW** (interface contracts → Appendix A) — registered in `server/CLAUDE.md` cross-doc table.

### B.2 — Fake providers (all variants)
- [x] `FakeSttProvider` (success-with-partials, empty-final, partials-then-error), `FakeTranslationProvider` (token-stream-then-final, immediate-final-only, error), `FakeTtsProvider` (chunked-then-complete, complete-only, error) — each emits ordered events with a **configurable delay before each event** via `yield return` + awaited delays and **honors `CancellationToken`** per `ARCH-012`. _(variant-by-enum ctor + `delayPerEvent`/scripted payloads + scriptable real-code `ProviderError`; pacing+cancellation via shared `FakeStreaming.PaceAsync`.)_
- [x] Files: NEW — `Providers/Fakes/Fake{Stt,Translation,Tts}Provider.cs` + `Providers/Fakes/FakeStreaming.cs` (shared `PaceAsync`) + `AiInterpreter.Tests/FakeProvidersTests.cs` (13).
- [x] Anchors: `ARCH-012`, `ARCH-020`. Cross-doc invariant: none. _(lesson §6 — streaming-fake pattern.)_

### B.3 — Latency model + MetricsAggregator (+ tests)
- [x] `LatencyEventFactory` stamps `LatencyEvent` with `clockSource` + `relativeMs` from the documented origin (ARCH-013 clock rules); `MetricsAggregator` computes universal + cascade + realtime metrics and the MUST/nice tiers (nice → `n/a`, never error). _(factory: `Create`/`Stamp`(injected `IClock`), relativeMs round+clamp≥0; aggregator: absolute-`Timestamp` math — cross-clock-safe, no-clamp on skew, never throws.)_
- [x] Tests: `MetricsAggregatorTests` (8) — MUST-pair formulas incl. **cross-clock** pairs (+/- skew, aggregator-no-clamp pinned vs factory-clamp); missing nice-tier event → `n/a`; realtime tier; empty-input no-throw.
- [x] Files: NEW — `Metrics/LatencyEventNames.cs`, `Metrics/MetricsModels.cs` (`TurnMetrics`), `Metrics/LatencyEventFactory.cs`, `Metrics/MetricsAggregator.cs`, `AiInterpreter.Tests/MetricsAggregatorTests.cs`; extended — `Program.cs` (DI: `IClock`→`SystemClock` + factory + aggregator). _(brief 008.)_
- [x] Anchors: `ARCH-013`, `ARCH-020`. Cross-doc invariant: **none** — consumes the existing A.3 `LatencyEvent` (already carries `clockSource`); `TurnMetrics` is an area-local computed type (not persisted/wire this slice → Appendix A registration deferred until first serialized). _(lesson §7; ARCH-013 "Metric origins" note added this round.)_

### B.4 — Streaming cascade orchestrator (+ tests, with fakes)
- [x] `CascadeStreamingOrchestrator` drives `STT partials/finals → per-finalized-segment translation (streamed) → TTS (streamed)` exactly per `ARCH-011` "Streaming pipeline" (nested per-segment loop; concurrent sub-utterance interleaving deferred per ARCH-025), stamping `stt.*`/`translation.*`/`tts.*` LatencyEvents on **first arrival** of each event type (via B.3's `LatencyEventFactory.Stamp` — never synthesize/back-date; forbidden-pattern #3) and emitting a flat transport-agnostic `IAsyncEnumerable<CascadeOutputEvent>` (`Transcript`/`Latency`/`Audio`/`Error`/`Done`). _(B.3 origins: also emits the stage-start markers `cascade.audio.received`/`translation.started`/`tts.started` the metric origins measure from.)_
- [x] Implements: empty-transcript short-circuit (`cascade.empty_transcript`, no translation/TTS), partial-failure rules (translation-fails → source kept; TTS-fails → both transcripts kept), and per-stage timeout/cancellation via linked CTS + `CancelAfter` (ARCH-012/ARCH-018). _(STT = per-event idle timeout via arm/disarm; OCE filtered `when (!ct.IsCancellationRequested)` so caller-cancel ≠ stage-timeout — both reviewer-flagged + fixed in-slice; lesson §8.)_
- [x] Tests: `CascadeOrchestratorTests` (CRITICAL, 9) — success path + stage ordering; **empty-transcript short-circuit (translation/TTS NEVER invoked via call-count spies; Status=Failed)**; two partial-failure cases; timeout → `*.timeout` retryable; first-arrival stamping; **multi-segment per-segment streaming** + idle-timeout regression + caller-cancellation propagation.
- [x] Files: NEW — `Cascade/CascadeStreamingOrchestrator.cs` (nested per-segment loop), `Cascade/CascadeModels.cs` (`CascadeOutputEvent` + `CascadeStartParams`), `AiInterpreter.Tests/CascadeOrchestratorTests.cs`; extended — `Metrics/LatencyEventNames.cs` (+`stt.started`, ARCH-013 MUST event). _(brief 009; no `Program.cs` change — DI is C.4.)_
- [x] Anchors: `ARCH-011`, `ARCH-012`, `ARCH-013`, `ARCH-018`, `ARCH-020`. Cross-doc invariant: none (`CascadeOutputEvent`/`CascadeStartParams` area-local — Appendix A at C.4 when serialized). _(Step-9 arch notes added this round: ARCH-012 STT-idle-timeout, ARCH-011 turn-lifecycle stage-label.)_

### B.5 — Cost estimator (+ tests)
- [x] `CostEstimator` loads pricing, **branches on pricing basis** (audio-minute vs token vs character vs audio-output-tokens), converts realtime audio-seconds→tokens, computes per-turn + per-minute cost, emits `CostEstimate` with assumptions + `pricingConfigVersion`. _(per-stage methods + `EstimateCascadeTurn` composite — Provider="cascade", Model=translation-model-used, PricingBasis="composite"; degrade via `Result<CostEstimate>`; `0.0` rate estimates to 0 ≠ absent; `decimal` no-rounding. lesson §9.)_
- [x] Tests: `CostEstimatorTests` (IMPORTANT, 12) — deterministic per-basis estimates (Deepgram min, OpenAI tokens for translation, gpt-4o-mini-tts audio-output tokens + approx-minute fallback, tts-1 chars); realtime conversion + cached-rate (incl. mini fall-back); cascade composite; cost/min + zero-duration; missing pricing/usage/model → unavailable; `0.0`-rate-still-estimates.
- [x] Files: NEW — `Cost/CostEstimator.cs`, `Cost/CostModels.cs` (`CostUsage`), `AiInterpreter.Tests/CostEstimatorTests.cs`; extended — `Program.cs` (DI: `CostEstimator`, first consumer of the A.5 pricing singleton). _(brief 010.)_
- [x] Anchors: `ARCH-014`, `ARCH-020`. Cross-doc invariant: none (`CostUsage` area-local). _(Step-9 arch note: ARCH-014 "Estimator conventions" — `"composite"` basis value + `RealtimeTokensPerAudioSecond=50` estimate.)_

### B.6 — WER calculator + phrase store (+ tests)
- [x] `WerCalculator` (DP edit distance over normalized word arrays: invariant-lowercase → strip `\p{P}` punctuation → collapse whitespace, **accents preserved**; `WER=(S+I+D)/N`, S/I/D backtraced individually, tie-break match>sub>del>ins; **WER unbounded** >1.0; empty reference → `ArgumentException`); `EvaluationPhraseStore` (self-loading facade, degrade-don't-crash per lesson §3) loads `evaluation-phrases.json` (10: 5 EN + 5 ES, no intra-word punctuation).
- [x] Tests: `WerCalculatorTests` (CRITICAL, 13) — perfect=0; deletion; insertion; substitution; empty hypothesis; **empty reference (N=0) rejected (no divide-by-zero)**; punctuation/casing normalization; accent-preserve; combined S/I/D (tie-break-invariant); **WER>1.0**; phrase-store load / degrade / size-guard.
- [x] Files: NEW — `Evaluation/WerCalculator.cs`, `Evaluation/EvaluationPhraseStore.cs`, `Evaluation/evaluation-phrases.json`, `AiInterpreter.Tests/WerCalculatorTests.cs`; extended — `Program.cs` (DI: `WerCalculator` + `EvaluationPhraseStore`), `AiInterpreter.Api.csproj` + `AiInterpreter.Tests.csproj` (copy the data file), `.env.example` (`EVALUATION_PHRASES_PATH`). _(brief 011; lesson §10.)_
- [x] Anchors: `ARCH-015`, `ARCH-020`. Cross-doc invariant: extended (`EVALUATION_PHRASES_PATH` env → ARCH-028 + cross-doc row, written this round). _(Step-9 arch note: ARCH-015 tie-break + accent-preserve.)_

### B.7 — Session store, persistence writer, summary (+ tests)
**Split into B.7a (SAFETY: store+writer+sentinel, `9bfea76`) + B.7b (summary service, `2d6a591`) — both landed.**
- [x] `SessionStore` (in-memory), `SessionPersistenceWriter` (write-on-end MUST + best-effort per-turn; filename `session_YYYYMMDDTHHMMSSZ_<short-id>.json`; **path-traversal guard** — server-generated id `^[A-Za-z0-9_-]+$`, resolved path stays under `SESSION_DATA_DIR`), `SessionSummaryService` (computes `SessionSummary` on demand).
- [x] Tests: `SessionPersistenceTests` (IMPORTANT) — round-trip; **sentinel assertion that JSON contains neither standard key, nor ephemeral secret, nor raw audio** (invariants 6/6b/8); transcripts/latency present; `../` sessionId rejected. _(Defense-in-depth cross-check, origin B.1 + B.4 security reviews: the writer must never serialize streaming raw audio — `TtsAudioChunk.Bytes` **or** `CascadeOutputEvent.Audio.Bytes` — structurally safe today since the session model has no audio field, but the sentinel + writer review should confirm it explicitly.)_ ✓ + `SessionSummaryServiceTests` (B.7b).
- [x] Files: NEW — `Sessions/SessionStore.cs`, `Sessions/SessionPersistenceWriter.cs`, `Sessions/SessionSummaryService.cs`, `AiInterpreter.Tests/SessionPersistenceTests.cs` (+ `SessionSummaryServiceTests.cs`). _(B.7a security-reviewer PASS #1/#2/#3/#5; B.7b pure read-only aggregation reusing the B.3 `MetricsAggregator`.)_
- [x] Anchors: `ARCH-016`, `ARCH-008`, `ARCH-019`, `ARCH-020`. Cross-doc invariant: none.

### B.8 — Error sanitizer (+ tests)
**Landed (SAFETY, invariant #4, `0262ce1`).**
- [x] `ErrorSanitizer` maps internal/provider errors → safe `ProviderError`/`UiError` (no stack traces, no secrets); preserves `HttpStatusCode`/`Retryable`; logs original server-side only. _(safe-by-construction: fixed message per code, never interpolates `ex.Message`/`Result.Error`/stack; logs original single-lined — log-forgery hardening; projects `ProviderError`→`UiError`, owns the generic `Exception`/`Result` boundary. lesson §13.)_
- [x] Tests: `ErrorSanitizerTests` — exception containing an API-key substring + stack → `SafeMessage` contains neither; status/retryable preserved. _(+ `Result<T>` overload, B.9c-i.)_
- [x] Files: NEW — `Security/ErrorSanitizer.cs`, `Security/UiError.cs` (backend record, ARCH-007 mirror), `AiInterpreter.Tests/ErrorSanitizerTests.cs`. _(security-reviewer PASS #4.)_
- [x] Anchors: `ARCH-019`, `ARCH-018`, `ARCH-020`. Cross-doc invariant: **NEW** `UiError` backend record (Appendix A + `server/CLAUDE.md` cross-doc row, written this round).

### B.9 — Session + Config HTTP endpoints (+ config tests)
**Split into B.9a (SAFETY #4 global handler, `88a7f3e`) + B.9b (SAFETY #1 config, `278caf2`) + B.9c-i (session endpoints, `e39ec7b`) + B.9c-ii (turn endpoints, `9e0066b`) — all landed** (two distinct safety invariants can't share a commit + the controller is large).
- [x] `SessionsController`: `POST /api/sessions`, `GET /api/sessions/{id}`, `POST …/end`, `GET …/summary` (B.9c-i), `POST …/turns`, `POST …/turns/{turnId}/complete`, `POST …/turns/{turnId}/events` (B.9c-ii) — request/response JSON exactly per `ARCH-009` (camelCase, nested config/direction/providerProfile). Backend owns `turnId` for all modes. _(thin controller → `SessionService`; `/complete` is realtime-only — cascade persists via C.4 WS; `CompleteTurnRequest` shape documented into ARCH-009.)_
- [x] `ConfigController`: `GET /api/config` reports configured booleans + model lists from **key presence only** (never values) per `ARCH-009`. _(B.9b; security-reviewer PASS #1.)_
- [x] **Global sanitizing exception handler (safety; origin A.5).** Wire `UseExceptionHandler` → normalized `UiError` via B.8's `ErrorSanitizer` (no stack traces / no secrets; ARCH-018/019). Map `Result`/`Result<T>` → response DTOs (never serialize `Result` directly; origin A.3). Enforce sane collection-size bounds on request bodies (`Transcripts`/`LatencyEvents`/`Metadata` — count caps via DataAnnotations; origin A.3). _(B.9a thin `IExceptionHandler` PASS #4; collection-COUNT caps landed — per-ITEM string caps deferred to carry-forward, bounded by Kestrel 30MB.)_
- [x] Tests: `ConfigEndpointTests` (IMPORTANT) — `configured=false` when a key is absent; no secret echo. _(+ `SessionsControllerTests`, `GlobalExceptionHandlerTests`.)_
- [x] Files: NEW — `Controllers/SessionsController.cs`, `Config/ConfigService.cs` (+`IConfigService`), `Config/ConfigModels.cs`, `Sessions/SessionService.cs` (+`ISessionService`), `Sessions/SessionDtos.cs`, `Security/GlobalExceptionHandler.cs`, `Security/ResultExtensions.cs`, `Controllers/ConfigController.cs`, + tests. _(new codes `session.not_found`/`turn.not_found`; framework 404/405 → ProblemDetails accepted for unrouted paths.)_
- [x] Anchors: `ARCH-009`, `ARCH-018`, `ARCH-019`, `ARCH-020`. Cross-doc invariant: extended (DTOs → Appendix A; `session.not_found`/`turn.not_found` → ARCH-018 — written this round).

### B.10 — Provider boundary tests (against fakes)
**Landed (`759cac2`).**
- [x] **NEW-create** `AiInterpreter.Tests/ProviderBoundaryTests.cs` (CRITICAL per ARCH-020) exercising the B.1 interfaces + B.2 fakes at the **interface level** (provider-agnostic — C.5 extends the same file with real providers). Scoped tight (6): net-new = **error-code preservation** (a `*Failed` carries the real `ProviderError` Code/Retryable/Stage/Provider through the boundary) + interface-level success-ordering anchors. Cancellation (B.2) + the mapper truth table (B.1) referenced, not re-tested. No real keys. (Real-provider/HTTP-mock + real-provider cancellation cases added "extended" in C.5.) _(lesson §17.)_
- [x] Files: NEW — `AiInterpreter.Tests/ProviderBoundaryTests.cs`.
- [x] Anchors: `ARCH-012`, `ARCH-020`. Cross-doc invariant: none.

### Acceptance criteria (B)
- [x] All B.X ticked. **All backend tests green** (orchestrator, provider boundary via fakes, WER, cost, persistence, metrics, sanitizer, config). Invest per the ARCH-020 priority tiers — `MetricsAggregatorTests`/`ErrorSanitizerTests` are THIN-IF-NEEDED. Session can be created/ended/persisted via HTTP using fake providers end-to-end. No real provider keys required to run tests. **Verified 2026-05-29: full suite `dotnet test` 161/161 green; build 0W/0E; `dotnet format` clean. Phase B COMPLETE.**

---

## Phase C — Cascade Real Providers + Streaming Transport

**Goal:** Implement the real Deepgram/OpenAI providers behind the Phase-B interfaces and the streaming WebSocket endpoint (+ blob fallback), so a real cascade turn streams end-to-end. **Highest-risk integration #1.**

**Spec anchors:** `ARCH-011`, `ARCH-012`, `ARCH-009`, `ARCH-008`, `ARCH-018`, `ARCH-019`, `ARCH-030`, `ARCH-020`.

### C.1 — DeepgramSttProvider (live WS + pre-recorded fallback)
**Landed (`<C.1 hash>` — set at /orchestrate-end).** Thin manual-smoke transport shell over a pure unit-TDD'd `DeepgramSttMapping`; `Channel<SttEvent>` callback bridge; one `ISttProvider` seam routes live-WS vs REST by `SttRequest.Encoding`. **NOT yet DI-wired** (consumers C.4 WS + C.5 blob — intentional B.1/B.2 seam-then-consumer pattern). 10 tests; suite 161→171. _(lesson §18; brief 020.)_
- [x] NuGet `Deepgram` (**v6.6.1**). Live path: `CreateListenWebSocketClient`, `model=nova-3`, `language=multi`, `smart_format=true`, `interim_results=true`, `encoding=linear16`, `sample_rate=<from start msg>`, `channels=1`, `utterance_end_ms`; emit `SttStarted → SttPartial* → SttFinal` per segment. _(WS `v2.WebSocket.LiveSchema`; callback→IAsyncEnumerable via `Channel<T>`, cancellation completes writer + `Stop()`s socket.)_
- [x] Fallback path: `CreateListenRESTClient` on a blob → single `SttFinal` (no interim; `stt.first_partial` = `n/a`); Deepgram auto-detects container — **no transcoding**. _(pure `ParsePrerecorded(SyncResponse)`; `PreRecordedSchema` lives in `Models.Listen.v1.REST`.)_
- [x] Exception/HTTP → `ProviderError` per the `ARCH-012` mapping table (429/401/403/400/5xx/empty/timeout) via reused `ProviderErrorMapper`. _(**v6.6.1 limitation, hardening tracked C.6:** the SDK throws `DeepgramRESTException` **without** HTTP status on the common error path → a real 429/401/403 degrades to `stt.unknown` (non-retryable); status-bearing mapping only on the rare empty-body `HttpRequestException`. Pinned + documented as intended.)_
- [x] Files: NEW — `Providers/Deepgram/DeepgramSttProvider.cs` (shell), `Providers/Deepgram/DeepgramSttMapping.cs` (pure helpers), `AiInterpreter.Tests/DeepgramSttProviderTests.cs` (10); extended — `AiInterpreter.Api.csproj` (`Deepgram` 6.6.1 + `InternalsVisibleTo("AiInterpreter.Tests")`).
- [x] Anchors: `ARCH-011`, `ARCH-012`, `ARCH-030`. Cross-doc invariant: **none** (confirmed — `DeepgramOptions`/`ISttProvider`/`SttEvent`/`ProviderErrorMapper` all consumed unchanged). _(ARCH-011 STT-path + empty-transcript-ownership realization notes added this round.)_

### C.2 — OpenAiTranslationProvider (streamed, both models)
**Landed (`fffa4c1`).** Raw-`HttpClient` SSE streaming shell + pure `OpenAiTranslationMapping`; mock-`HttpMessageHandler` end-to-end TDD (lesson §20). **NOT yet DI-wired** (consumers C.4 per-segment + C.5). 10 tests; suite 175→185. _(brief 021.)_
- [x] Responses API `POST /v1/responses` with `stream=true`; map `response.created→Started`, first `response.output_text.delta→translation.first_token + Partial`, deltas→Partial, `response.completed→Final` (+ usage tokens). Set **nested** `reasoning.effort="minimal"`, `text.verbosity="low"` (top-level `reasoning_effort` is Chat-Completions-only; no `[DONE]`; usage snake_case `response.usage.input_tokens/output_tokens`). Faithful-interpreter system instruction (output ONLY the translation). Model selectable (`gpt-5.4-nano` default, `gpt-5.4-mini`); record model used.
- [x] Exception/HTTP → `ProviderError` via `ProviderErrorMapper` (status-bearing — no C.1 Q5 gap); in-band SSE `error` event → `translation.unknown`. Per-stage timeout covers the whole send + read loop.
- [x] Files: NEW — `Providers/OpenAI/OpenAiTranslationProvider.cs` (shell), `Providers/OpenAI/OpenAiTranslationMapping.cs` (pure), `AiInterpreter.Tests/OpenAiTranslationProviderTests.cs` (10).
- [x] Anchors: `ARCH-011`, `ARCH-012`. Cross-doc invariant: **none** (confirmed — Options/contracts/mapper unchanged). _(ARCH-011 SSE-shape realization note added this round; lesson §20.)_

### C.3 — OpenAiTtsProvider (chunk streaming)
**Landed (`19acc15`).** Raw-`HttpClient` binary chunk-streaming shell + pure `OpenAiTtsMapping`; mock-`HttpMessageHandler` end-to-end TDD (lesson §20 + binary addendum). **NOT yet DI-wired** (consumers C.4 + C.5). 8 tests; suite 185→193. _(brief 023.)_
- [x] `POST /v1/audio/speech` with chunk-transfer streaming + **`stream_format="audio"`** (gpt-4o-mini-tts defaults to `sse`/base64-in-SSE — load-bearing); `tts.first_audio` = genuine first chunk; default `ResponseFormat=mp3` (config: wav/pcm); voice config-driven (uses `request.Voice`); guard 4096-char input cap pre-call via `MapStatus(400)` → `tts.invalid_request` (non-retryable, no HTTP call).
- [x] Emit `TtsStarted (after 200) → TtsFirstAudio → TtsAudioChunk(seq from 0)* → TtsComplete` (ContentType from response header, format-map fallback); exception/HTTP → `ProviderError` via `ProviderErrorMapper` (status-bearing — no Q5 gap); §20 timeout-covers-read-loop + non-inverted OCE filter.
- [x] Files: NEW — `Providers/OpenAI/OpenAiTtsProvider.cs` (shell), `Providers/OpenAI/OpenAiTtsMapping.cs` (pure), `AiInterpreter.Tests/OpenAiTtsProviderTests.cs` (8).
- [x] Anchors: `ARCH-011`, `ARCH-012`. Cross-doc invariant: **none** (confirmed). _(ARCH-011 TTS `stream_format` realization note added this round; lesson §20 binary addendum.)_

### C.4a — Cascade streaming WebSocket endpoint (core)
**Landed (`accd567` encoding-allowlist [SECURITY] + `fff1ec2` endpoint wiring — 2 commits).** Production wiring: `WS /api/cascade/stream` reachable; C.1/C.2/C.3 now live behind the unchanged B.4 orchestrator. 18 tests; suite 193→211. _(brief 024; lesson §21.)_
- [x] `WS /api/cascade/stream` per `ARCH-009`: client `start` → binary PCM → `stop`; server `transcript`/`latency`/`audio`/`cost`/`error`/`done`. Wires real providers into the **unchanged** `CascadeStreamingOrchestrator` (via `Channel<AudioFrame>` bridge); persists the turn (best-effort, B.7). _(thin shell + pure `CascadeWsMapping`/`CascadeStartValidation`; single socket sender.)_
- [x] DI swaps fakes → real providers by **key-presence** (`ConfigService.HasKey`) + `USE_FAKE_PROVIDERS` override; orchestrator + interfaces unchanged (seam proven).
- [x] **`encoding` allowlist (SECURITY, own commit).** `{linear16,pcm}` ordinal closed set validated (+ malformed-JSON rejected) **before** building `CascadeStartParams` (ARCH-019 header-injection boundary). Invalid → `cascade.invalid_audio`. _(security-reviewer PASS all 5; #3 raw-audio-never-accumulated confirmed.)_
- [x] **`LatencyStage.Overall` (cross-doc).** Added to the enum; `turn.recording.*` (WS, server-clock) + `turn.completed` (orchestrator, re-stamped Capture→Overall) carry it. _(ARCH-005 + Appendix A written this round.)_
- [x] **Cost emission.** `EstimateCascadeTurn` from observable usage (STT recording-minutes + translation tokens via `translation.final` Metadata [FORK-1a, additive] + TTS char-proxy) → `cost` message; degrades wholesale. _(ARCH-014 note written this round; TTS char-proxy = G.5 disclosure.)_
- [x] Files: NEW — `Cascade/CascadeStartValidation.cs`, `Cascade/CascadeWsMapping.cs`, `Cascade/CascadeWebSocketEndpoint.cs`, + 2 test files; extended — `Program.cs` (DI swap + map WS), `Sessions/SessionModels.cs` (`Overall`), `Cascade/CascadeStreamingOrchestrator.cs` (`turn.completed` Overall + `translation.final` Metadata tokens), `MetricsAggregatorTests.cs` (stale fixture). _(`Controllers/CascadeController.cs` is C.5.)_
- [x] Anchors: `ARCH-009`, `ARCH-011`, `ARCH-019`, `ARCH-005`, `ARCH-013`, `ARCH-014`. Cross-doc invariant: **`LatencyStage.Overall`** + WS message DTOs → ARCH-005/Appendix A (written this round); `CascadeOutputEvent`/`CascadeStartParams` stayed **internal** (only the WS DTOs serialize).

### C.4b — Cascade WS-boundary hardening
**Landed (`<C.4b robustness hash>` + `<C.4b SECURITY hash>` — set at /orchestrate-end; 2 commits).** All eight deferred items shipped over the live `WS /api/cascade/stream` path. Suite 211→257 green; build 0W/0E; format clean. security-reviewer invariants #1–#5 PASS. 2 commits: robustness helpers, then the SECURITY WS boundary (bounded channel + Origin both live in `CascadeWebSocketEndpoint.cs` → one isolated SECURITY commit; whole-file staging can't split them — orchestrator-approved). _(brief 025; lesson §22.)_
- [x] **`Origin` validation (origin A.5).** The WS upgrade bypasses CORS, so `CascadeWebSocketEndpoint` validates the `Origin` header against `FRONTEND_ORIGIN` (exact ordinal) **before** `AcceptWebSocketAsync` via pure `CascadeOriginValidation.IsAllowedOrigin`; disallowed/missing/`"null"` → 403 + `UiError`-shaped `cascade.forbidden_origin`, no socket accepted (non-WS → `UiError`-shaped 400). _(SECURITY commit.)_
- [x] **`TtsFirstAudio.ContentType` clamp (security low, origin B.4).** Provider content-type clamped to an audio allowlist (→ `audio/mpeg` fallback) before serializing into the WS `audio` message; pinned on the live `ToServerMessage` path, not just the helper.
- [x] **Bound the `Channel<AudioFrame>` (security medium, origin C.4a — FIRM).** `CreateBounded(256, FullMode=Wait)` backpressure (pump `TryWrite`→`await WriteAsync`; no audio dropped) + **writer-completed-on-`Done`** so a post-terminal parked write can't hang (a regression the bounding itself introduced, caught by code-quality review). _(SECURITY commit; G.4 5-min-no-leak tie.)_
- [x] **Double-`End`/`complete` guards (origin B.9c-i/ii, LOW).** Store-level idempotency: `SessionStore.FinalizeTurn → {Turn, Applied}` (already-terminal → `Applied:false`, transform skipped) + `End()` no longer re-stamps `EndedAt`; **both** the WS endpoint AND `SessionService.CompleteTurnAsync` (HTTP `/complete`) route through it (guarding one path only = half-open); the B.9c-ii 200-with-warning contract preserved.
- [x] **Stream-without-terminal hardening (origin B.4).** A started translation/TTS stage ending WITHOUT its terminal event → `<stage>.unknown` + `Done(Failed)` (was a silent skip/complete); STT partials-then-no-final → `stt.unknown`, clean silence → `Completed`. New non-exception `ProviderErrorMapper.Unknown(provider,stage)` factory (fixed `SafeMessage`, invariant #4).
- [x] **Start-frame field validation + error-shape polish (origin C.4a flags).** Reject blank `sessionId`/`turnId` + `sampleRate<=0`; **cap every client string at 256** (store-key/`done`-echo DoS — closes the [HIGH] security finding); non-WS → `UiError`-shaped 400; malformed-JSON / missing-field → `cascade.invalid_request` (distinct from the `cascade.invalid_audio` encoding allowlist; single parametrized `Invalid(code,msg)` factory); oversized/fragmented start frame rejected at the receive (manual-smoke).
- [x] **`VoiceByLanguage` resolution + stale comment (origin C.3).** `OpenAiTtsMapping.ResolveVoice(requestVoice, targetLang, options)` applied in `BuildRequestBody` (explicit request → `VoiceByLanguage[lang]` → `options.Voice`; provider-owned so the orchestrator stays provider-agnostic); stale "unused until C.3" comment fixed.
- [x] **Unit-pin the multi-segment cost accumulation (origin C.4a, test-gap #5).** Per-segment token/char accumulation extracted to a pure `CascadeWsMapping` fold (area-local `CascadeCostInputs`, non-serialized) + multi-segment additive-summing test (pins the C.4a undercount fix).
- [x] Files: NEW — `Cascade/CascadeOriginValidation.cs`, `AiInterpreter.Tests/{CascadeOriginValidationTests,SessionStoreTests}.cs`; extended — `Cascade/CascadeWebSocketEndpoint.cs`, `Cascade/CascadeStartValidation.cs`, `Cascade/CascadeWsMapping.cs`, `Cascade/CascadeStreamingOrchestrator.cs`, `Providers/Abstractions/ProviderErrorMapper.cs`, `Providers/OpenAI/OpenAiTtsMapping.cs`, `Providers/OpenAI/OpenAiOptions.cs`, `Sessions/SessionStore.cs`, **`Sessions/SessionService.cs`** (Flag-1 addition — guards the HTTP `/complete` path), `Program.cs` (+5 test files).
- [x] Anchors: `ARCH-009`, `ARCH-019`, `ARCH-018`, `ARCH-020`, `ARCH-011`. Cross-doc invariant: **none** (confirmed — `CascadeCostInputs`/`FinalizeResult` area-local + non-serialized). Arch-doc notes written this round: ARCH-018 (`cascade.invalid_request` + `cascade.forbidden_origin`), ARCH-011/018 (fail-closed stream-without-terminal + `Unknown` factory), ARCH-019 (Origin-at-endpoint + WS field caps).

> **C.4b residuals (deferred, non-blocking — none cut promised scope; surfaced by review):** (a) `StartFrameDto.Type=="start"` validation (protocol-honesty; the allowlist + field checks are the security boundary) — future hardening / G; (b) the TOCTOU not-found live-view read outside the gate (acknowledged single-node-MVP limitation — already documented on `SessionStore.Get`; `FinalizeTurn` returns null if the turn vanished → no corruption); (c) the pump's ghost-client-on-`ReceiveAsync` teardown (bounded by `RequestAborted`, pre-existing, G.4-adjacent). The C.6 LOW items (live-WS unbounded `Channel<SttEvent>`, Connect/Stop race) remain as listed under C.6.

### C.5 — Cascade blob fallback endpoint
**Landed (`<C.5 upload-validation SAFETY hash>` + `<C.5 endpoint hash>` — set at /orchestrate-end; 2 commits). CLOSES PHASE C.** Suite 257→284 green; build 0W/0E; format clean. security-reviewer invariants #1–#5 PASS. _(brief 026; lesson §23.)_
- [x] `POST /api/cascade/turn` (multipart): pre-recorded STT → streamed translation/TTS → single `CascadeTurnResponse` JSON (assembled turn + target audio base64 in-body, **never persisted** — invariant #3); **audio upload validation** (`CASCADE_MAX_UPLOAD_BYTES` ~10MB cap + content-type allowlist `audio/webm|ogg|wav|mpeg|mp4` → `cascade.invalid_audio` 413/415; **MIME params stripped** so `audio/webm; codecs=opus` is accepted — the MediaRecorder primary format; **multipart/Kestrel body bounded** to the cap so oversized→413 not 500). The documented non-streaming fallback. _(SAFETY commit = the upload validator, invariant #5.)_
- [x] The pre-recorded fallback lives in `Cascade/CascadeOrchestrator.cs` — a **thin adapter** that wraps the blob as a 1-frame container-encoding `AudioFrame` stream → the **unchanged** `CascadeStreamingOrchestrator.RunAsync` → collects the events via `CascadeWsMapping.AssembleTurn` + cost fold + `FinalizeTurn` (C.4b). NOT in the controller (ARCH-008); no pipeline re-implementation (ARCH-011). Pre-recorded STT comes free (C.1 routes non-`linear16` → REST).
- [x] Tests: `ProviderBoundaryTests` extended with the **SafeMessage sentinel sweep** across the real-provider mappings (invariant-#4 cross-check for reals). _(The standalone 429→`rate_limited` / timeout→`*.timeout` boundary cases are **covered by C.1–C.3 (per-provider HTTP-mock) + B.10 (interface-level-via-fakes)** + the Deepgram 429 is SDK-`err_code`-based → not HTTP-mockable; re-adding them would duplicate — documented decision, satisfies lesson §17's "extend boundary to reals" via the sweep + the per-provider tests. Net-new = the blob-path cascade outcomes + the sweep + the 0-byte pin.)_
- [x] **Real-provider test sweeps (origin C.3):** (a) SafeMessage sentinel — DONE (across `DeepgramSttMapping`/`OpenAiTranslationMapping`/`OpenAiTtsMapping.ToFailed` + `ProviderErrorMapper`, asserting no body/`ex.Message`/`err_msg`/`err_code`/stack in any `SafeMessage`); (b) **0-byte-audio TTS contract** — pinned as `TtsStarted → TtsComplete` (no `TtsFirstAudio`); the MVP contract (real TTS always returns audio).
- [x] Files: NEW — `Cascade/CascadeUploadValidation.cs`, `Cascade/CascadeOrchestrator.cs`, `Cascade/CascadeBlobModels.cs`, `Controllers/CascadeController.cs`, `AiInterpreter.Tests/{CascadeUploadValidationTests,CascadeBlobTests,CascadeControllerTests}.cs`; extended — `Program.cs` (DI + multipart/Kestrel body bound), `ProviderBoundaryTests.cs` (sentinel), `OpenAiTtsProviderTests.cs` (0-byte pin).
- [x] Anchors: `ARCH-009`, `ARCH-011`, `ARCH-008`, `ARCH-019`, `ARCH-018`, `ARCH-020`. Cross-doc invariant: **none** (model-wise). Doc rows written this round: **`CascadeTurnResponse`** → Appendix A; **`CASCADE_MAX_UPLOAD_BYTES`** → ARCH-028 + `.env.example`; blob-fallback realization → ARCH-009/011; **blob-STT-cost limitation** → ARCH-014 + G.5; upload allowlist + body-bound → ARCH-019 item 8.

> **C.5 residuals (deferred, non-blocking — review-surfaced, no promised-scope cut):** (a) `CascadeTurnForm` lacks `[Required]` on Source/Target → a missing direction silently binds En→En (bounded for the no-auth MVP; **relevant to Phase D** when the frontend supplies direction — fold into D form-validation or a hardening pass); (b) the **FORK-1a `SttFinal.Duration`** enhancement to price the blob STT stage (surface Deepgram's pre-recorded `SyncResponse.Metadata.Duration` → `stt.final` Metadata → cost; a cross-doc `SttFinal` change touching the orchestrator — deferred as gold-plating for a fallback at Phase-C-close; tracked).

### C.6 — Real-provider hardening (origin C.1)
> Hardening backlog surfaced by C.1 (brief 020 Step-9). **User decision 2026-05-29: PULL THE Q5 ITEM FORWARD (option ii)** — error fidelity feeds the G.5 comparison write-up, so it's sequenced as the **immediate next slice after C.1, before C.2** (brief 022). The remaining two items stay deferred/lower-priority + non-acceptance-blocking (Phase C exits on "a real cascade turn streams end-to-end") — they may stay unchecked at Phase-C exit with a Log note.
- [x] **Deepgram error-status fidelity (Q5, MEDIUM) — DONE `f69a366` (brief 022 `deepgram_error_status_fidelity`; pulled forward, landed before C.2).** v6.6.1 throws `DeepgramException`/`DeepgramRESTException` with **no HTTP status** on the common error path, so a real 429/401/403 degrades to `stt.unknown` (non-retryable) — losing rate-limit retryability + auth distinction. Extract the status from the SDK exception → route through the ARCH-012 mapper (vendor-agnostic `ProviderErrorMapper.MapStatus`). **Cross-doc:** ARCH-012 note + Appendix A (flips C.1 test 8's expectation; no structural rework). Collapses the C.5 reconciliation note above (real Deepgram 429 → `rate_limited` once this lands). _(Step-1 reality: `ErrCode` = Deepgram `err_code` **string** (matched string→status). **Accepted residual:** 5xx-**with-body** → `stt.unknown` (Deepgram 5xx uses key `error_code` ≠ `err_code` → `ErrCode` stays at the SDK default `"Unknown Error Code"`; `ErrMsg`-matching rejected as fragile); 5xx-empty-body still → `upstream_unavailable`. 429/401/403/400 recovered = the user's stated goal. See brief 022.)_
- [ ] **Live-WS unbounded `Channel<SttEvent>` (LOW):** the C.1 live path bridges via an unbounded channel — a stalled consumer could grow it for a session. Bounded-channel backpressure is the fix; ARCH-019 single-user posture tolerates today. (Activates at C.4 when the live path is wired; stability-relevant at G.4.)
- [ ] **Connect/Stop race on very-fast cancellation (LOW):** a Connect/Stop race on immediate cancellation can drop a late `SttFailed` (no hang/crash; bounded).
- [ ] Anchors: `ARCH-012`, `ARCH-018`, `ARCH-019`. Cross-doc invariant: the Q5 item is **ARCH-012 mapper** when it lands.

### Acceptance criteria (C)
- [x] **MET (2026-05-29).** All C.X ticked **except the C.6 LOW residuals** (live-WS unbounded `Channel<SttEvent>` + Connect/Stop fast-cancel race — remain open at Phase-C exit by the C.6 plan, documented, G.4-adjacent). The cascade streams honestly (the orchestrator emits source partials → target tokens → first TTS audio before `tts.complete`, each stage stamped on real first-arrival — unit-pinned against fakes, B.4/C.4a/C.4b; never synthesized — forbidden #3/#4); per-stage LatencyEvents are real; the turn persists (best-effort). Boundary tests green (B.10 + the C.5 real-provider SafeMessage sweep). Blob fallback works (`POST /api/cascade/turn`, `CascadeControllerTests`). **Real-key end-to-end smoke** (live Deepgram/OpenAI on both transports) = the C/D demo-checklist item (ARCH-020 exempt; logic is fake-tested). Suite 284 green.

---

## Phase D — Frontend Core + Cascade Streaming UI

**Goal:** Build the SPA shell, session setup, audio capture (streaming PCM + blob fallback), the cascade streaming client, playback, and the live transcript/metrics/cost panels — so a user can run a streaming cascade turn end-to-end in the browser.

**Spec anchors:** `ARCH-007`, `ARCH-030`, `ARCH-009`, `ARCH-011`, `ARCH-013`, `ARCH-014`, `ARCH-017`, `ARCH-018`, `ARCH-020`, `ARCH-029`.

### D.1 — App shell, API clients, state store ✅ (2026-05-29; brief 027; 20 web tests, suite 22/22 green; reviewers: security #1–#5 PASS, code-quality 1 MEDIUM fixed in-slice)
- [x] React+Vite shell; `sessionStore` holding `UiSessionState` (ARCH-007); API clients `sessionsApi`/`cascadeApi`/`configApi` (realtime/evaluation added later). `VITE_API_BASE_URL` wired (per-call `?? ''` → dev proxy zero-config).
- [x] **Clean-separation invariant:** UI components render only from `sessionStore`/`UiSessionState` (ARCH-007); transports (cascade WS client, realtime client) own all wire detail; components never import transport-client internals. _(Shell renders only from the store; no transport internals exist yet — the structural separation test is practical at D.4/D.7 when a transport client lands.)_
- [x] Files: NEW — `web/src/{App.tsx (mod), state/sessionStore.ts, api/{http,configApi,sessionsApi,cascadeApi}.ts, types/domain.ts, vite-env.d.ts}`, `web/.env.example`, `web/vite.config.ts (mod: `/api` proxy, `ws:true`)`. (`main.tsx` unchanged — store is a module singleton via `useSyncExternalStore`, no provider needed.)
- [x] Anchors: `ARCH-007`, `ARCH-029`, `ARCH-009`, `ARCH-005`. Cross-doc: **first `web/src/types/` models landed** → `web/CLAUDE.md` cross-doc table populated (view models → ARCH-007; wire mirrors → ARCH-009/Appendix A; enums → ARCH-005); no `ARCHITECTURE.md` edit (mirror-registration, not a contract change). Lessons §2–§4 (web/) banked.

### D.2 — Session setup, mode + model selectors, config-gating ✅ (2026-05-29; brief 028; 19 web tests, suite 40/40 green; reviewers: security #1–#5 PASS, code-quality 1 HIGH fixed in-slice)
- [x] `SessionSetup` (label, direction, **realtime-model + translation-model selectors**) + `ModeToggle`; **disable unconfigured modes** from `providerHealth` (Flow A — config fetched by D.1's App on-mount bootstrap; D.2 reads `providerHealth` from the store via pure `selectors.ts`). ModeToggle disabled during recording/processing/playing (`canToggleMode`). Start/End wire `sessionsApi.createSession`/`endSession` via a DI'd `sessionActions.ts`.
- [x] Files: NEW — `web/src/components/{SessionSetup,ModeToggle}.tsx`, `web/src/state/{selectors,sessionActions}.ts`. MOD — `sessionStore.ts` (`updateSessionConfig` w/ guarded transition; removed dead D.1 `configureSession`), `App.tsx` (mounts both).
- [x] Anchors: `ARCH-007`, `ARCH-009`, `ARCH-017`. Cross-doc invariant: none (consumed D.1 types). Lessons §5–§7 (web/) banked. Reachability: closes D.1's `createSession`/`endSession` foundation-ahead-of-callers gaps; `getSession`/`createTurn`/`cascadeApi` remain → D.4.

### D.3 — Audio capture controller (streaming PCM + blob fallback) ✅ (2026-05-29; brief 029; 7 web tests, suite 47/47 green; reviewers: security #1–#5 PASS, code-quality 1 HIGH + 4 MEDIUM fixed in-slice)
- [x] Streaming: `getUserMedia` → `AudioContext` → `AudioWorkletNode` (Float32→Int16 linear16, ~20ms frames) → binary frames via `onFrame` callback; declare `encoding=linear16` + actual `sampleRate` (no resample). Fallback: `MediaRecorder` with **probed** mimeType (probe order per ARCH-030) → blob (duration clamped, SECURITY). _(Browser-API shell = manual-smoke per ARCH-020; pure helpers — conversion/probe/mic-map/duration-clamp — test-first.)_
- [x] Mic-permission-denied path → `micErrorToUiError` (`mic.permission_denied`, sanitized) via `onError`. _(Pure mapping pinned here; the full component path + Start-disabled is the D.7 mic-denied component test.)_
- [x] Files: NEW — `web/src/audio/{pcm,captureSupport,audioCaptureController}.ts` + `pcmWorklet.ts` (+ `pcm.test.ts`, `captureSupport.test.ts`). No `vite.config.ts` change (`addModule(new URL('./pcmWorklet.ts', import.meta.url))` is Vite-native — confirm in the C/D demo-smoke).
- [x] Anchors: `ARCH-030`, `ARCH-007`, `ARCH-020`. Cross-doc invariant: none (reuses D.1 `UiError`). Lesson §8 (web/) banked. Reachability: controller + worklet are foundation-ahead-of-callers → D.4 (`RecordingControls → controller → cascadeStreamClient`).

### D.4 — Cascade streaming client + recording controls ✅ (split a/b — both landed 2026-05-29; cascade recording path reachable end-to-end)
- [x] **D.4a** (brief 030; 16 web tests, suite 63/63 green; reviewers: security #1–#5 PASS, code-quality 2 HIGH+2 MED fixed in-slice): `cascadeStreamClient` opens `WS /api/cascade/stream`, sends `start` → binary PCM via `sendFrame` → `stop`, dispatches `transcript`/`latency`/`cost`/`error`/`done` into the store (`audio` → `onAudio` callback ONLY, never the store — invariant #3); TDD'd against an injected fake `WebSocket`. + the 6 streaming store actions (deferred from D.1) + 4 wire types tightened into `domain.ts`. Lessons §9–§10 (web/); cross-doc rows for the 4 wire types.
- [x] **D.4b** (brief 031; 12 web tests, suite 75/75 green; reviewers: security #1–#5 PASS, code-quality 1 HIGH+3 MED fixed in-slice): `RecordingControls` (gated by `canStartRecording`/`canStopRecording` — the ARCH-007 transition table) + a DI'd `recordingActions.ts` orchestration wiring `createTurn → beginTurn → capture → cascadeStreamClient.start` (`ttsVoice:''` → backend `ResolveVoice`). **Closes the D.1 `createTurn` + D.3 capture + D.4a client foundation-ahead-of-callers gaps** — cascade recording runs end-to-end. Client hardened (pre-open frame-queue + deferred/idempotent stop); `inFlight` concurrent-start guard. Lesson §11. (Playback + panels are D.5/D.6.)
- [x] Files: NEW — `cascade/cascadeStreamClient.ts`, `state/{recordingActions,selectors(+),sessionStore(+)}.ts`, `components/RecordingControls.tsx`, `types/domain.ts`(+4 wire types) (+ their tests); MOD — `audioCaptureController.ts` (singleton), `App.tsx` (mounts RecordingControls).
- [x] Anchors: `ARCH-009`, `ARCH-007`, `ARCH-011`. Cross-doc invariant: 4 TS wire mirrors (D.4a → `web/CLAUDE.md` rows; no ARCHITECTURE.md change).

### D.5 — Playback controller ✅ (2026-05-29; brief 032; 4 web tests, suite 79/79 green; reviewers: security #1–#5 PASS, code-quality 2 HIGH + mediums fixed in-slice)
- [x] Streamed cascade audio → `MediaSource` progressive playback (append queue pumped on `updateend`); **fallback** to assembled-blob `HTMLAudioElement` (best-effort — a static element `src` can't progressively play a live stream; MSE primary). Stamp `playback.started` on the `playing` event (once/turn → `appendLatencyEvent`). No overlapping playback (a new turn resets the prior). Audio held transient — never in the store (invariant #3); `reset()` revokes the object URL.
- [x] Files: NEW — `web/src/audio/playbackController.ts`(+test); MOD — `cascade/cascadeStreamClient.ts` (settable `onAudio` delegate `setAudioSink`), `main.tsx` (composition-root wiring). Closes the D.4a/D.4b `onAudio` foundation-ahead gap. Lesson §12 (web/). `clampAudioContentType` added (defense-in-depth, mirrors C.4b).
- [x] Anchors: `ARCH-030`, `ARCH-013`, `ARCH-007`. Cross-doc invariant: none (reuses `LatencyEvent`). **Limitation:** cascade `playback.started` is frontend-display-only (carry-forward → F.3/G.5); top-level latency-delta computation is a D.6 design question (carry-forward).

### D.6 — Transcript, metrics, cost panels (live) ✅ (2026-05-29; brief 033; 13 web tests, suite 79→92 green; 2 commits [infra + panels]; reviewers: invariants PASS [#2 N/A], code-quality mediums triaged)
- [x] `TranscriptPanel` (dumb `{text,isFinal}[]` projection — store normalizes, lesson §10), `MetricsPanel` (current-turn top-level + cascade per-stage + session averages by mode; `n/a` for unavailable), `CostPanel` ("Estimated cost/min" qualified + model + assumptions tooltip). **Metrics three-source model (lesson §13):** per-stage from store; **top-level deltas frontend-computed** (`deriveTurnMetrics` Between on absolute timestamps — the backend structurally can't for cascade); session-avgs from `GET /summary` (`sessionsApi.getSummary`). Client lifecycle stamps (`recording.started`/`stopped`/`completed`) added; store retains raw `latencyEvents[]` + full `cost`.
- [x] Files: NEW — `web/src/components/{TranscriptPanel,MetricsPanel,CostPanel}.tsx`; MOD — `state/{selectors(deriveTurnMetrics,formatCostPerMinute),sessionStore(retention+turn.completed stamp+setSummary),recordingActions(2 stamps)}.ts`, `api/sessionsApi.ts`(getSummary), `types/domain.ts`(+`SessionSummary`/`ModeSummary`/`WerSummary`; `TurnViewModel`+`latencyEvents`/`cost`), `App.tsx`(mount + summary auto-fetch).
- [x] Anchors: `ARCH-007`, `ARCH-013`, `ARCH-014`. **Cross-doc: 2 tightens** — `TurnViewModel`+`latencyEvents`/`cost` (→ ARCH-007 §4 block edited + `web/CLAUDE.md` row); `SessionSummary`/`ModeSummary`/`WerSummary` mirror (→ `web/CLAUDE.md` row; ARCH-005/009/Appendix A; no contract change). Lesson §13 (web/) banked.

### D.7 — Error banner + frontend state tests ✅ (2026-05-29; brief 034; 5 web tests, suite 92→97 green; reviewers: security 0 findings #4 PASS, code-quality 2 MED+2 LOW fixed in-slice) — **CLOSES PHASE D**
- [x] `ErrorBanner` renders the store's sanitized `UiError[]` via `errorCopy` — a pure code→actionable-copy map that reads ONLY `code`, never echoes `safeMessage` (structural never-raw, ARCH-007/018; sentinel-pinned). Replaces the inline raw-`safeMessage` list.
- [x] Tests (light, PRD-aligned, jsdom): **#1** mode-toggle disabled during recording/processing/playing (pins D.2 `canToggleMode`); **#2** **mic-denied** (`getUserMedia` rejection → actionable `mic.permission_denied` surfaces + turn fails: **Stop disabled, Start RE-ENABLED for retry** — matching the "…and retry" copy + the D.2 retry-after-fail design; the literal "Start disabled" was imprecise spec wording → clarified here + in ARCH-020, lead-approved).
- [x] Files: NEW — `web/src/components/{errorCopy(+test),ErrorBanner,ModeToggle.test,micDenied.test}`, `web/src/test/setup.ts`; MOD — `App.tsx` (mount ErrorBanner), `vite.config.ts` (`test.setupFiles`; node env kept — per-file `// @vitest-environment jsdom`), `package.json`/`-lock` (devDeps: jsdom + `@testing-library/{react,dom,jest-dom}`, dev-only).
- [x] Anchors: `ARCH-007`, `ARCH-018`, `ARCH-020`. Cross-doc invariant: none (consumes `UiError`). Lesson §14 (web/) banked. Stands up the jsdom/Testing-Library component-test env (deferred from A.1).

### Acceptance criteria (D) ✅ PHASE D COMPLETE (2026-05-29)
- [x] All D.X ticked (D.1–D.7). The cascade frontend is wired end-to-end in code: session setup + mode/model selectors (config-gated, unconfigured modes disabled) → streaming-cascade record (capture → WS) → live source/target transcripts (partials→finals) + per-stage latency + estimated cost/min → audible TTS playback → sanitized error surface. **Frontend state tests green (97/97** incl. the 2 ARCH-020 transitions: mode-toggle-disabled + mic-denied). _The live in-browser EN→ES streaming-cascade smoke (real keys, end-to-end < 3s observed) is the **C/D demo-checklist** (non-blocking, ARCH-020-exempt — pending real OpenAI/Deepgram keys), consistent with the A/B/C real-key-smoke deferral._

---

## Phase E — Realtime Mode

**Goal:** Implement the Realtime path: backend ephemeral-credential broker + event ingest, the browser WebRTC client with VAD-off manual turns, event normalization, connection lifecycle, and mode-switch/recovery flows. **Highest-risk integration #2.**

**Spec anchors:** `ARCH-010`, `ARCH-009`, `ARCH-007`, `ARCH-013`, `ARCH-014`, `ARCH-017`, `ARCH-018`.

### E.1 — Realtime client-secret broker ✅ (2026-05-29, brief 035)
- [x] `RealtimeClientSecretService` + `POST /api/realtime/client-secret`: calls **`POST https://api.openai.com/v1/realtime/client_secrets`** (GA) with the standard key; body per `ARCH-010` (session.type=realtime, model, instructions, `output_modalities:["audio"]`, `audio.input.turn_detection=null`, `audio.input.transcription.model=gpt-4o-transcribe`, `audio.output.voice`); map `value→clientSecret`, `expires_at→expiresAt`. **Never** `/v1/realtime/sessions`. _(Response parse is dual-shape: GA top-level `{value,expires_at,session}` primary + legacy nested `client_secret.{…}` fallback — ARCH-010 corrected this round.)_
- [x] Tests: token-failure path sanitized; one bounded auto-retry honoring `Retry-After` (only this path; injected test-controllable delay). _(20 tests; SAFETY sentinels for #1 standard-key-non-leak + #2 ek_-response-only; security-reviewer PASS.)_
- [x] Files: NEW — `Realtime/RealtimeClientSecretService.cs`, `Controllers/RealtimeController.cs` (+ `Realtime/RealtimeClientSecretMapping.cs` pure mapper, `Realtime/RealtimeModels.cs` DTOs + `RealtimeMintOutcome` + `RealtimeModelCatalog`); modified `Program.cs` (typed HttpClient). Model resolved + allow-listed (off-catalog → 400, fail-closed before upstream); missing key short-circuits.
- [x] Anchors: `ARCH-010`, `ARCH-009`, `ARCH-018`. Cross-doc invariant: none (DTOs implement the documented ARCH-009 §6 contract; Appendix A `ClientSecret` row refined this round).

### E.2 — Realtime turn-events ingest ✅ (DONE — delivered by B.9c-ii, no new code)
- [x] `POST /api/sessions/{id}/turns/{turnId}/events` persists normalized events (with `clockSource`); turn created via `POST …/turns` first (backend-owned turnId). _(Verified 2026-05-30: `SessionsController.AppendEvents:70-80` + `AppendEventsRequest` `[Required,MaxLength(500)] List<LatencyEvent>` — `LatencyEvent` carries `ClockSource`; persisted verbatim. B.9c-ii forward-built this for E.2/E.4 (its DTO docstring says so). Normalization is the frontend's E.4 job; the backend persists generically — no realtime extension needed.)_
- [x] Files: extended — `Controllers/SessionsController.cs`. _(Already present from B.9c-ii; no E.2 delta.)_
- [x] Anchors: `ARCH-009`, `ARCH-010`. Cross-doc invariant: none.

### E.2b — Realtime turn cost ✅ (2026-05-30, brief 036 — wired `EstimateRealtime`; resolves the breakdown gap)
- [x] `CostEstimator.EstimateRealtime` (was ZERO callers → realtime `CostEstimate` null) **wired** into `SessionService.CompleteTurnAsync`: branch `Mode == Realtime` (cascade WS-priced, never here); model from `SessionConfig.ProviderProfile.RealtimeModel`; input audio-seconds from the merged `turn.AudioDurationMs`; computed **inside the idempotent `FinalizeTurn` transform**. `CostEstimator` injected into `SessionService`.
- [x] Tests: 7 (RealtimeTurnCostTests) — realtime sets cost (model-keyed, **`"tokens"`-basis**, seconds in `Units`); cascade unchanged; pricing-absent → null; **unreported/zero duration → null (never a synthetic $0.00)**; idempotent re-complete; output counted when reported, disclosed-unavailable when absent. Suite 304→311 green.
- [x] **Step-2.5 fork RESOLVED → (a):** added optional `CompleteTurnRequest.OutputAudioDurationMs?` (forward-built for E.4 to report; absent → output disclosed-unavailable **at the wiring layer** — `EstimateRealtime` silently zeroes `AudioOutputSeconds ?? 0m`, so the caller appends the disclosure). _(Note: `PricingBasis` is `"tokens"` not `"audioSeconds"` — corrected from the brief; realtime bills audio tokens, seconds in `Units`.)_
- [x] Files: extended — `Sessions/SessionService.cs` (cost wiring), `Sessions/SessionDtos.cs` (`CompleteTurnRequest` +`OutputAudioDurationMs?`); new `AiInterpreter.Tests/RealtimeTurnCostTests.cs`. Brief **036**.
- [x] Anchors: `ARCH-014`, `ARCH-009`, `ARCH-013`. **Cross-doc invariant: YES** (`CompleteTurnRequest` +`outputAudioDurationMs?` → ARCH-009 §6 + Appendix A + ARCH-014 realization note, orchestrator-written this round).

### E.3 — Browser WebRTC client ✅ (2026-05-30, brief 038 — foundation transport library; consumer-pending → E.4/E.5)
- [x] `realtimeWebRtcClient`: mint secret → `RTCPeerConnection` + `oai-events` data channel → `addTrack` mic → `createOffer`/`setLocalDescription` → **POST `offer.sdp` to `https://api.openai.com/v1/realtime/calls`** (Bearer ephemeral, `Content-Type: application/sdp`, **no `?model=`**) → `setRemoteDescription(answer)`; remote audio via `ontrack`. _(Realized as a **manual-smoke shell** (`createRealtimeWebRtcClient`, browser-WebRTC exempt) over **pure tested seams**: `realtimeCallsUrl` (no `?model=`) + `exchangeSdpOffer` (Bearer `ek_`/`application/sdp`/text answer; status-derived `retryable`); capture = the client's own `getUserMedia`/`addTrack` of the raw track. Plus `realtimeApi.mintClientSecret` + a pure stateless GA-event normalizer `realtimeEvents` (`parse`+`normalize`→`NormalizedRealtimeEvent`; classify-only — E.4 stamps). web lesson §15.)_
- [x] Files: NEW — `web/src/realtime/realtimeWebRtcClient.ts`, `web/src/realtime/realtimeEvents.ts`, `web/src/api/realtimeApi.ts` (+ their `.test.ts`); modified `web/src/types/domain.ts` (+`RealtimeTokenRequest`/`RealtimeTokenResponse` TS mirrors). +21 web tests (97→118 green); security-reviewer PASS on #1/#2/#4 (the `ek_` is a transient client local — never the store/persisted/logged). _(slice hash pending the impl's done-with-slice report; rides this round-seal.)_
- [x] Anchors: `ARCH-010`, `ARCH-007`. **Cross-doc invariant: mirror-registration** (the `RealtimeTokenRequest`/`RealtimeTokenResponse` TS mirror — `web/CLAUDE.md` cross-doc row written this round; Appendix A's `ClientSecret` already carries the backend row, so **no ARCHITECTURE.md contract change**). ARCH-010 §7 gained an E.3 realization note (the normalizer's GA field reads, smoke-pending).

### E.4 — Manual turn control + event mapping + metrics
> **Split E.4a (data path) ✅ + E.4b (turn control + UI)** at the 2026-05-30 round (E.4 was ~200 lines across 5 concerns — too big for one slice; D.4a→D.4b precedent).

#### E.4a — Realtime event sink + metrics + reporting ✅ (2026-05-30, brief 039 — the data path; consumer-pending → E.4b)
- [x] `createRealtimeEventSink({store, clock})` — a **per-turn stateful** sink: `handle(NormalizedRealtimeEvent)` maps transcripts by role (**accumulating incremental deltas into cumulative partials** — the §10 store action is cascade-cumulative; reset the source accumulator on `.completed`; finalize the target on `responseDone`), stamps first-of-type latency browser-clock (`realtime.first_audio_delta`/`first_transcript_delta`, once each), `completeTurn` on `responseDone` (the store owns the `turn.completed` stamp), `failTurn` on `error`. **`audioDelta` is TIMING-ONLY** — stamps `first_audio_delta`, writes NO audio to the store (invariant #3; realtime audio plays via the WebRTC media track, never persisted; structurally enforced by an audio-sink-less `Pick<SessionStore>`).
- [x] `sessionsApi.appendTurnEvents(sid, tid, events)` → `POST /api/sessions/{id}/turns/{turnId}/events` (`{events}`, the backend `AppendEventsRequest`) — reports the turn's client events so the backend aggregates the canonical realtime metrics (§13 realtime half).
- [x] Files: NEW — `web/src/realtime/realtimeEventSink.ts` (+`.test.ts`); modified `web/src/api/sessionsApi.ts` (+`.test.ts`). +12 web tests (118→130 green); security-reviewer PASS on #1/#2/#3/#4 (invariant-#3 sentinel genuine). web lesson §16. _(slice hash pending the impl's done-with-slice report; rides this round-seal.)_
- [x] Anchors: `ARCH-010`, `ARCH-013`. **Cross-doc invariant: none** (reuses `LatencyEvent`/`TranscriptSegment`/`UiError`/`NormalizedRealtimeEvent`; `{events}` mirrors the existing `AppendEventsRequest`). ARCH-010 §7 gained an E.4a metrics-split + first-audio smoke-confirm note.

#### E.4b — Manual turn control + RecordingControls + TranscriptPanel ✅ (2026-05-30, brief 040 — CLOSES the realtime reachability gap; realtime now reachable + audible from the real UI)
- [x] `createRealtimeTurnController({store, client, api, clock})` (the §11 cascade-recording analogue): `startTurn` (createTurn→beginTurn→lazy-connect→per-turn E.4a sink wired to the client's raw `onServerEvent` (parse→normalize→sink)→`session.update turn_detection=null`→`input_audio_buffer.clear`→stamp `recording.started`); `stopTurn` (`commit`→`response.create`→stamp `recording.stopped`); report on `responseDone`; `inFlight` guard. **Buffer-delimited** turns — the mic streams continuously (E.3 `addTrack`), no per-turn mic toggle.
- [x] RecordingControls dispatches by `currentMode` (realtime→controller / cascade unchanged) — the real entry point (`App.tsx`). Client refactor: += `sendClientEvent`/raw `onServerEvent`, **normalize moved INTO the tested controller** (removed `deps.onEvent` — shrinks the exempt shell); the singleton routes the remote audio track to a detached `<audio>` (invariant #3 live-track-never-stored) with a once-stamped `playback.started` (the realtime first-audio fallback). TranscriptPanel "source unavailable" (D.6/D.7) gains characterization tests.
- [x] Files: NEW — `web/src/realtime/realtimeTurnController.ts` (+`.test.ts`), `web/src/components/{TranscriptPanel,RecordingControls}.test.tsx`; modified `web/src/realtime/realtimeWebRtcClient.ts`, `web/src/components/RecordingControls.tsx`. +14 web tests (130→144 green); security-reviewer PASS #1–#4. web lesson §17. _(slice hash pending the impl's done-with-slice report; rides this round-seal.)_
- [x] Anchors: `ARCH-010`, `ARCH-013`. **Cross-doc invariant: none** (the `RealtimeWebRtcClient` surface change is area-internal transport, not a contract model). ARCH-010 §7 gained an E.4b manual-turn-control envelope realization note. _(B.3-origin `realtime.session.connecting` stamp at connect-start → moves to E.5 with the persistent-pc connect hoist.)_

### E.5 — Connection lifecycle, disconnect, recovery, mode-switch
> **Backend-status note (2026-05-30):** **Flow-G mode-transition recording is already built** (`SessionStore.RecordModeTransition`). **Flow-H backend stale-session flush ✅ SHIPPED** (E.5-backend, brief 037). **Split E.5a (lifecycle + disconnect + teardown) ✅ + E.5b (mode-switch Flow G + recovery)** at the 2026-05-30 round (E.5 was ~200 lines across many concerns).

#### E.5a — Connection lifecycle + disconnect + teardown ✅ (2026-05-30, brief 041)
- [x] One persistent `RTCPeerConnection` across turns via `realtimeConnectionManager` (idempotent `ensureConnected` — discharges the double-`connect()` guard; **the connect latch resets on a failed connect** so a transient failure doesn't brick the mode — the controller catches→`failTurn`+abort). `realtime.session.connecting` stamped at connect-**initiation** (browser clock → `realtime_connect_ms`), `connected`/`disconnected` from the pc connectionstate (a settable `onConnectionState` shell delegate → tested mapper).
- [x] **Disconnect surfaced** (ARCH-018): connectionstate `failed`/`disconnected` → stamp `realtime.session.disconnected` + frontend-synthesized sanitized `UiError` → `failTurn` (active, populates the turn's `errors`) / `addError` (between turns) + `errorCopy` advise-switch-to-Cascade. **Teardown on End** (wired into `SessionSetup` End): close DC/pc, stop tracks, release stream, detach+null `<audio>`, reset latches + the `playback.started` once-guard.
- [x] Files: NEW — `web/src/realtime/realtimeConnectionManager.ts` (+`.test.ts`), `web/src/components/SessionSetup.test.tsx`; modified `web/src/realtime/realtimeWebRtcClient.ts` (`onConnectionState` + `close()`→`teardown()` + `<audio>` detach), `realtimeTurnController.ts` (delegate connect + connect-fail abort), `web/src/components/errorCopy.ts` + `SessionSetup.tsx` (End→teardown). +9 web tests (144→153 green); security-reviewer PASS #1–#4 (0 findings). web lesson §18.
- [x] Anchors: `ARCH-010`, `ARCH-018`, `ARCH-013`. **Cross-doc invariant: none** (the client surface change is area-internal transport). ARCH-010 §7 gained an E.5a realization note. **Discharges 4 carry-forwards:** failed-turn `Errors`, double-connect guard, B.3 `connecting` stamp, `<audio>` lifecycle teardown.

#### E.5b — Mode-switch (Flow G) teardown ✅ (2026-05-30, brief 042 — scoped TIGHT) · recovery + ModeTransitionEvent-persist RE-SEQUENCED
- [x] **Flow-G mode-switch teardown:** `realtimeConnectionManager.onModeSwitch(from, to)` tears down iff `from==='realtime' && to!=='realtime'` (switch AWAY from realtime → release pc/mic/`<audio>`, no **double-mic** vs cascade); cascade→realtime reconnects lazily (`ensureConnected`). The switch-away rule lives on the manager (store reducer stays PURE, ARCH-007); `ModeToggle` onClick calls it with `(state.mode, value)` + skips a same-mode no-op. +4 web tests (153→157 green); security-reviewer PASS #1–#5 (0 findings). web lesson §18 (mode-switch addendum). Files: `realtimeConnectionManager.ts`(+`.test.ts`), `ModeToggle.tsx`(+`.test.tsx`).
- [ ] **RE-SEQUENCED (backend-dependent, → a backend+frontend slice / when a backend impl cycles in):** `ModeTransitionEvent` emit + persistence (the Flow-G timeline) — backend `SessionStore.RecordModeTransition` is **built but UNWIRED** (no endpoint) + the team is frontend-only this round → needs a backend `POST /api/sessions/{id}/mode-transition` endpoint + the frontend POST + the TS `ModeTransitionEvent` mirror + the `modeTransitions` graduation. **Not deliverable-blocking** (the comparison derives each turn's mode from the per-turn `mode` field, not the timeline). _(See Carry-forward.)_
- [ ] **DEFERRED (G / documented fallbacks):** `expiresAt` re-mint before expiry (60-min cap > 5-min demo) + optional auto-reconnect (≤2, ARCH-010 nice-to-have); the LOW hardenings (`responseDone`+`error` ordering race, `onServerEvent` write-only immutability, SessionSetup teardown-fires-even-if-`endSession`-fails).
- Anchors: `ARCH-010`, `ARCH-017`, `ARCH-018`. **Cross-doc invariant: none** this slice (the `ModeTransitionEvent` mirror/graduation defers with the persistence slice).

### E.6 — Realtime model selection ✅ (2026-05-30 — REALIZED ACROSS D.2/E.1/E.2b/E.3; verify-and-tick, no dedicated slice)
- [x] `OPENAI_REALTIME_MODEL` + UI selector choose `gpt-realtime`/`gpt-realtime-mini`; model recorded on the turn/providerProfile for cost comparison. _(Verified 2026-05-30: **UI selector** = `SessionSetup.tsx:57-63` (`<select value={state.realtimeModel}>` over `models.realtimeModels` from the `ConfigResponse` catalog → `updateSessionConfig({realtimeModel})`), built at D.2; **backend `OPENAI_REALTIME_MODEL` resolve + allowlist** = E.1 (`RealtimeClientSecretService` + `RealtimeModelCatalog`); **the selected model flows to the realtime session** — `realtimeWebRtcClient.ts:227` mints with `model: state.realtimeModel`; **recorded on the providerProfile** (`session.config.providerProfile.realtimeModel`) + **priced** at E.2b (`CostEstimator.EstimateRealtime` keyed by `ProviderProfile.RealtimeModel`). So both `gpt-realtime` and `gpt-realtime-mini` are selectable + comparable.)_
- [x] Files: `Realtime/RealtimeClientSecretService.cs` (E.1), `web/src/components/SessionSetup.tsx` (D.2) — both already shipped.
- [x] Anchors: `ARCH-010`, `ARCH-014`. Cross-doc invariant: none.

### Acceptance criteria (E)
- [x] **All E.X ticked** (E.1·E.2·E.2b·E.3·E.4·E.5·E.6 ✅). The realtime turn path is **built + wired + fake/unit-tested** end-to-end: voice-in→translated-voice-out (WebRTC mic → `ek_` mint → SDP handshake → `response.create` → remote `<audio>`); source + target transcripts (or "source unavailable" when input transcription off); persistent connection across a multi-turn session; mode switch Realtime↔Cascade within one session (teardown, no double-mic); disconnect surfaced (advise-switch, not swallowed). **🟡 The LIVE-observed half — actual audible voice in→out + `speech-end→first-audio < 1.5s` measured against real OpenAI keys — is the ARCH-020 demo-checklist (browser-WebRTC/audio is manual-smoke-exempt per the root TDD posture; never unit-asserted).**

---

## Phase F — Evaluation, Summary & Comparison

**Goal:** Ship the committed standalone WER Evaluation panel + endpoints, and the comparison summary that aggregates by mode and by model variant.

**Spec anchors:** `ARCH-015`, `ARCH-009`, `ARCH-007`, `ARCH-014`, `ARCH-017`.

### F.1 — Evaluation endpoints
- [x] `GET /api/evaluation/phrases`; `POST /api/evaluation/transcribe` (STT-only, no translation/TTS — returns hypothesis + sttProvider/model + latency); `POST /api/evaluation/wer` (returns full `WerResult` incl. normalized fields). _(F.1a `7e29f35` phrases+wer; F.1b `f9cf6a9` transcribe.)_
- [x] **Hypothesis length cap (SECURITY, origin B.6 security review — MEDIUM).** `POST /api/evaluation/wer` MUST cap the request-body hypothesis length (suggest ~2000 chars / ~500 words → `400 evaluation.invalid_phrase`) **before** invoking `WerCalculator.Compute` — the calculator allocates an `n×m` DP matrix, so an unbounded hypothesis is a memory-DoS surface once it's request-reachable. (The calculator itself is safe; the boundary is F.1's job — ARCH-019.) _(F.1a: capped as the service's FIRST action, domain-400 not `[MaxLength]`, unit-pinned via an injected compute delegate; security-reviewer PASS; lesson §27.)_
- [x] **`LoadError` never surfaced (security low, origin B.6 / B.5 family).** `EvaluationPhraseStore.LoadError` (and any `Result.Error`) embeds path/`ex.Message` fragments — the controller exposes only `isLoaded` / a fixed-safe message, never the raw error (pairs with B.8/B.9 sanitizer). _(F.1a: `GET /phrases` degrades to an empty list; no-leak sentinel test.)_
- [x] Files: NEW — `Controllers/EvaluationController.cs`, `Evaluation/EvaluationService.cs`. _(+ `Evaluation/EvaluationModels.cs`; modified `Program.cs`, `Security/ErrorSanitizer.cs`.)_
- [x] Anchors: `ARCH-009`, `ARCH-015`, `ARCH-019`. Cross-doc invariant: extended (DTOs → Appendix A). _(+ ARCH-016 turn-WER persist note, ARCH-018 codes, ARCH-028 `EVAL_MAX_UPLOAD_BYTES`.)_
- [x] **F.1b transcribe (SECURITY, invariant #5/#3).** `POST /api/evaluation/transcribe` validates the audio upload (size+content-type → 413/415) **before** any provider call via the shared `CascadeUploadValidation`; STT-only + stateless (audio never persisted, no turn); joins multiple `SttFinal`s. _(F.1b `f9cf6a9`; security-reviewer PASS; lesson §28.)_

### F.2 — Evaluation panel (standalone, MUST)
- [x] `EvaluationPanel`: phrase selector, reference display, record+transcribe a phrase, WER result, "WER is STT-only" explanation (Flow D). Persists WER on the session. _(F.2 `f1697bc`: select→record (`recordBlob`)→`POST /transcribe`→`createTurn`→`POST /wer` w/ turnId (persists via a dedicated backend-only eval turn); DI'd `evaluationActions` + `evaluationApi`; web lessons §19–§20.)_
- [x] Files: NEW — `web/src/components/EvaluationPanel.tsx`, `web/src/api/evaluationApi.ts`. _(+ `web/src/state/evaluationActions.ts`; modified `web/src/types/domain.ts` (5 mirrors + `TranscribeParams`), `web/src/App.tsx`.)_
- [x] Anchors: `ARCH-007`, `ARCH-015`, `ARCH-017`. Cross-doc invariant: extended (5 TS mirrors + `TranscribeParams` → `web/CLAUDE.md`).

### F.3 — Comparison summary
- [x] `ComparisonSummary`: avg latency by mode, estimated cost/min by mode **and by model variant** (both realtime + both translation models), error counts, WER summary, turn counts. _(F.3 `f7daae1`: per-mode + WER from `GET /summary`; the per-model-variant cost split DERIVED frontend-side from each persisted turn's wire `costEstimate` (`GET /session`) — `ModeSummary` is per-mode only — via a pure `aggregateCostByVariant` util (§13) + the `ComparisonTurn` projection; two sources degrade independently; honest `n/a`; web lesson §21.)_
- [x] Files: NEW — `web/src/components/ComparisonSummary.tsx`. _(+ `web/src/state/comparisonAggregation.ts`, `web/src/state/comparisonActions.ts`; modified `web/src/types/domain.ts` (`ComparisonTurn`), `web/src/App.tsx`.)_
- [x] Anchors: `ARCH-009`, `ARCH-007`, `ARCH-014`. Cross-doc invariant: extended (`ComparisonTurn` projection → `web/CLAUDE.md`).

### F.4 — Eval-turn exclusion from ModeSummary (BACKEND; user decision 2026-05-30, F.2 Q1)
> **Origin:** USER DECISION (relayed by lead, 2026-05-30) on the F.2 eval-turn `turnCount` skew — chose the **clean backend exclusion** over document-only. F.2/F.3 already shipped (`f1697bc`/`f7daae1`, round-seal `407be44`); the frontend is UNCHANGED — F.3 reads `ModeSummary.TurnCount` live from `GET /summary`, so once the backend excludes eval turns, F.3's comparison auto-shows exact counts with **no F.3 rework**. **Must land BEFORE G.5 (the write-up) + the live demo** so the comparison counts are exact. Needs a **backend implementer** (spin a brief backend cycle).
- [x] **Mark eval turns + exclude them from per-mode aggregation.** Add an explicit eval-turn marker to the turn model (design: `IsEvaluation` bool on `InterpretationTurn`, default false). Set it server-side at `POST /api/evaluation/wer` (the eval-defining moment — inside the existing `UpdateTurn` transform that attaches the `WerResult`), so **F.2's frontend stays identical** (it already calls `/wer` with the `turnId`). `SessionSummaryService.SummarizeMode` excludes `IsEvaluation` turns from `ModeSummary.TurnCount` + the per-stage/cost averages; `SummarizeWer` STILL includes them (the WER comes from eval turns — that's the point). _(F.4 `1e8a655`: trailing-defaulted `IsEvaluation`, atomic with `WerResult` in the one `UpdateTurn` transform; `SummarizeMode` filters `&& !t.IsEvaluation` (count+averages+ErrorCount); security PASS; lesson §29.)_
- [x] **Step-2.5 design Qs:** (1) top-level `SessionSummary.TurnCount` **counts ALL** (incl. eval), per-mode excludes — so top-level ≠ sum-of-per-mode (delta = eval turns); pinned by the e2e test. (2) the orphaned-eval-turn (computeWer-failed → unmarked) stays the bounded carry-forward residual (marker-at-`/wer` handles the success path; set-at-create rejected — would need an F.2 signal).
- [x] Files: extended — `Sessions/SessionModels.cs` (`InterpretationTurn` + `IsEvaluation`), `Evaluation/EvaluationService.cs` (`/wer` `UpdateTurn` transform sets the marker), `Sessions/SessionSummaryService.cs` (exclude in `SummarizeMode`). _(`SessionStore.CreateTurn` needed NO change — named args + default.)_
- [x] Anchors: `ARCH-005`, `ARCH-009`, `ARCH-013`. **Cross-doc invariant: CHANGED** — `InterpretationTurn` gains `IsEvaluation` → `ARCHITECTURE.md` ARCH-005 + Appendix A + ARCH-013 semantic note + `server/CLAUDE.md` ARCH-005 row (written this round). No `web/` mirror change (the wire turn is opaque frontend-side; F.3 reads `costEstimate`/`turnCount`, not `isEvaluation`). Persistence sentinel still PASS (a bool is benign).

### Acceptance criteria (F)
- [x] WER panel returns a score for a scripted phrase and persists it. Summary compares modes and model variants from real session data. Cost panel reflects the model used. _(F.1 endpoints `7e29f35`/`f9cf6a9`; F.2 panel `f1697bc`; F.3 comparison `f7daae1`. Backend 339 green · web 193 green. **Phase F core (F.1–F.3) COMPLETE** — round-seal `407be44`.)_ Live-observed acceptance (real-key WER score + populated multi-mode/multi-variant comparison) is part of the G demo-checklist (ARCH-020-exempt).
- [x] **F.4 (eval-turn exclusion) landed `1e8a655`** so the comparison turn counts are exact (user decision 2026-05-30). Backend 339→345 green; F.3 frontend unchanged (auto-corrects). **PHASE F FULLY COMPLETE.**

---

## Phase G — Docs, Demo, Stability & Write-up

**Goal:** Make the project runnable, demoable, stable for 5 minutes, and produce the required documentation + comparison write-up.

**Spec anchors:** `ARCH-021`, `ARCH-023`, `ARCH-007`, `ARCH-010`, `ARCH-013`, `ARCH-014`, `ARCH-015`, `ARCH-019`, `ARCH-020`, `ARCH-029`, `ARCH-022`.

### G.1 — README
- [x] Overview, architecture summary, local setup (clean clone → run), env vars (ARCH-028), run commands, demo script link, provider config, metric meanings, **cost-estimate disclaimer**, WER explanation, known limitations, secure-context note, "session JSON is sensitive — don't commit". _(Orchestrator-authored `README.md` — all 12 spec'd elements present; the demo-script link forward-points to `docs/DEMO_SCRIPT.md` (G.3, smoke-gated).)_
- [x] Files: NEW — `README.md`.
- [x] Anchors: `ARCH-023`, `ARCH-021`, `ARCH-029`, `ARCH-019`. Cross-doc invariant: none.

### G.2 — CLAUDE.md / AGENTS.md
- [x] How the agent was directed; architecture-first workflow; constraints (no secrets to frontend; no raw audio; preserve provider interfaces; scoped commits). _(The AI-collaboration NARRATIVE authored as `docs/AI_COLLABORATION.md`. **USER-CONFIRMED FORM 2026-05-30:** keep it as the separate build-narrative artifact cross-referencing the existing operational `CLAUDE.md`/`AGENTS.md` (root + `server/` + `web/`) — do NOT fold/clobber. README front-door links it + the comparison write-up.)_
- [x] Files: NEW — `docs/AI_COLLABORATION.md` (the operational `CLAUDE.md`/`AGENTS.md` pre-exist as the agent-instruction files and stand as-is; user-confirmed).
- [x] Anchors: `ARCH-023`. Cross-doc invariant: none.

### G.2b — Backend `.env` auto-loader (FINDING, deliverable correctness; origin 2026-05-31 smoke-prep)
> **FINDING (lead, 2026-05-31 — during the real-key smoke prep):** the backend has **NO `.env` loader** — no `DotNetEnv`, no `Env.Load()` in `Program.cs`; .NET reads only **process** env vars. So `cp .env.example .env && dotnet run` leaves all keys unloaded (`GET /api/config` → `configured:false` everywhere), which **breaks "clean clone runs from README"** (ARCH-021/029). The lead unblocked the live smoke by sourcing the env (`set -a && source ../../.env && set +a && dotnet run`).
- [x] **(a) Doc-correctness fix (DONE 2026-05-31):** the runbook §2 + README §2 start steps now use the working `set -a && source ../../.env && set +a && dotnet run` form (+ a note). So the README/runbook are correct **now**, before (b).
- [ ] **(b) Backend auto-loader (small slice — backend impl, post-smoke):** add `DotNetEnv` to `AiInterpreter.Api` + `Env.Load()` at startup **Development-only** (guard on `ASPNETCORE_ENVIRONMENT==Development`; production uses real env / Secrets Manager per ARCH-022) so a plain `dotnet run` auto-loads `.env`. `.env` is already gitignored. **On (b) landing, simplify the README/runbook back to a plain `dotnet run`.** _(Not dispatched now — backend impl takes it after the smoke. Small; no cross-doc invariant. Lead-recommended over (a)-only.)_
- [ ] Anchors: `ARCH-028`, `ARCH-029`. Cross-doc invariant: none.

### G.3 — Demo script
- [ ] `docs/DEMO_SCRIPT.md` per `ARCH-021` (2 Realtime + 2 Cascade EN→ES, switch direction, 1+1, switch translation model + repeat a cascade turn, 1 WER, show summary, open JSON, explain tradeoffs); include the suggested EN/ES phrases.
- [ ] Files: NEW — `docs/DEMO_SCRIPT.md`.
- [ ] Anchors: `ARCH-021`. Cross-doc invariant: none.

### G.4 — Stability validation + manual preflight
- [ ] Run the `ARCH-020` manual preflight; resource-cleanup discipline verified (stop tracks, single reused `AudioContext`, drained playback, disposed pc); **5-minute run: (1) no disconnect, (2) no audio drift/overlap, (3) no memory leak**. Log results.
- [ ] Runtime smoke for the PRD mic-denied path: deny mic permission → UI shows the recovery hint and Start is disabled (ARCH-018).
- [ ] **Backend code-hygiene cleanups (opportunistic; origin F.1 review LOWs — none a current bug).** (a) `EvaluationService.SttLanguage = language.ToString().ToLowerInvariant()` is BCP-47-fragile for a future enum member (e.g. `ZhCn` → `"zhcn"`, invalid) — make the enum→BCP-47 mapping explicit when a 3rd language pair lands (origin: 2026-05-30 F.1b). (b) Fold `evaluation.invalid_request` into `ErrorSanitizer.SafeMessageForCode`/`ForCode` (currently inline, matches the `CascadeController` precedent — a both-controllers tidy). (c) Extract the duplicated `OneFrameAsync` (`EvaluationService` + `CascadeOrchestrator`) to a shared util on the 3rd caller. _(Fold into any slice already touching these; not worth a dedicated slice.)_
- [ ] Files: extended — frontend audio/realtime cleanup paths as needed; `server/AiInterpreter.Api/Evaluation/*` for the hygiene cleanups.
- [ ] Anchors: `ARCH-020`, `ARCH-007`, `ARCH-010`. Cross-doc invariant: none.

### G.5 — Comparison write-up
- [~] `docs/COMPARISON_WRITEUP.md` (1–2 pp) using **real measured values** from session JSON: what was built; measurement method + limitations (cross-clock skew, backend-measured TTS timing, estimate-not-billing cost, English-leaning ES TTS voice); Realtime vs Cascade latency/cost/quality(WER)/controllability; **recommendation (when each fits)**; the **time-to-onboard a new language pair** (PRD impact metric — a config/provider-capability change in Cascade vs a model-capability question in Realtime); limitations + next steps. _(SCAFFOLDED — replaced the A.1 placeholder; structure + methodology + the full limitations section + the recommendation/language-pair frameworks are written; every measured number is a `[SMOKE: …]` placeholder. **Real numbers HELD pending the real-key smoke run** (user gate); F.4 lands first so §3.3's turnCount is exact.)_
- [ ] **Limitations to call out explicitly (accreted, route hot from F):** (a) **Cascade end-to-end latency (speechEnd→playback) is `n/a`** — the cascade WS has no client→server latency-report channel (realtime's `POST …/events` has no cascade analogue), so the comparison shows it `n/a` (honest degradation; the cascade per-stage backend latencies + realtime end-to-end ARE shown; origin D.5/D.6, last-consumer F.3). (b) the standing C/D real-key-smoke + realtime-GA-shape confirms (demo-checklist). _(NOTE — the per-mode `turnCount` eval-turn skew is **NOT a write-up limitation**: it is **FIXED by F.4** (user-requested must-do — `ModeSummary` excludes eval turns → exact counts), which lands before this write-up. The only residual is a rare computeWer-failure orphan, bounded. Do not list turnCount as a limitation once F.4 is in.)_
- [ ] Files: NEW — `docs/COMPARISON_WRITEUP.md`.
- [ ] Anchors: `ARCH-023`, `ARCH-013`, `ARCH-014`, `ARCH-015`. Cross-doc invariant: none.

### G.6 — Optional deployment notes (trim/nice)
- [ ] `ARCH-022` deployment notes only if local demo is stable + time remains; HTTPS requirement called out.
- [ ] Files: extended — `README.md` or `docs/`.
- [ ] Anchors: `ARCH-022`. Cross-doc invariant: none.

### Acceptance criteria (G)
- [ ] All G.X (except G.6 optional) ticked. Clean-clone run succeeds via README. Demo script runs through. 5-minute stability passes all three checks. Write-up makes a defensible, evidence-backed recommendation.

---

## Phase H — UI/UX Styling & Polish (USER-REQUESTED, 2026-05-31)

> **Origin:** USER-REQUESTED scope addition (relayed by lead, 2026-05-31), surfaced by the real-key smoke. The frontend is **functionally correct but has NO CSS** — visually raw + gives near-zero feedback (e.g. no visible active-mode state → the user thinks the mode toggle is broken). The user wants it to "look good with good UX" and explicitly to "expand on it later." **TDD posture:** visual styling is **MANUAL-SMOKE-EXEMPT** (like mic/WebRTC — root CLAUDE.md TDD posture) — H slices are verified **visually**, NOT unit-TDD'd (a styling brief, not a `/tdd` brief). **Clean-separation invariant holds (ARCH-007):** components still render only from the store; H is **CSS/layout/markup-for-styling only — no logic changes**.

**Spec anchors:** `ARCH-007` (frontend clean-separation — H must not violate it), `ARCH-013`/`ARCH-014`/`ARCH-015` (the metric/cost/WER panels H makes legible). _(H is a UX layer over the shipped behavior; it implements no new architecture.)_

### H.1 — First usability/visual pass (MUST — drives the demo)
- [ ] A global **design-system/CSS baseline**: CSS custom properties (color/spacing/typography scale), an app-shell **layout** (header + mode toggle + the panels grid), and visual hierarchy — enough to demo confidently.
- [ ] **Visible state feedback (the headline gap):** the **active mode** is unmistakably shown (the `ModeToggle` already sets `aria-pressed` — style it so the selected mode is visually obvious); **session status** (configured / active / ending / ended) + **turn status** (idle / recording / processing) render as clear visual cues; the **ErrorBanner** is styled as an error.
- [ ] **Styled controls + panels:** buttons (Start/Stop/End/Record), the model/direction `<select>`s, and the Transcript / Metrics / Cost panels are legible + visually grouped.
- [ ] **EvaluationPanel + ComparisonSummary made legible:** the WER result (% / S·I·D·N) + the by-mode/by-variant comparison grid read as proper tables/cards, not raw text.
- [ ] Files: NEW — a CSS baseline (`web/src/styles/` or `App.css` + component CSS — H.1 Step-2.5-equivalent design Q); MODIFIED — components gain `className`s / minimal semantic wrappers (NO logic change; the clean-separation invariant + the existing component tests stay green).
- [ ] **Verification: MANUAL SMOKE** (not unit tests) — a visual checklist (active-mode obvious, status cues visible, panels legible, the demo flow looks presentable) + confirm `npm run test`/`typecheck`/`lint` stay green (no logic/contract regression). Anchors: `ARCH-007`, `ARCH-020` (manual-smoke tier).

### H.2+ — Deeper polish (LATER — user "expand on it later")
- [ ] Responsive layout, fuller a11y pass (focus states, contrast, keyboard nav), motion/transitions, empty/loading states, and any design-system refinement. _(Scoped later per the user; H.1 is the demo-ready baseline.)_

### H.3 — Session history / review view (USER-REQUESTED FEATURE, 2026-05-31; sequence AFTER the smoke bug-fixes)
> **Origin:** USER-REQUESTED feature gap (relayed by lead, 2026-05-31): "nowhere in the dashboard to review previous sessions + their results." The app persists every session to `data/sessions/*.json` (ARCH-016) but has **no UI to browse past sessions**. This is a NEW FEATURE, not a bug. **Sequencing:** dispatch AFTER the smoke bug-fixes (Findings 1–3 / briefs 048–049) land — the currently-persisted sessions hold the GARBLED metrics (negative latency, mislabeled mode); once the NRE + the metrics-stamping/label/mode fixes land, the history view shows VALID data. **⬆ PULLED UP (2026-05-31 — the user asked twice):** now sequenced **right AFTER the active cascade/realtime fixes land** (052 empty_transcript + 050-backend mode-switch + 053 realtime transcript/metrics) — and **AHEAD of** the metrics-polish + G.3/G.5 + Phase I. Rationale: it was queued behind the bug fixes because persisted sessions held garbled/failed data; once turns are clean, the history view shows valid results. **Author the brief when those fixes clear** (don't dispatch mid-052); it pairs with the 2c mode-transition timeline for richer review.
- [ ] **(a) Backend — list-sessions endpoint.** A `GET /api/sessions` index is **NOT present today** (confirmed: the route manifest has `GET /api/sessions/{id}` but no list/index endpoint). Add a lightweight list endpoint returning a **no-secret / no-audio** session-summary list (e.g. `sessionId`, `label`, `startedAt`, `endedAt?`, mode(s), `turnCount`) — same safety shape as the rest (invariants #1/#3; enumerate `SESSION_DATA_DIR` / reuse `SessionStore`). Anchors: `ARCH-009`, `ARCH-016`, `ARCH-019`.
- [ ] **(b) Frontend — history panel/view.** List past sessions → select → render that session's summary + turns by **reusing the existing store-driven panels** (Metrics/Cost/Comparison/Transcript — ARCH-007). Load a selected session via `GET /api/sessions/{id}` (+ `/summary`). New view/route vs a panel-in-shell is a brief-time design Q. Anchors: `ARCH-007`, `ARCH-009`.
- [ ] **Verification:** TDD the backend list endpoint (deterministic — list shape + the no-secret/no-audio sentinel + empty-list); the frontend list+select is manual-smoke + component tests where deterministic (selection → renders the right session).
- [ ] **Dependencies:** AFTER Findings 1–3 (valid persisted data). Cross-area (backend list endpoint + frontend view) — needs both impls.

### Acceptance criteria (H)
- [ ] H.1: the app **looks presentable + gives clear UX feedback** (active mode, session/turn status, legible panels) — verified by manual smoke; the demo can be driven confidently. No logic/contract change (clean-separation held; suites green).

---

## Phase I — Auto-VAD / continuous turn detection (USER-DIRECTED, 2026-05-31)

> **Origin:** USER-DIRECTED scope addition (relayed by lead, 2026-05-31). **Decision:** BOTH modes get auto-VAD, but a **manual Start/Stop mode stays as a user-selectable toggle** — auto for hands-free, **manual for precise latency measurement**. **NO Pipecat** (it's Python; our backend is .NET — VAD is done natively). **Architecture:** this REVISES the locked ARCH-003 turn-based decision → *"turn-based (manual) OR auto-VAD (continuous), user-selectable."* The user **APPROVED this as a directed change (NOT an escalation)**; the orchestrator writes the ARCH-003 revision + the ARCH-010/011/007 realization notes WHEN THE PHASE IS AUTHORED.
> **Sequencing:** dispatch AFTER the bug-fix arc (051/050/empty_transcript) + the real-key smoke (clean numbers) + G.3/G.5/H.3. Logged now so it's not lost; **NOT dispatched.**

**Spec anchors (when authored):** `ARCH-003` (REVISED — manual OR auto-VAD, user-selectable), `ARCH-010` (realtime `turn_detection`), `ARCH-011` (cascade endpointing), `ARCH-007` (capture-mode toggle UI), `ARCH-013` (the heuristic-speech-end measurement note).

### I.1 — Realtime server-VAD (auto option) [~1 slice, frontend]
- [ ] Enable OpenAI Realtime **server-VAD** as an auto option (`turn_detection: server_vad`/`semantic_vad` instead of the current `null`); the realtime client handles auto-triggered responses instead of the manual `input_audio_buffer.commit` + `response.create` (refines lesson §17's buffer-delimited manual turn). Anchors: `ARCH-010`.

### I.2 — Cascade endpointing (auto option) [~few slices, backend + frontend]
- [ ] Drive cascade turn boundaries off **Deepgram endpointing** (`utterance_end_ms`/`speech_final` — already partially wired in `DeepgramSttProvider`) with CONTINUOUS mic streaming, instead of the Stop button. Backend: emit turn-complete on endpoint; frontend: continuous capture + auto-turn lifecycle. Anchors: `ARCH-011`.

### I.3 — Capture-mode toggle (manual ⟷ auto) [frontend]
- [ ] A user-selectable **capture-MODE toggle**: **manual** (the current precise Start/Stop — the measurement path) ⟷ **auto** (hands-free VAD). Distinct from the Realtime/Cascade *interpretation*-mode toggle. Anchors: `ARCH-007`.

### I.4 — Measurement + comparison notes (→ G.5) [docs]
- [ ] Document (G.5): auto-VAD's speech-end is **heuristic** (VAD-detected) vs manual's exact Stop-click — so the `speechEnd→firstAudio` metric's start point is VAD-ESTIMATED in auto mode (manual stays the precise-measurement reference). ALSO: **hands-free (auto) vs push-to-talk (manual)** enriches the **controllability** comparison axis — a plus for the write-up. Anchors: `ARCH-013`.

### Acceptance criteria (I)
- [ ] Both modes support auto-VAD (realtime server-VAD; cascade endpointing) with continuous mic; the **manual Start/Stop path is preserved + user-selectable**; the measurement caveat + the controllability-axis enrichment are documented (G.5); ARCH-003 revised + the ARCH-010/011/007 realization notes written.

---

## Trims / Nice-to-Haves Catalog

Deferred items with come-back guidance (from `ARCH-025`):

- **Sub-utterance incremental translation** — translate mid-segment rather than per finalized STT segment. Belongs in `Cascade/CascadeStreamingOrchestrator.cs`; needs the STT→translation handoff rule changed + new metrics; cross-doc impact ARCH-011/ARCH-013.
- **Atomic persistence (temp→rename) + per-WER write granularity** — `Sessions/SessionPersistenceWriter.cs`; add durability tests. MUST stays write-on-end + best-effort per-turn.
- **Realtime auto-reconnect** (≤2 attempts; re-mint + rebuild) — `web/src/realtime/realtimeWebRtcClient.ts`; default is detect+advise-switch.
- **Optional AWS deployment** (G.6) — `ARCH-022`.
- **Second real provider per stage / DeepL / ElevenLabs / Deepgram Aura** — provider abstractions already allow it; add a new `I*Provider` impl + config.
- **Nice-tier metrics** (`stt.first_partial` on fallback, etc.) degrade to `n/a`.

**Fallbacks (use only if blocked — not trims):** Realtime backend WebSocket proxy (if WebRTC blocked); Cascade blob + Deepgram pre-recorded (if live streaming blocked) — already built in C.5 as the documented fallback.

---

## Decisions tabled

Owner decisions are **resolved** (reflected in ARCHITECTURE.md): full streaming cascade (MUST); both Realtime models; full standalone WER panel; both translation models switchable. Remaining are **build-time confirmations** (ARCH-027 §16), none blocking:

- Confirm `gpt-realtime`/`gpt-realtime-mini` access in the target OpenAI account (E.1).
- Confirm Realtime input-transcription deltas with the chosen config; else degrade source-transcript UI (E.4).
- ✅ **RESOLVED (translation half) at 051 (2026-05-31):** `pricing.json` translation rates are now real + on-key — `gpt-5-nano` 0.05/0.40, `gpt-5-mini` 0.25/2.00, version `2026-05-31-payg-estimates` (no `0.0` placeholder; a guard test couples catalog↔pricing↔non-zero estimate). **Still open:** the realtime audio-seconds→token conversion factor (A.4) — `CostEstimator.RealtimeTokensPerAudioSecond = 50` is the working estimate (commented + surfaced in every realtime estimate's `Assumptions`); confirm against OpenAI realtime audio-token billing before relying on realtime cost numbers.
- Confirm capture format/no-transcoding on target browsers (C.1/D.3).

---

## Log

_(Append-only, date-stamped.)_

- **2026-05-28 — Phase A COMPLETE** (`ef05ccb` → A.5). Stood up: .NET solution + Vite scaffold (A.1); provider config Options + `.env.example` (A.2); ARCH-005 domain model + shared `JsonDefaults` + Clock/Result (A.3); ARCH-014 pricing + degrade-safe loader (A.4); ASP.NET host wiring — DI/env-bridge/JSON/CORS/WebSockets/port 5179/`GET /api/health` (A.5). Backend builds 0W/0E (WAE), **28 tests green**, real host serves `/api/health`. Lessons §1–§4 banked; cross-doc invariants (Options + domain models + pricing) registered in Appendix A + `server/CLAUDE.md`. Re-sequenced to later phases: TS mirror types + Vite dev-config → D.1; global sanitizing exception handler → B.9 (safety, no current exposure); cascade WS `Origin` validation → C.4. Commit cadence: commit-as-we-go on `main`, local-only (no remote). **Next: Phase B** (backend core seams + tests against fakes).

- **2026-05-28 — Phase B opened (B.1–B.2); team wound down at B.2 (context cycle).**
  - Planning-level: provider contracts + `ProviderErrorMapper` (B.1) and the three fake providers (B.2) landed against the B.1 interfaces — the deterministic substrate the rest of Phase B builds + tests against. **24 commits across A.1→B.2; 50 tests green; runnable host.**
  - Decisions: the error-mapper is the single owner of the ARCH-012 table with `SafeMessage` never echoing `ex.Message` (lesson §5); fakes emit **real** ARCH-012 error codes (not invented `*.failed`); the paced-async-iterator streaming-fake pattern (lesson §6).
  - Scope shifts: none new — earlier re-sequences hold (TS types + Vite → D.1; B.9 exception-handler + C.4 WS-`Origin` + B.7 `TtsAudioChunk` cross-check inlined to their phases).
  - **Cycle:** impl context hit **ACTION (76%)** at the B.2 boundary → **full team wind-down** (no successors spawned; the human restarts the team fresh). Clean break — nothing in flight, B.2 fully committed + documented.
  - Next session target: **B.3 (latency model + `MetricsAggregator`)**.
  - Reference: implementer session doc `001-2026-05-28-phase-a-plus-b1-b2.md`; briefs `001`–`007`; lessons §1–§6.

- **2026-05-28 — Phase B core seams (B.3–B.6) landed; crash-recovered round close.**
  - Built the four mid-Phase-B deterministic seams against fakes: metrics/latency layer (B.3 `620f542`), streaming cascade orchestrator (B.4 `9b679b1` — CRITICAL, the spec centerpiece), cost estimator (B.5 `af40aaa`), WER calculator + scripted-phrase store (B.6 `edcbacd`). Session doc `002` (`88049bb`). **50 → 92 tests green** (B.3 +8, B.4 +9, B.5 +12, B.6 +13).
  - Decisions: aggregate metrics from absolute `Timestamp` (cross-clock safe), `relativeMs` is display-only, factory-clamps-but-aggregator-doesn't (lesson §7); cascade = nested per-segment loop with per-event idle-timeout + OCE-filter caller-cancel split (lesson §8); cost branches on basis, a `0.0` rate ≠ absent config, cascade → one composite estimate (lesson §9); WER normalize + DP-backtrace S/I/D, unbounded, empty-ref precondition (lesson §10).
  - Hot-routed this round: ARCH-013 metric-origins · ARCH-012 STT-idle-timeout · ARCH-011 stage-label · ARCH-014 estimator-conventions · ARCH-015 WER-note · ARCH-028 `EVALUATION_PHRASES_PATH`; lessons §7–§10 + four `server/CLAUDE.md` index rows. Two precondition Findings folded into phase tasks: **C.4** `encoding` allowlist (MEDIUM) + **F.1** hypothesis-length cap (MEDIUM); related sanitizer-lows into B.8 / F.1. B.3 cross-doc invariant corrected to **none** (consumes existing A.3 `LatencyEvent`).
  - **Crash recovery:** predecessor orchestrator died mid-`/orchestrate-end` (machine shutdown). Slice commits (`620f542`/`9b679b1`/`af40aaa`/`edcbacd`) + session doc (`88049bb`) were already committed; only the round-seal (doc reconciliation) was outstanding. Successor verified the predecessor's uncommitted edits complete + correct, advanced Currently-in-progress → B.7a, appended this entry, and sealed the round.
  - Next session target: **B.7a (session store + persistence writer + sentinel — SAFETY)**; brief `012` drafted.
  - Reference: implementer session doc `002-2026-05-28-phase-b-b3-b6.md`; briefs `008`–`012`; lessons §7–§10.

- **2026-05-29 — Phase B COMPLETE (B.7a–B.10); fresh team, planned Phase-B-close cycle.**
  - Drove the fresh team from B.7a through Phase-B exit: B.7a session store + persistence writer + **safety sentinel** (`9bfea76`), B.7b summary service (`2d6a591`), B.8 error sanitizer + `UiError` (`0262ce1`), B.9a global sanitizing exception handler (`88a7f3e`), B.9b ConfigController (`278caf2`), B.9c-i session-lifecycle endpoints (`e39ec7b`), B.9c-ii turn-lifecycle endpoints (`9e0066b`), B.10 provider-boundary contract suite (`759cac2`). Session doc `003` (`30e2ba7`). **92 → 161 tests green** (full `dotnet test` verified at close; build 0W/0E; format clean). **Phase A + Phase B COMPLETE** — full session/turn/config HTTP surface + global sanitizing handler wired + reachable; all deterministic backend seams tested against fakes (no real keys).
  - Decisions: **B.9 split a/b/c-i/c-ii** for safety-commit isolation (two distinct safety invariants — #4 global handler, #1 config — can't share a commit; the controller is large + session-CRUD vs turn-lifecycle separable). Four **safety slices** (B.7a/B.8/B.9a/B.9b), each a mandatory `security-reviewer` PASS; **no safety finding escalated all round**. `UiError` backend record (ARCH-007 mirror) with server-only `[JsonIgnore] HttpStatusCode`; `ErrorSanitizer` safe-by-construction + log-forgery single-lining + `ForCode` for expected conditions; `/end` MUST-persist vs `/complete` best-effort (both 200-with-`persistenceWarning`, never 500); `PersistedPath` filename-only; controller `Result`→DTO never-serialize boundary; DataAnnotations on record **parameters**; `/complete` realtime-only (cascade persists via C.4 WS). Orchestrator caught + trimmed its own over-broad `CompleteTurnRequest` proposal at Step 2.5 (removed cost/WER/cascade fields — invariant #5 + ARCH-014 + realtime-only).
  - Hot-routed this round: **lessons §11–§17** (+ 7 `server/CLAUDE.md` index rows); ARCH-016 (filename/overwrite-in-place/sentinel-scope), ARCH-009 (summary pure-compute; session+turn endpoint realizations + `CompleteTurnRequest` spec-gap closed), ARCH-018 (sanitizer boundary + global handler + `session.not_found`/`turn.not_found` codes), Appendix A (`UiError` backend record + session/turn DTOs); `UiError` cross-doc row. Briefs `013`–`019` authored.
  - Scope shifts: none cut. Carry-forward triaged (see Step-5.5 below). The C.4 SECURITY-MEDIUM set + F.1 WER-cap remain inlined in their phase tasks.
  - New blockers: none. Build-confirms (`gpt-5.4-mini` pricing, realtime token factor) still pending real billing (C/E), non-blocking.
  - Next session target: **C.1 (`DeepgramSttProvider` — live WS + pre-recorded fallback)** — Phase C, real providers (highest-risk integration #1).
  - Reference: implementer session doc `003-2026-05-29-phase-b-b7-b10.md`; briefs `012`–`019`; lessons §11–§17.

- **2026-05-29 — Phase C real providers + cascade WS wired end-to-end (C.1/C.6/C.2/C.3/C.4a); fresh team, context auto-cycle at C.4a→C.4b.**
  - Drove the fresh Phase-C team through the three real cascade providers + the streaming WS integration: **C.1** Deepgram STT (`3e5e899`), **C.6** Deepgram error-status fidelity (`f69a366`), **C.2** OpenAI translation (`fffa4c1`), **C.3** OpenAI TTS (`19acc15`), **C.4a** cascade WS endpoint (`accd567` SECURITY encoding-allowlist + `fff1ec2` endpoint). Session doc `004` (`0d8f788`). **Suite 161→211 green** (+50; build 0W/0E; format clean). **Phase C's real cascade now streams end-to-end** — `WS /api/cascade/stream` drives the three real providers through the **unchanged** B.4 orchestrator (seam proven); C.1/C.2/C.3 production-reachable.
  - Decisions made: each real provider = a thin manual-smoke transport shell over a pure unit-TDD'd mapper (real network exempt per ARCH-020; deterministic surfaces TDD'd) — lessons §18 (real-provider pattern), §19 (SDK-hidden-status recovery), §20 (raw-HTTP/SSE + binary-chunk streaming), §21 (cascade WS endpoint). The "verify SDK/API surfaces by reflection, not docs" discipline caught a load-bearing reality on all three providers (Deepgram `err_code` string + 5xx `error_code` key; OpenAI nested `reasoning.effort`/no-`[DONE]`/snake_case usage; TTS `stream_format:"audio"` required) — each a would-be silent runtime failure. C.4a kept the orchestrator unchanged except minimal stamps (`turn.completed`→Overall, `translation.final` Metadata carries tokens for cost — FORK-1a). DI swap real↔fake by key-presence + `USE_FAKE_PROVIDERS`. `LatencyStage.Overall` added (cross-doc).
  - Scope shifts: **C.6 pulled forward (user decision)** — the Deepgram error-status fidelity hardening was sequenced AHEAD of C.2 (error fidelity feeds G.5; recover HTTP status from the SDK `err_code` → vendor-agnostic `MapStatus`, reused by C.3's input-cap guard). **C.4 split** into C.4a (core, landed) + **C.4b** (boundary hardening — bounded channel [firm], Origin, ContentType clamp, double-end, stream-without-terminal, start-frame validation, cost-accumulation unit-pin) — C.4b queued for the successor. Accepted residuals (documented, not silent): Deepgram **5xx-with-body→`stt.unknown`** (different JSON key; `ErrMsg`-match rejected as fragile); cascade **TTS cost target-char proxy** (audio-mode reports no usage) → G.5 disclosure + ARCH-014 note.
  - Hot-routed this round: **lessons §18–§21** (+4 `server/CLAUDE.md` index rows); **`LatencyStage.Overall`** → ARCH-005 + Appendix A; **`ProviderErrorMapper.MapStatus`** → Appendix A + ARCH-012 note; cascade WS DTOs → Appendix A; ARCH-011 (STT/TTS realizations, empty-transcript ownership, stage-label resolved), ARCH-013/014 (cascade cost-usage sourcing + TTS char-proxy), ARCH-009 (WS realization). Briefs `020`–`024` authored. Carry-forward triaged (see Step-5.5 in this commit).
  - New blockers: none. **First-real-key smoke still pending** for the cascade WS path + OpenAI usage-nesting confirm (C.4/D demo checklist) — non-blocking (logic is fake-tested; real network exempt per ARCH-020).
  - Next session target: **C.4b (cascade WS-boundary hardening)**; then **C.5 (blob fallback)** closes Phase C. Fresh team resumes at C.4b.
  - Reference: implementer session doc `004-2026-05-29-phase-c-c1-c4a.md`; briefs `020`–`024`; lessons §18–§21.

- **2026-05-29 — PHASE C COMPLETE (C.4b WS hardening + C.5 blob fallback); fresh team, user-directed backend round-seal + Phase-D transition.**
  - Drove the fresh Phase-C-close team through the last two backend slices: **C.4b** cascade WS-boundary hardening — `b226fe9` (robustness) + `4165551` (SECURITY: bounded `Channel<AudioFrame>` + `Origin` validation); **C.5** cascade blob fallback — `88bac2b` (SECURITY: upload validation, invariant #5) + `49ec08d` (`POST /api/cascade/turn` — closes Phase C). Session doc `005` (`1944d02`). **Suite 211→284 green** (+73; build 0W/0E; format clean). **Phase C is COMPLETE** — both cascade transports (`WS /api/cascade/stream` streaming + `POST /api/cascade/turn` blob) wired, hardened, persisting, tested vs fakes through the **unchanged** B.4 orchestrator. Phases A + B + C done.
  - Decisions made: **C.4b** — bounded-channel-with-backpressure + **writer-complete-on-`Done`** (bounding created a post-terminal pump-park regression, caught by code-quality review); Origin-validation-at-endpoint (WS bypasses CORS); fail-closed on a terminal-less real stream (`<stage>.unknown` via a new non-exception `ProviderErrorMapper.Unknown` factory); idempotent terminal completion at the store (`FinalizeTurn`) routed through **both** the WS + HTTP `/complete` paths; honest rejection-code split (`invalid_audio`/`invalid_request`/`forbidden_origin`); the pump's OCE swallow kept (evidence-backed — rethrow risks an unobserved task exception). **C.5** — the blob fallback is a **thin adapter reusing the unchanged streaming orchestrator** (pre-recorded STT via container-encoding routing; collect the same event stream into a JSON turn); **strip MIME params before the upload allowlist** (`audio/webm; codecs=opus` is the MediaRecorder primary format — a review catch that would have 415'd all of Phase D); the blob **does not price STT** → degrade honestly, **no processing-wall-clock proxy** (a ~30× undercount = a synthetic metric the streaming-honesty posture forbids; the WS path prices fully).
  - Commit-packaging: **2 commits per slice, not 3** (the C.4b bounded-channel + Origin both live in `CascadeWebSocketEndpoint.cs`; the C.5 body-limit shares `Program.cs` with the orchestrator DI) — whole-file staging can't split them, so the SECURITY surface is isolated in one reviewable commit each (orchestrator commit-cadence call, lead-informed; honors "don't over-split").
  - Hot-routed this round: **lessons §22 (WS-boundary hardening) + §23 (blob fallback)** (+2 `server/CLAUDE.md` index rows); **Appendix A** `CascadeTurnResponse`; **`CASCADE_MAX_UPLOAD_BYTES`** → ARCH-028 + `.env.example`; ARCH-018 (`cascade.invalid_request` + `cascade.forbidden_origin`), ARCH-011/018 (fail-closed + `Unknown` factory), ARCH-019 items 10–11 (Origin-at-endpoint + WS field caps) + item 8 (upload allowlist + body bound), ARCH-014 (blob-STT-cost limitation → G.5 disclosure). Briefs `025`–`026` authored. **No cross-doc ARCH-005 record field changed** (the new types are area-local/non-serialized except `CascadeTurnResponse`). Carry-forward triaged (1 deleted, 1 added, 1 updated; 6 kept/spread to D/E/G).
  - Scope shifts: none cut. **C.6/C.4b LOW residuals** remain open at Phase-C exit (documented, G.4-adjacent — live-WS unbounded `Channel<SttEvent>`, Connect/Stop race, `StartFrameDto.Type` validation, TOCTOU live-view read). **Tracked enhancement:** FORK-1a `SttFinal.Duration` → blob STT cost.
  - New blockers: none. **Real-key smoke** (both cascade paths + OpenAI `usage`-nesting) = the C/D demo-checklist (non-blocking, ARCH-020 exempt).
  - Next session target: **D.1 (app shell, API clients, state store)** — Phase D, **frontend (`web/`)**; a **fresh frontend team** cycles in (orchestrator + `web/` implementer). D.1 re-sequences in the TS mirror types (from A.3) + the Vite dev-config; the frontend consumes the documented ARCH-009 / Appendix A backend surface.
  - Reference: implementer session doc `005-2026-05-29-phase-c-c4b-c5.md`; briefs `025`–`026`; lessons §22–§23.

- **2026-05-29 — Phase D D.1–D.5 COMPLETE: the frontend cascade path ships end-to-end (fresh frontend team).** Round sealed by this `docs(tasks)` commit.
  - Planning-level: a single `web/` implementer session built the **entire Phase-D cascade frontend** in five slices — D.1 foundation (types/store/clients/shell + Vite dev-proxy + Flow-A config bootstrap), D.2 session-setup + config-gating selectors + DI'd session actions, D.3 capture controller (linear16 streaming + blob fallback), D.4a cascade WS client + streaming store actions + 4 wire types, D.4b recording controls + the orchestration wiring capture→WS→store, D.5 playback controller + onAudio wiring. **The cascade now runs end-to-end in the browser** (capture → WS → live transcripts/per-stage-latency/cost → audible TTS). Web suite 0→79 green; security-reviewer invariants #1–#5 PASS every slice; every code-quality HIGH/MEDIUM fixed in-slice.
  - Decisions made: **split D.4 into D.4a/D.4b** (transport+store first, orchestrating component second — per the C.4a/C.4b precedent; large interlocking surface). **Pragmatic-accrete TS mirroring** (mirror only what each slice consumes; opaque markers for not-yet-consumed wire types — tightened as slices land). **Backend owns voice resolution** — the frontend sends a blank `ttsVoice` so `ResolveVoice` picks by target language (verified against `OpenAiTtsMapping.cs`). **Transport clients TDD'd against an injected fake `WebSocket`** (full lifecycle, not just pure helpers) — surfaced the real lifecycle bugs (error-frame double-fail, socket-leak-on-restart, stop-on-CONNECTING).
  - Hot-routed this round: **web lessons §2–§12** (+ index rows) — external store, base-URL-agnostic clients + single `http` failure boundary, Vitest fetch-mock mechanics, config-gating selectors, **guard-a-merging-action's-lifecycle-transition**, DI'd actions modules, capture/AudioWorklet gotchas, cascade WS client + lifecycle hardening, streaming-transcript normalization, recording orchestration + 3 concurrency edges, playback MSE-vs-blob. **`web/CLAUDE.md` cross-doc table** populated (first `web/src/types/` models: view models → ARCH-007, 4 streaming wire types + DTOs → ARCH-009/Appendix A; no `ARCHITECTURE.md` change — mirror-registration). Briefs `027`–`032`.
  - Scope shifts: none cut. Carry-forward: 3 added (D.6 metrics-sourcing [load-bearing, KEEP for D.6]; cascade `playback.started` not persisted → F.3/G.5; blob-fallback completeness → G), 2 client-side-mitigated + updated (model-allowlist; `CascadeTurnForm [Required]`), 2 spread to E (`'ending'`-gate, capture-reuse). The **D.6 metrics-sourcing** call (top-level latency deltas read the backend canonical `MetricsAggregator`/`GET /summary`, NOT a client recompute; cascade speechEnd→playback needs a client `turn.recording.stopped` browser stamp) is the key D.6 design input.
  - New blockers / operational: **FINDING — the RTK git hook silently swallows `git commit`** (HEAD doesn't move despite "ok"); the impl twice reported phantom hashes (`1e2f63a`, `7c3f2e9`). Mitigated by the user turning OFF Bash auto-mode (manual approval) + `command git commit -F` + `command git rev-parse` HEAD verification + one-Bash-per-message (bundling cascade-cancels under the `claude-opus-4-8`-classifier hiccup). Banked to memory `rtk-git-commit-swallow`. **The D.1–D.5 impl hit HARD-STOP (85%)** and cycles out at this seal; the **lead heartbeat went stale** mid-cycle — a fresh `web/` implementer must be re-spawned (lead/human) for D.6/D.7.
  - Next session target: **D.6 (live transcript/metrics/cost panels)** then **D.7 (error banner + the 2 PRD component tests)** — closes Phase D. Then Phase E (Realtime).
  - Reference: implementer session doc `006-2026-05-29-phase-d-d1-d5.md`; slice commits D.1 `d9d7b66` / D.2 `35e52a8` / D.3 `42bffc0` / D.4a `4acdb18` / D.4b `54f399b` / D.5 `8ba8fb6`; briefs `027`–`032`; web lessons §2–§12.

- **2026-05-30 — Phase D COMPLETE: D.6 + D.7 close the frontend (wholesale cycle).** Round sealed by this `docs(tasks)` commit.
  - Planning-level: **D.6** (the 3 live panels + the metrics infra: `deriveTurnMetrics` / `getSummary` / the `SessionSummary` mirror / the recording.started/stopped/completed stamps / `latencyEvents[]`+`cost` store retention) + **D.7** (ErrorBanner + actionable `errorCopy` + the jsdom/Testing-Library component-test env + the 2 ARCH-020 transitions render-verified). **Phase D is complete — the cascade runs end-to-end in the browser.** Web suite 79→97 green; security invariants PASS; every code-quality HIGH/MEDIUM fixed in-slice.
  - Decisions made: the **three-source metrics model** (per-stage from store; top-level client-timing deltas **frontend-computed** because the backend structurally can't for cascade — verified vs `MetricsAggregator`; session-avgs from `GET /summary`). The cascade **`playback.started` is frontend-display-only** (no client→server latency channel → `n/a` in the persisted/summary; → F.3). **Spec clarification (lead-approved):** mic-denied → "Start RE-ENABLED for retry" (not "Start disabled" — the imprecise ARCH-020/MVP_TASKS wording, which would trap the user against the "…and retry" copy + the D.2 retry-after-fail design); clarified in ARCH-020 + MVP_TASKS D.7.
  - Hot-routed this round: **web lessons §13** (three-source metrics) + **§14** (jsdom/Testing-Library setup + `errorCopy` never-raw) + index rows. **Cross-doc:** `web/CLAUDE.md` rows for `TurnViewModel`(+`latencyEvents`/`cost`) + `SessionSummary`/`ModeSummary`/`WerSummary`; **`ARCHITECTURE.md` ARCH-007 §4** TurnViewModel block edited (the 2 retention fields) + **ARCH-020** mic-denied wording clarified. Briefs `033`–`034`. Phase-D acceptance + Deliverable-map rows ticked.
  - New blockers / operational: the **RTK-commit-swallow FINDING** persisted this round (commits land via `command git commit -F` + HEAD-verify + one-Bash-per-message; auto-mode off). A **stale-heartbeat protocol drift** at the D.5 close was corrected (a 262s-stale `/context-check` read fired a premature HARD-STOP; corrective adopted — flag staleness + re-read before any cycle; memory `stale-heartbeat-cycle-guard`). Carry-forward: 2 D.6 G-defers added (the contradictory-frame status-overwrite [backend-protected]; unbounded store growth → G.4).
  - Scope shifts: none cut. **Wholesale cycle:** both teammates cycle out at this seal (orch was at 73% WARN, Phase D complete = the user-directed cycle point); the lead re-spawns a fresh team for Phase E.
  - Next session target: **Phase E (Realtime)** — E.1; needs real OpenAI Realtime keys; safety invariants #1/#2 live. The first real-key cascade smoke (C/D demo-checklist) remains outstanding.
  - Reference: implementer session doc `007` (D.6+D.7); slice commits D.6 `ce451aa`+`2d62cca` / D.7 `01727f3`; briefs `033`–`034`; web lessons §13–§14.

- **2026-05-30 — Phase E (Realtime) BACKEND COMPLETE: E.1 + E.2b + E.5-backend (E.2 by B.9c-ii); backend→frontend cycle.** Round sealed by this `docs(tasks)` commit.
  - Planning-level: drove the fresh backend team from the Phase-D seal through the entire Phase-E backend obligation. **E.1** ephemeral client-secret broker (`348ea76`, SAFETY — invariants #1/#2 LIVE + sentinel-pinned; GA `client_secrets` mint, model resolve+allowlist, one bounded 429 retry); **E.2b** realtime per-turn cost wiring (`a6bc540` — `EstimateRealtime`→`/complete`, was zero-callers; degrade-to-null, never a synthetic $0.00); **E.5-backend** stale-session flush (`f007a72` — Flow-H auto-end+persist on `Create` via `CreateAsync`). **E.2** (turn-events ingest) needed no code — B.9c-ii already delivered it (verified). Backend suite **284 → 316** green; E.1 security-reviewer PASS; all code-quality fixed in-slice.
  - Pre-orient FINDINGs (load-bearing): (1) E.2 already built by B.9c-ii (the `/events` endpoint persists normalized `LatencyEvent`s with `ClockSource`); (2) the realtime cost was **orphaned** in the breakdown ("→ E.4/E.5" but no subtask owned the backend touch) → pulled forward + inlined as **E.2b**; (3) the E.5 backend stale-flush pulled forward as the last backend slice. Surfaced to the lead; lead-approved the 2-backend-slice plan + the durable on-file record (peer message bodies were dropping → wrote the plan to MVP_TASKS, pinged one-line).
  - Decisions made: **GA response shape** — verified via Context7 that `client_secrets` returns **top-level** `{value,expires_at,session}` (NOT the legacy nested shape ARCH-010 had documented) → tolerant dual-shape parser + ARCH-010 correction (the §18/§20 verify-the-live-surface trap). **Realtime cost output-seconds fork → (a):** optional `CompleteTurnRequest.OutputAudioDurationMs?` forward-built for E.4; absent → output disclosed-unavailable **at the wiring layer** (the estimator silently zeroes a `?? 0` usage field). **`PricingBasis` is `"tokens"`** (the billing unit), not the input audio-seconds. **Stale-flush owed write uses `CancellationToken.None`** (a cancelled `POST /api/sessions` must not abort the prior session's persist → half-state); `CreateAsync` keeps flush orchestration in the service (ARCH-008).
  - Hot-routed this round: **server lessons §24** (broker pattern + verify-GA-shape) + **§25** (realtime cost wiring) + **§26** (stale-flush + `CancellationToken.None`) + 3 `server/CLAUDE.md` index rows; **ARCH-010** GA-shape correction, **ARCH-009 §6 + Appendix A** (`CompleteTurnRequest`+`outputAudioDurationMs?` + the E.1 token DTOs refined), **ARCH-014** realtime-cost realization, **ARCH-017** Flow-H realization. Briefs **035–037**. E.1/E.2/E.2b ticked; E.5-backend portion marked shipped.
  - Scope shifts: **E.2b created** (orphaned cost wiring inlined as a real backend subtask); **E.5 split** (backend stale-flush pulled forward; frontend WebRTC lifecycle stays in E.5). No scope cut. Carry-forward triage: 1 deleted (D.6, completed) + 2 updated + 6 added.
  - New blockers: none. Commit discipline held (`command git commit -F` + HEAD-verify, one-Bash-per-message); no safety finding escalated.
  - **Cycle:** lead-directed backend→frontend **wholesale transition** (backend Phase-E complete; context all-OK — a *planned*, not forced, cycle). Both teammates cycle out at this seal; the lead spawns a fresh orchestrator + a **`web/` frontend impl for E.3** (browser WebRTC client — consumes E.1's `ek_…` broker + E.2's events ingest, both documented).
  - Next session target: **E.3** (frontend WebRTC client). KEEP for E.3: capture-reuse-for-realtime. Early carry-forward for the frontend: **E.4 must report `outputAudioDurationMs`** (the realtime cost output side).
  - Reference: implementer session doc `008` (`233bfd2`); slice commits E.1 `348ea76` / E.2b `a6bc540` / E.5-backend `f007a72`; briefs `035`–`037`; server lessons §24–§26.

- **2026-05-30 — Phase E (Realtime) FRONTEND COMPLETE: E.3→E.4→E.5 + E.6 verify-and-tick; night wind-down at Phase-E-complete.** Round sealed by this `docs(tasks)` commit.
  - Planning-level: drove the fresh frontend team across the entire Phase-E frontend — the browser realtime path from nothing to reachable + audible + disconnect-resilient + mode-switch-clean. 5 slices: **E.3** `87d77d0` (transport library — `mintClientSecret`, the pure GA-event normalizer, the manual-smoke WebRTC handshake shell) → **E.4a** `7b63fc4` (data path — the per-turn event sink + `appendTurnEvents`) → **E.4b** `f2b5647` (turn control + RecordingControls + TranscriptPanel — **CLOSED the realtime reachability gap**, audible) → **E.5a** `a09c15e` (connection lifecycle — persistent pc, disconnect-surface, teardown) → **E.5b** `f95bea6` (mode-switch teardown, no double-mic). **E.6 verify-and-tick** (realtime model selection already realized across D.2/E.1/E.2b/E.3 — no slice). Web suite **97 → 157** green; security-reviewer PASS every slice; every code-quality HIGH/MEDIUM fixed in-slice.
  - Decisions made: **split E.4→a/b + E.5→a/b** (the D.4a/D.4b precedent — data-path vs UI-wiring; right-sized slices + context hygiene). **Normalize moved INTO the tested controller** (E.4b — shrank the manual-smoke shell). **`audioDelta` is TIMING-ONLY** (invariant #3 — realtime audio is the WebRTC media track, never stored). **Incremental-token accumulation** (realtime deltas are incremental vs cascade-cumulative — the impl caught a real correctness gap in my E.4a brief). **Latch-reset-on-failed-connect** (E.5a HIGH — a transient connect failure would otherwise have permanently bricked realtime mode). **E.4b audio-output gap** — my brief omitted the remote-track playback; the impl flagged it → interim detached `<audio>` + a once-stamped `playback.started` added.
  - **FINDING (re-sequenced, backend dependency):** Flow-G's `ModeTransitionEvent` **persistence** is blocked — the backend `RecordModeTransition` is built-but-UNWIRED (no endpoint) + the team was frontend-only → needs a backend `POST …/mode-transition` slice + a frontend POST + the TS mirror + the `modeTransitions` graduation. NOT deliverable-blocking (the comparison derives each turn's mode from the per-turn `mode` field). **Surfaced to the lead: the next cycle's team MUST include a backend implementer** (also for Phase F).
  - **Process FINDING (fixed):** the per-slice preflight (`web/CLAUDE.md`) omitted `format:check` → 9 realtime files drifted from Prettier (caught only at `/session-end`, fixed `5c14acb`). This round-seal **adds `format:check` to the preflight line** (first + non-optional) so it can't recur.
  - Hot-routed this round: **web lessons §15–§18** (realtime transport / data-path sink / turn controller / connection-lifecycle + mode-switch) + 4 `web/CLAUDE.md` index rows; the **`RealtimeToken` cross-doc mirror-registration row**; **4 ARCH-010 §7 realization notes** (E.3/E.4a/E.4b/E.5a). Briefs **038–042**. E.3/E.4(a,b)/E.5(a,b)/E.6 ticked; the **Deliverable-map Realtime row ❌→🟡**; Phase-E acceptance code-complete (live-observed half = demo-checklist).
  - Scope shifts: **E.4 + E.5 each split a/b**; **E.6 verify-and-tick** (no slice); the `ModeTransitionEvent` persistence + re-mint/auto-reconnect + LOW hardenings deferred (re-sequenced / G / ARCH-010-optional fallbacks). **No scope CUT** — all deferrals are documented re-sequences or optional fallbacks. Carry-forward triage: 4 discharged (failed-turn errors, double-connect, connecting-stamp, `<audio>` lifecycle), 1 re-sequenced (ModeTransitionEvent → backend), rest spread/kept.
  - New blockers: **the next phase needs a backend implementer** (Phase F + the ModeTransitionEvent persistence). Commit discipline held (`command git commit -F` + HEAD-verify, one-Bash-per-message); no safety finding escalated.
  - **Cycle:** lead-directed **night wind-down** at the Phase-E-complete milestone — a clean PHASE boundary (impl at ~0.9 slices to ACTION; a *planned*, not forced, pause). Team paused for the night; Phase F + G resume on the user's go with a backend implementer in the next team.
  - Next session target: **Phase F** (Evaluation, Summary & Comparison — WER endpoint hardening F.1, comparison summary) + the `ModeTransitionEvent` persistence backend slice. **Needs a backend impl.**
  - Reference: implementer session doc `009` (`66794d2`); slice commits E.3 `87d77d0` / E.4a `7b63fc4` / E.4b `f2b5647` / E.5a `a09c15e` / E.5b `f95bea6` (+ `style(web)` `5c14acb`); briefs `038`–`042`; web lessons §15–§18.

- **2026-05-30 — Phase F opened: F.1 evaluation endpoints (F.1a WER + F.1b transcribe). Backend→frontend area transition.** Round sealed by this `docs(tasks)` commit.
  - Planning-level: fresh backend team opened Phase F with the WER **Evaluation panel**'s backend — the three ARCH-009/015 endpoints the F.2 panel + F.3 summary consume. Split into **2 security-isolated commits** (one safety pin each): **F.1a** `7e29f35` (`GET /api/evaluation/phrases` + `POST /api/evaluation/wer`; pin = the ARCH-019 hypothesis-length DoS cap before `WerCalculator.Compute`'s n×m DP matrix) → **F.1b** `f9cf6a9` (`POST /api/evaluation/transcribe`, STT-only; pin = invariant-#5 upload validation before any provider call + invariant-#3 stateless/never-persist-audio). Backend suite **316 → 339** green (F.1a +16, F.1b +7); build 0W/0E; security-reviewer + code-quality-reviewer PASS both commits. Orchestrator ran a confirmatory `dotnet test` at the round-seal gate (339/339) given a mid-F.1b build-break-and-fix.
  - Decisions made: **Q1=(a) persist WER server-side** when `turnId` present (feeds F.3's session-JSON summary) — **reuse the existing `SessionStore.UpdateTurn` seam, NOT a new method → `SessionStore` unchanged**; `UpdateTurn` (unconditional) not `FinalizeTurn` (refuses an already-terminal turn). **Hypothesis cap in the SERVICE** (the single WER chokepoint, unit-pinned via an injected compute delegate) as a **domain 400, not a `[MaxLength]`** (which would ProblemDetails-bypass the chokepoint); `Hypothesis` stays `string?` (empty = valid WER). **Transcribe STT-only + stateless** — wrap the blob as a 1-frame `AudioFrame` through the same `ISttProvider` REST path; **join multiple `SttFinal`s** (never last-only → truncated hypothesis). **Reuse `CascadeUploadValidation`** across both audio routes + set the framework body-limit backstop to `max(cascade, eval)`.
  - Orchestrator-added test rigor at Step-2.5 (both landed): an **asymmetric WER test** (reference≠hypothesis — pins arg-order/source-from-store; every symmetric perfect-match test misses a `Compute(ref,hyp)` swap) + a **multi-`SttFinal`-join test** (prevents silent hypothesis truncation).
  - Hot-routed this round: **server lessons §27** (cap-in-service chokepoint + reference-from-store + `UpdateTurn`-not-`FinalizeTurn`) + **§28** (STT-only transcribe + join-finals + reuse-validation + body-limit-max) + 2 `server/CLAUDE.md` index rows; **ARCH-009** F.1a+F.1b realization notes, **ARCH-018** +`evaluation.invalid_phrase`/`.phrase_not_found`/`.invalid_request`, **Appendix A** Wer+Transcribe DTOs concrete, **ARCH-028 + `.env.example`** +`EVAL_MAX_UPLOAD_BYTES`, ARCH-016/019 realization notes. Brief **043**. F.1 ticked.
  - Scope shifts: none cut. SttLanguage-BCP-47 fragility + 2 declined-reviewer-LOW cleanups (`evaluation.invalid_request`→`ForCode`; `OneFrameAsync` extraction) inlined as a **G.4 opportunistic-cleanup task** (flagged-not-dropped). No deferment escalated.
  - **Process FINDING (banked):** repeated same-turn hash/count mis-transcriptions this slice — **two phantom hashes** (`4be4c43`, `6256d77`; real `7e29f35`/`f9cf6a9`) + miscounts (339→"338/1"→"341"; true 339). Each caught by the orchestrator independently verifying `git log`/`dotnet test` rather than trusting the reported figure. Rule banked to memory `rtk-git-commit-swallow`: never quote a count/hash in the same turn as the producing command — read the literal output first.
  - New blockers: none. Commit discipline held (`command git commit -F` + HEAD-verify, one-Bash-per-message; orchestrator resolved a lead-vs-impl hash discrepancy by verifying HEAD = `f9cf6a9`).
  - **Cycle:** lead-directed **backend→frontend area transition** (F.2/F.3 are frontend) — the user cancelled an earlier pause-after-F.1 and chose to continue. The **orchestrator persists** (holds the F.1 endpoint context the panel needs); the lead spins down the backend impl + spawns a fresh **`web/` frontend impl for F.2**.
  - Next session target: **F.2** (frontend `EvaluationPanel` — consumes `GET /phrases`, `POST /transcribe`, `POST /wer`; persists WER via the turn). Then **F.3** (`ComparisonSummary`). Still backend-dependent for later: the `ModeTransitionEvent` persistence slice (carried from Phase E).
  - Reference: implementer session doc `010` (`52456bb`); slice commits F.1a `7e29f35` / F.1b `f9cf6a9`; brief `043`; server lessons §27–§28.

- **2026-05-31 — Phase F COMPLETE: F.2 EvaluationPanel + F.3 ComparisonSummary (frontend).** Round sealed by this `docs(tasks)` commit.
  - Planning-level: the frontend team closed Phase F — the evaluation/comparison UI consuming F.1's endpoints. **F.2** `f1697bc` (the standalone Flow-D WER `EvaluationPanel`: select→record→`POST /transcribe`→`createTurn`→`POST /wer` w/ turnId to score **and persist**) → **F.3** `f7daae1` (the Flow-E `ComparisonSummary`: by-mode latency/cost/errors/turns/WER from `GET /summary` + the per-model-variant cost split derived frontend-side). **web suite 157 → 193 green** (F.2 +17, F.3 +19); no safety pin either slice (eval/read-only views); both fresh-eyes code-quality reviewed (F.2: 4 MED fixed/1 declined-w-evidence; F.3: 1 HIGH non-finite-NaN guard + 3 MED fixed/1 declined-w-precedent). **Phases A–F all done — both modes + the full evidence layer ship.**
  - Decisions made: **F.2 persists WER via a DEDICATED backend-only eval turn** (`createTurn`→`POST /wer` w/ turnId) — the only path that reaches the session JSON for F.3's `WerSummary`; the frontend interpretation-turn machine stays untouched. **Eval-turn `turnCount` skew → the USER REQUESTED the backend fix (must-do F.4):** the eval turn inflates `ModeSummary.TurnCount` (quality averages null-skipping + unaffected; `WerSummary` exact). At the seal this was a documented-limitation default; **the user (via lead, 2026-05-30) then REJECTED document-only and required the clean backend exclusion → tracked as the must-do F.4 slice** (`IsEvaluation` marker + `SummarizeMode` exclusion; lands before G.5/demo; F.3 frontend unchanged — it auto-corrects). **F.3 two-source-no-double-truth:** `/summary` owns per-mode + WER; `/session`'s persisted turns own the per-variant split (the §13 frontend-aggregation precedent — `ModeSummary` is per-mode only). **The impl caught a load-bearing wire detail:** the persisted turn serializes cost as **`costEstimate`** (C# property name), NOT the frontend `TurnViewModel.cost` — reading the wrong one silently empties the breakdown (pinned by a direct `toComparisonTurn` test). **F.3 Q5 (cascade end-to-end latency channel): accept `n/a` + document (G.5)** — NOT escalated (the comparison stays rich; a channel is a cross-area cascade-WS change).
  - Orchestrator-added test rigor at Step-2.5 (all landed): F.2 — a `createTurn`-failure arm (the Q1-introduced step) + a gate-closed-without-session test; F.3 — a direct `toComparisonTurn` wire-`costEstimate` pin (incl. the `cost`-only-no-`costEstimate` silent-break case) + a null-vs-`[]` distinct-degradation render. Verified F.3 green myself at the gate (193/193) given the phase boundary.
  - Hot-routed this round: **web lessons §19** (dedicated-eval-turn WER persist) + **§20** (recordBlob reuse / DI'd-flow-returns-transient) + **§21** (per-variant frontend aggregation; the `costEstimate`≠`cost` gotcha; two-source-no-double-truth) + 3 `web/CLAUDE.md` index rows; **2 cross-doc rows** (the 5 F.2 eval mirrors + `TranscribeParams`; the F.3 `ComparisonTurn` projection — both mirror-registrations, NO `ARCHITECTURE.md` contract change); **G.5 limitation note** (cascade `n/a`; the eval-turn `turnCount` note was **subsequently corrected** — the user required the F.4 backend fix, so turnCount is NOT a write-up limitation). Briefs **044–045**. F.2/F.3 + the Phase-F acceptance criteria ticked. **(Post-seal: F.4 backend eval-turn-exclusion added per user decision — `69a77cd`; brief `046`.)**
  - Scope shifts: none cut. Carry-forward triage: **4 deleted** (3 F.2/F.3 working-set items shipped + the cascade-`playback.started` item resolved by F.3's Q5 decision), 2 markers re-pointed to G (`outputAudioDurationMs`, cached-audio-input — F.3 is read-only, didn't add the realtime-output measurement), 1 added (orphaned-eval-turn cleanup → G), ~17 kept (G/demo-checklist/backend-`ModeTransitionEvent` — Phase-G-distributed). No deferment escalated.
  - New blockers: none. Commit discipline held (`command git commit -F` + HEAD-verify, one-Bash-per-message; F.1a/F.1b/F.2/F.3 hashes all independently `git log`-verified — the impl's two F.1b/F.2 phantom transcriptions were caught + corrected, the commits themselves always landed clean). The lead's heartbeat is stale (status-line not writing — flagged, non-blocking).
  - **Cycle:** Phase F closed cleanly. The team continues (user cancelled the earlier pause). Context all-OK at the seal (orch ~38%, frontend impl ~30%) — no auto-cycle. Next is **Phase G** (docs/demo/stability/write-up); G.1/G.2/G.5 are docs (orchestrator-authorable), G.4 has the accreted backend cleanups (a backend impl if those are pulled in) + the frontend 5-min stability run.
  - Next session target: **Phase G** — start with G.5 (the comparison write-up, now that real measured values are producible) and/or G.1 (README); the standing real-key smokes feed both.
  - Reference: implementer session doc `011` (`41eaf86`); slice commits F.2 `f1697bc` / F.3 `f7daae1`; briefs `044`–`045`; web lessons §19–§21.

- **2026-05-31 — F.4 eval-turn exclusion (Phase F FULLY complete) + Phase-G no-live-data docs drafted.** Round sealed by this `docs(tasks)` commit.
  - Planning-level: closed Phase F with the **user-requested** backend fix. **F.4** `1e8a655` — `InterpretationTurn` gains a trailing `IsEvaluation` bool, set server-side at `POST /wer` (atomic with the `WerResult` in the one `UpdateTurn` transform); `SessionSummaryService.SummarizeMode` excludes eval turns from the per-mode `ModeSummary` (`TurnCount` + averages + `ErrorCount`); `SummarizeWer` keeps them. **F.3's comparison turn counts are now EXACT with ZERO frontend change** (F.3 reads `ModeSummary.TurnCount` live from `/summary`). Backend suite **339→345 green**; security PASS; build/format clean (orchestrator confirmatory re-run at the gate: 345/345). **Phases A–F all done.**
  - Decisions made: **the user (via lead, 2026-05-30) REQUIRED the clean backend exclusion + REJECTED document-only** (F.2 Q1) — F.4 is that must-do (NOT a deferred limitation; I corrected 4 stale "deferred/document-only" records at `a6745cb` when the lead flagged them). **Top-level `SessionSummary.TurnCount` counts ALL turns (incl. eval); only per-mode excludes** → top-level ≠ sum-of-per-mode (delta = eval turns) — documented in the ARCH-013 note + the G.5 write-up presentation so no reader is confused. **Explicit marker, not a `has-WerResult` heuristic** (couples two concepts). **Set-at-`/wer`** keeps F.2 byte-identical; the failure-path orphan stays the bounded carry-forward residual.
  - Scope shifts: **F.4 ADDED to Phase F by user decision** (was a documented limitation; promoted to a must-do backend slice). **No cut.** Carry-forward triage (F.4-close): 0 deleted / 0 deferred / 0 inlined / 1 updated (the orphan item → confirmed failure-path residual) / 0 added / rest KEEP.
  - Hot-routed this round: **server lesson §29** (explicit-marker-for-auxiliary-turns + the keep-the-sentinel-fixture-current security meta-rule) + the `server/CLAUDE.md` index row; **ARCH-005** `InterpretationTurn` + `IsEvaluation`; **Appendix A** `InterpretationTurn` row; the **ARCH-013** top-level-vs-per-mode semantic realization note; the `server/CLAUDE.md` ARCH-005 cross-doc row annotation. Brief `046`. F.4 + the Phase-F acceptance ticked.
  - **Phase-G no-live-data docs (this round also folds the parallel orchestrator-authored docs, committed earlier at `576078d`):** **G.1 `README.md`** (clean-clone-to-run front door — all 12 spec'd elements; ticked `[x]`); **G.2 `docs/AI_COLLABORATION.md`** (the "how AI was directed" narrative; **user-confirmed form** — keep as a separate artifact cross-referencing the operational `CLAUDE.md`/`AGENTS.md`; ticked `[x]`); **G.5 `docs/COMPARISON_WRITEUP.md`** (SCAFFOLDED — structure + methodology + limitations + recommendation/language-pair frameworks; `[SMOKE: …]` placeholders; `[~]`). The README front-door links the write-up + the AI-collaboration narrative (user-requested grader check).
  - New blockers: none. The real-data Phase-G items (G.3 demo script, G.5 real numbers, G.4 5-min stability) are **HELD for the user's real-key smoke** (the one genuine user gate — provider keys + a live run). Commit discipline held (`command git commit -F` + HEAD-verify, one-Bash-per-message; F.4 `1e8a655` independently `git log`-verified).
  - **Cycle:** Phase F closed; both tracks (the F.4 backend slice + the no-live-data G docs) advanced per the user's "docs + backend now, smoke later." Context all-OK at the seal. The backend impl's next work is smoke-gated / optional (the `ModeTransitionEvent` slice).
  - Next session target: **Phase G real-data items** — once the user runs the real-key smoke, fill the G.5 `[SMOKE: …]` numbers + author G.3 (demo script) + run G.4 (5-min stability). The C/D + realtime GA-shape smoke confirms feed the same run.
  - Reference: implementer session doc `012` (`1bb8ed2`); slice commit F.4 `1e8a655`; brief `046`; server lesson §29; the G-docs commit `576078d`.

- **2026-05-31 — Real-key smoke prep + Phase H added (user-requested) + team pause for the user's UI design.** Orchestrator-only docs round (no `/tdd` cycle); sealed by this `docs(tasks)` commit.
  - Planning-level: at the user's direction (via lead), prepped + supported the **real-key smoke** and opened the **UI/UX styling** scope, then paused the team for the user to design the UI in Claude Design (style-first).
  - **Real-key smoke runbook** `86be724` — `docs/runbooks/real-key-smoke-runbook.md` (comprehensive, copy-pasteable: prereqs/keys, start+verify, the T1–T7 test matrix, the 5-min ARCH-020 stability, a fill-in capture table → the G.5 `[SMOKE: …]` placeholders + acceptance checklist, the post-session safety grep, troubleshooting+fallbacks). The lead drove the live run against it.
  - **FINDING (`.env` loader)** `f4526ef` — the backend has no `.env` loader (.NET reads process env only), so `cp .env.example .env && dotnet run` left keys unloaded (`configured:false`), breaking clean-clone-from-README (ARCH-021/029). Lead worked around it via `set -a && source .env && set +a`. (a) doc-correctness fix landed (runbook §2 + README §2 use the working form); (b) tracked as **G.2b** — a small backend slice to add `DotNetEnv`/`Env.Load()` Development-only so a plain `dotnet run` auto-loads `.env` (backend impl, post-resume; then simplify the docs).
  - **Phase H (UI/UX styling) ADDED (user-requested)** `62c88e8` — the frontend is functionally correct but has NO CSS (no visible active-mode state → the toggle reads as broken). H.1 = a first usability/visual pass (design-system baseline + visible mode/status feedback + legible panels); H.2+ = later polish. TDD posture: visual styling is **manual-smoke-exempt** (a styling slice, not `/tdd`); clean-separation holds (CSS/markup-only). Brief `047` drafted. **`docs/design/ui-design-spec.md`** (lead-authored, codebase-grounded — the full component/state-machine/layout/flows contract) is the input to the user's Claude Design pass; folded into this seal.
  - Scope shifts: **Phase H added** (user-requested UI/UX) + **G.2b added** (the `.env`-loader Finding). No cut. Carry-forward: unchanged (no new triage — orchestrator-only docs round).
  - New blockers: none. The team **PAUSES** here for the user's UI design (style-first); the remaining Phase-G real-data items stay smoke-gated. Commit discipline held (`command git commit -F` + HEAD-verify, one-Bash-per-message; hashes `86be724`/`f4526ef`/`62c88e8` all verified).
  - **Cycle:** lead-directed **team pause** (user designing the UI in Claude Design). The lead runs `/team-end` (handoff) + spins the orchestrator + backend impl down; a **fresh team resumes for H.1** when the user returns with their design. Nothing in flight (backend impl `/session-end`-closed at F.4; H.1 drafted-not-dispatched).
  - Next session target (resume): **H.1 — UI baseline** (apply the user's Claude Design output as the CSS baseline, per brief `047` + `docs/design/ui-design-spec.md`) → then the smoke-gated Phase-G items (G.3 / G.5 numbers / G.4) + G.2b.
  - Reference: runbook `86be724`; `.env` Finding `f4526ef`; Phase H + brief `047` `62c88e8`; `docs/design/ui-design-spec.md` (this seal). No implementer session doc (orchestrator-only round).
